# 14-3 Implement listUncompletedTasks and listProjects tools

## Description
Build simplified server handlers that list uncompleted tasks and projects with optional filters and grouping, providing streamlined outputs for frequent workflows.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-10-01 00:20:00 | Created | N/A | Proposed | Task defined for listing tools implementation | AI_Agent |

## Requirements
- Implement `listUncompletedTasks` handler with optional filters (`projectId`, `onlyFlagged`, `onlyAvailable`).
- Implement `listProjects` handler with optional switches (`folderId`, `listProjectNames`, `listByFolder`).
- Ensure outputs include required metadata (IDs, names, flagged status, dates, task IDs).
- Reuse existing serializers for task/project metadata where possible to avoid duplication.
- Handle absence of optional data gracefully (e.g., missing due date).

## Implementation Plan
- Extend simplified tool registry to register new listing tools.
- Reuse consolidated task query utilities to fetch uncompleted tasks respecting filters.
- Implement folder grouping helper for `listByFolder` option.
- Add smoke/integration tests covering filter combinations and grouped outputs.

## Verification
- `npm run build` passes with new listing handlers.
- Smoke script verifies core outputs for defaults and optional flags.
- Manual MCP invocation confirms correct grouping and metadata fields.

## Files Modified
- `server-mcp-simplified.ts`
- Shared serializers/utilities as required
- `docs/delivery/14/14-3.md`

[Back to task list](./tasks.md)
