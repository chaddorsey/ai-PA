# 14-2 Implement markTaskCompleted and moveTaskToProject tools

## Description
Implement dedicated handlers for task completion and moving tasks between projects within the simplified server, leveraging existing bridge utilities and ensuring robust validation.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-10-01 00:20:00 | Created | N/A | Proposed | Task defined for completion and move tools implementation | AI_Agent |

## Requirements
- Add handler for `markTaskCompleted` accepting `taskId`.
- Add handler for `moveTaskToProject` accepting `taskId` and `projectId`.
- Validate existence of referenced entities; return errors when invalid.
- Reuse transaction/bridge helpers to perform operations.
- Return success payloads with task/project IDs and updated status indicators.

## Implementation Plan
- Extend simplified server registry to include new tools referencing dedicated handlers.
- Implement handlers calling existing OmniFocus bridge functions (`completeTask`, `moveTaskToProject`).
- Use shared serializer to wrap success responses; convert thrown errors to JSON-RPC errors.
- Add unit/integration smoke tests verifying success and failure cases.

## Verification
- `npm run build` passes with new handlers included.
- Smoke tests demonstrate successful task completion and move operations.
- Manual MCP calls confirm error messages for missing/invalid IDs.

## Files Modified
- `server-mcp-simplified.ts`
- `bridge` helpers as needed
- `docs/delivery/14/14-2.md`

[Back to task list](./tasks.md)
