# 11-3 Propagate planned dates through MCP server

## Description
Extend the TypeScript bridge, schemas, and HTTP handlers to surface `plannedDate` and `effectivePlannedDate` fields in MCP responses.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-09-29 01:20:00 | Created | N/A | Proposed | Task file initialized | AI_Agent |

## Requirements
- Update TypeScript interfaces/Zod schemas to include planned date fields.
- Ensure HTTP bridge responses expose planned dates for tasks.
- Maintain consistent ISO string formatting and optional field handling.

## Implementation Plan
- Modify `bridge.ts` typings and shared interfaces.
- Adjust Zod schemas/serialization transforms to include planned dates.
- Rebuild TypeScript output and run smoke tests hitting MCP endpoints.

## Verification
- `listRemaining` and related commands include planned date fields when present.
- TypeScript compilation succeeds without errors.
- Manual MCP tests confirm planned dates propagate end-to-end.

## Files Modified
- `bridge.ts`
- `dist/` outputs (via build)

[Back to task list](./tasks.md)
