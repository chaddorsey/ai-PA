# 11-2 Expose planned dates in plugin payloads

## Description
Update the OmniFocus MCP plugin to include `plannedDate` and `effectivePlannedDate` in serialized task/project data, handling nullable values safely.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-09-29 01:20:00 | Created | N/A | Proposed | Task file initialized | AI_Agent |

## Requirements
- Add planned date fields to task serialization helper(s).
- Ensure ISO formatting and null safety for `plannedDate`/`effectivePlannedDate`.
- Confirm planned dates are included wherever tasks are serialized (lists, lookups, conversions).

## Implementation Plan
- Extend shared timestamp utilities to cover planned date conversion.
- Update relevant plugin serializers and confirm bundling.
- Manually verify plugin responses via Automation Console.

## Verification
- Plugin outputs `plannedDate` and `effectivePlannedDate` for tasks that possess them.
- Regression spot-check for existing timestamp fields.

## Files Modified
- `extra-files/omnifocus-mcp.omnijs`

[Back to task list](./tasks.md)
