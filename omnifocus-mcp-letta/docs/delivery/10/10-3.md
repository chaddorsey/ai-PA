# 10-3 Propagate timestamps through MCP server

## Description
Ensure the Node-based MCP server forwards newly exposed timestamps without schema or typing issues.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-09-28 22:20:00 | Created | N/A | Proposed | Task file created | AI_Agent |
| 2025-09-28 23:58:00 | Status Change | Proposed | InProgress | Start propagating timestamps through MCP server | AI_Agent |
| 2025-09-29 00:20:00 | Status Change | InProgress | Review | Bridge schemas updated; timestamp serialization verified | AI_Agent |

## Requirements
- Update TypeScript interfaces/Zod schemas to document `added`/`modified` fields.
- Confirm `callOmniFocus` responses include timestamps and remain valid JSON.
- Adjust any downstream formatting helpers or response wrappers.

## Implementation Plan
- Extend relevant interfaces in `bridge.ts` or related modules.
- Regenerate `dist` output and verify HTTP bridge responses.

## Verification
- Run sample MCP commands (`listRemaining`, `getTask`, etc.) and confirm timestamps propagate.
- TypeScript build (`npm run build`) completes without errors.
- Manual MCP call returns ISO strings for `added` and `modified`.

## Files Modified
- `bridge.ts`
- `server.ts` (if schema adjustments required)

[Back to task list](./tasks.md)

