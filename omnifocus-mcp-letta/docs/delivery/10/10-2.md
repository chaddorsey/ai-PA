# 10-2 Expose timestamps in plugin JSON payloads

## Description
Extend the OmniFocus plugin serialization logic so each dated entity includes `added` and `modified` fields formatted as ISO 8601 strings.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-09-28 22:20:00 | Created | N/A | Proposed | Task file created | AI_Agent |
| 2025-09-28 22:45:00 | Status Change | Proposed | InProgress | Begin updating plugin serializers | AI_Agent |
| 2025-09-28 23:55:00 | Status Change | InProgress | Review | Timestamp fields added; manual verification via `listRemaining` | AI_Agent |

## Requirements
- Update task/project/folder/tag/perspective serializers to append timestamps.
- Handle `null` for unsaved entities per API semantics.
- Ensure AppleScript/JS bridge returns consistent field names.

## Implementation Plan
- Modify plugin helper functions to read `added`/`modified` and convert to ISO strings.
- Add shared utility to avoid duplication.
- Validate responses locally via Automation Console.

## Verification
- Manual MCP `listRemaining` call shows `added`/`modified` ISO timestamps for outstanding tasks.
- No regressions observed in OmniFocus automation responses.

## Files Modified
- `extra-files/omnifocus-mcp.omnijs`

[Back to task list](./tasks.md)

