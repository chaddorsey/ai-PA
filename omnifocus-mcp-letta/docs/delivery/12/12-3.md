# 12-3 Implement simplified tool server with extended functionality

## Description
Build the new MCP server entry point that exposes only the simplified tool surface while incorporating freshness-aware sorting and planned-date/timestamp metadata.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-09-29 02:25:00 | Created | N/A | Proposed | Task stub created | AI_Agent |
| 2025-09-29 03:38:00 | Status Change | Proposed | InProgress | Begin implementing simplified server | AI_Agent |
| 2025-09-29 04:45:00 | Status Change | InProgress | Review | Simplified server entry point implemented with freshness options | AI_Agent |

## Requirements
- Implement consolidated tool handlers drawing from existing bridge functions.
- Ensure responses include `added`, `modified`, `plannedDate`, `effectivePlannedDate` where available.
- Add optional `sortOrder: freshness` parameter to relevant list endpoints.
- Update health check / transport wiring for simplified server.
- Provide configuration flag/command to switch between simplified and full tool servers.
- Reuse shared schemas/types established in task 12-2.

## Implementation Plan
- Reuse or adapt `server-mcp-simplified-8124.ts` as baseline.
- Hook into shared serializers to avoid duplication (e.g., `serializeTask`).
- Implement conditionals to flip between standard/freshness ordering.
- Normalize incoming MCP messages (e.g., remap `client` → `clientInfo`) before passing to transport.
- Add tests or smoke scripts verifying sorted outputs.
- Expose simplified server via new npm script (e.g., `start:simplified`).
- Ensure build outputs include new entry point (e.g., `dist/server-simplified.js`).
- Added new npm script `start:simplified` targeting the simplified server bundle.
- Ensured `tsconfig.json` and build output include `server-mcp-simplified.ts` and shared `schemas.ts`.

## Verification
- `npm run build` passes.
- Manual MCP calls confirm simplified tool list and freshness functionality (initialize + `taskOperations.list` with freshness sorting).
- Smoke script covers new endpoints.
- Legacy `start:http` entry point continues to work with full tool surface.

## Files Modified
- `server-simplified.ts` (new entry point).
- `bridge.ts` / serializers as needed.
- Smoke/test scripts.

[Back to task list](./tasks.md)

