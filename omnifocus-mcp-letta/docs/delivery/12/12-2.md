# 12-2 Design consolidated tool schemas with freshness options

## Description
Define updated schemas and response shapes for each simplified tool, incorporating planned/freshness metadata and consistent detail-level handling.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-09-29 02:20:00 | Created | N/A | Proposed | Task stub created | AI_Agent |
| 2025-09-29 03:10:00 | Status Change | Proposed | InProgress | Begin schema planning | AI_Agent |
| 2025-09-29 03:35:00 | Status Change | InProgress | Review | Draft schema outline captured in PRD | AI_Agent |
| 2025-09-29 04:10:00 | Status Change | Review | InProgress | Implementing shared schema enums and server wiring | AI_Agent |
| 2025-09-29 05:05:00 | Status Change | InProgress | Review | Freshness-aware minimal list verified via HTTP transport; schema updates in place | AI_Agent |

## Requirements
- Update JSON schema definitions for each tool to support `detailLevel` and optional `sortOrder` (freshness vs. default).
- Specify when freshness sorting is applicable (task lists, project lists, tag lists, perspectives).
- Document defaults ensuring backwards-compatible behavior for existing consumers.
- Ensure response payloads surface `added`, `modified`, `plannedDate`, `effectivePlannedDate` consistently across derived schemas.
- Define shared enums/interfaces (`DetailLevel`, `FreshnessSortOrder`) for reuse in TypeScript/JSON schema.

## Implementation Plan
- Draft schema updates for each tool category and review for duplication.
- Define shared TypeScript types/constants (e.g., `FreshnessSort`, `DetailLevel`).
- Capture expected request/response examples (append to PRD or reference doc).
- Review with stakeholders before implementation.
- Capture mapping table results from task 12-1 in schema appendix (see PRD notes).
- Document opt-in pattern for freshness (e.g., `sortOrder: "freshness"`) and ensure default ordering remains unchanged unless requested.

## Verification
- Schema diff approved (tracked in PRD).
- Generated TypeScript types align with schema updates.
- Sample responses validated against bridge serializers and HTTP tool call (`taskOperations.list` with `detailLevel: minimal`, `sortOrder: freshness`).
- Schema outline documented under “Schema Design Outline” section in PRD.

## Files Modified
- This task file (notes).
- `docs/delivery/12/prd.md` (planning references).
- `schemas.ts` (new shared enums for detail level and sort order).
- `server-mcp-simplified.ts` (schema usage).

[Back to task list](./tasks.md)

