# Unit 4 execution brief — raw-WS client-core (multi-surface continuity M1)

**Post-clear context.** Resume on git branch `feat/msc-app-server-sole-owner`.
Authority = the **Unit 4** section of `docs/plans/2026-08-12-multi-surface-continuity-m1-web-terminal-plan.md`.
Protocol frames (empirically verified, letta 0.30.19, `ws://127.0.0.1:4577/ws`)
= `docs/plans/2026-08-12-multi-surface-ws-spike-findings.md` **Sections C & E**.
Phase A (Units 1–3) is DONE and committed. Do only Unit 4.

**Target.** Build TS package `clients/letta-continuity-core/` — a raw-WS client
speaking the App Server protocol on ONE ordered connection. Modules (per plan):
`ws.ts` (loopback connect, no auth, **server-version assertion at hello**),
`protocol.ts` (**sole** home of every frame type/shape + `request_id`-keyed RPC
for `conversation_*`), `stream.ts` (single stream ordered by **`event_seq`**:
`stream_delta`/`update_subagent_state`/`update_queue`/`turn_finished`; boundaries
from `turn_finished`/`loop_status`), `catchup.ts` (`conversation_messages_list`
snapshot + **message-id watermark** dedup — NOT `event_seq`), `connection.ts`
(connected/reconnecting/disconnected), `pointer.ts` (read a durable
`{agent,conversation}` state file). NO SDK dep, NO observer side-channel, NO
arbitration/flock. Approvals: the **injecting** client auto-sends
`approval_send=deny` (fail-closed); observers never respond.

**Completion metrics (ALL required).**
1. All modules exist; `protocol.ts` is the only place frame strings live.
2. A **committed contract test** round-trips every used frame and fails loudly
   on drift (the server-upgrade gate).
3. Passing tests: ordered single-stream render incl. a foreign turn; reconnect
   message-id-watermark dedup (no dup/loss); `conversation_list`/`conversation_create`
   RPC; concurrent sends server-serialized; approval fail-closed.
4. Typecheck + lint clean.
5. Committed on the branch (conventional message + CLAUDE.md footer). Mark
   Unit 4 `[x]` in the plan.

**Boundary.** ONLY the Unit 4 library. Do NOT build Unit 5 (terminal) / Unit 6
(web) / any server-side unit; do NOT deploy, `launchctl`, or touch the live
enrichment service or `lc-local-backend`. Any live smoke check uses ONLY the
docs agent (`agent-local-3898b33a-2249-4f1c-9478-26a9aad26d4a`), a benign
no-tool prompt ("Reply with exactly: OK. No tools."), the `default`
conversation, loopback — never MC or other agents. If `:4577` is unreachable,
SKIP live checks and rely on unit tests (record the skip); never start a server.

**Bail-out** (STOP; write a short note to `docs/plans/2026-08-12-unit4-notes.md`;
do NOT loop): protocol frames diverge from the findings doc; a step needs user
approval or secrets; tests can't pass after 3 focused attempts; or any live
check would hang.

**Autonomy + clean finish.** Make reasonable decisions without asking — never
block on confirmation. Every WS/network op MUST have a timeout; no unbounded
waits, no retry loops. Prefer mockable unit tests over live calls so the run
completes deterministically offline.

**Conclude.** Run `/code-review` on the branch diff; apply safe fixes only;
then print a summary listing which completion metrics passed/failed. Then STOP.
