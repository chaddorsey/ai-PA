# 21-1 Define orchestration tool interface & schemas

[Back to task list](./tasks.md)

## Description
Specify the single Letta tool `orchestrate_scheduling` with typed args/docstring or Pydantic models. Define the input/output schemas (SchedulingProblem, Event, Proposal, MovedEvent, ObjectiveScores, ResponseEnvelope).

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-11-18 10:00:00 | Created | N/A | Proposed | Task file created | AI_Agent |
| 2025-11-18 10:30:00 | Status Change | Proposed | InProgress | Started implementation - created schemas and function stub | AI_Agent |
| 2025-11-18 10:45:00 | Status Change | InProgress | Review | Completed: All Pydantic models created, function stub with docstring, example payloads provided. Schemas validate and serialize correctly. | AI_Agent |
| 2025-11-18 11:00:00 | Status Change | Review | Done | Task approved and committed. All schemas validated, function stub complete with comprehensive docstring. | User |

## Requirements
- **Inputs**: `utterance` (str), `events_by_participant` (dict), `context_json` (dict|None).
- **Outputs**: `status` (ok/unsat/bad_input), `proposals[]` (list of Proposal objects), `explanation` (str), optional `relaxations[]` (on UNSAT), `debug` (dict).
- All fields documented with type hints and descriptions; schema renders correctly in Letta ADE/SDK.
- Pydantic models must support JSON serialization for Letta tool schema generation.

## Implementation Plan
1. Draft Pydantic models:
   - `Event`: Represents a calendar event with id, title, start, end, locked/protected/flexible flags
   - `SchedulingProblem`: Encodes the scheduling request (participants, duration, time window, preferences)
   - `Proposal`: A proposed meeting slot with start/end times, participants, moved events, objective scores
   - `MovedEvent`: Details about an event that needs to be moved to accommodate the proposal
   - `ObjectiveScores`: Breakdown of optimization objective values (moved minutes, focus block bonus, preference penalty)
   - `ResponseEnvelope`: Complete tool response with status, proposals, explanation, relaxations, debug info
2. Write the function stub with comprehensive docstring (Letta auto-schema generation):
   ```python
   def orchestrate_scheduling(
       utterance: str,
       events_by_participant: Dict[str, List[Event]],
       context_json: Optional[Dict[str, Any]] = None
   ) -> ResponseEnvelope:
       """Orchestrate scheduling by finding optimal meeting times..."""
   ```
3. Provide example payloads for integration tests:
   - Sample `events_by_participant` structure
   - Sample `context_json` with working hours, preferences, policy rules
   - Expected response structure for successful and UNSAT cases

## Verification
- Tool visible in Letta ADE; schema shows correct field types and docs
- Round-trip JSON validation passes for all Pydantic models
- Example payloads validate against schemas
- Docstring generates correct OpenAPI-style schema in Letta

## Files Modified
- `letta/scheduling_orchestrator/__init__.py` (new file - package initialization)
- `letta/scheduling_orchestrator/schemas.py` (new file with Pydantic models: Event, SchedulingProblem, Proposal, MovedEvent, ObjectiveScores, ResponseEnvelope, Relaxation, DebugInfo)
- `letta/scheduling_orchestrator/orchestrate_scheduling.py` (new file - function stub with comprehensive docstring)
- `letta/scheduling_orchestrator/example_payloads.py` (new file - example inputs and expected outputs for testing)
- `docs/delivery/21/21-1.md` (this file)
- `docs/delivery/21/tasks.md` (updated task status)

