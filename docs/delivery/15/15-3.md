# 15-3 Align server detail-level handling

## Description
Update the simplified MCP server handlers so detail-level selections propagate to project responses and quick tools group projects reliably by folder.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-10-08 14:25:00 | Created | N/A | Proposed | Task documented during PBI 15 planning | AI_Agent |

## Requirements
- Pass requested `detailLevel` and completion-state filters through `projectOperations` and quick tools to leverage enriched serializer outputs.
- Ensure `listProjects` respects `listProjectNames`, completion filters, and `listByFolder` without overwriting `taskIds` unintentionally.
- Confirm minimal, standard, full detail levels map to distinct payloads after pruning helpers run.
- Handle root-level projects consistently when grouping by folder.

## Implementation Plan
- Modify `server-mcp-simplified.ts` to forward detail-level and completion-filter args and adjust quick tool logic.
- Update helper functions (`toProjectSummary`, grouping reducers) to use new folder metadata and completion flags.
- Add safeguards around optional task hydration to avoid unnecessary bridge calls.

## Verification
- Smoke test calling `projectOperations` and `listProjects` with each detail level and grouping flag.
- Confirm automated or manual tests produce distinct payloads per detail level.

## Files Modified
- `docs/delivery/15/15-3.md`

[Back to task list](./tasks.md)
