# 22-1 Extend tool signature for event ID parameters

[Back to task list](./tasks.md)

## Description
Add optional `event_id` and `event_participant_id` parameters to `orchestrate_scheduling` function signature. Update docstring and type hints to document the new rescheduling functionality. Ensure backward compatibility with existing scheduling requests.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-01-21 13:00:00 | Created | N/A | Proposed | Task file created | AI_Agent |
| 2025-01-21 13:00:00 | Status Change | Proposed | InProgress | Starting implementation - adding parameters and updating docstring | AI_Agent |
| 2025-01-21 13:15:00 | Status Change | InProgress | Review | Completed: Added event_id and event_participant_id parameters, updated docstring with rescheduling use case and examples, added validation logic. Backward compatibility maintained. | AI_Agent |
| 2025-01-21 14:00:00 | Status Change | Review | Done | Parameter name updated from event_owner_id to event_participant_id to reflect that any participant can be used. | AI_Agent |

## Requirements
- Add optional `event_id: Optional[str]` parameter to `orchestrate_scheduling` function
- Add optional `event_participant_id: Optional[str]` parameter (required when `event_id` is provided)
- Update function docstring to document rescheduling use case and new parameters
- Update type hints to reflect new parameters
- Ensure backward compatibility: existing calls without these parameters must continue to work
- Document that `event_participant_id` is required when `event_id` is provided
- Update parameter descriptions to explain when to use explicit event ID vs natural language
- Note that `event_participant_id` can be any participant of the event, not necessarily the owner

## Implementation Plan
1. Read current `orchestrate_scheduling` function signature and docstring
2. Add `event_id: Optional[str] = None` parameter after existing parameters
3. Add `event_participant_id: Optional[str] = None` parameter after `event_id`
4. Update docstring to:
   - Document rescheduling use case in overview
   - Add parameter descriptions for `event_id` and `event_participant_id`
   - Provide examples of rescheduling requests (both explicit ID and natural language)
   - Explain when to use explicit event ID vs natural language detection
   - Clarify that any participant ID can be used to fetch the event
5. Add validation logic to check that `event_participant_id` is provided when `event_id` is provided
6. Ensure function signature remains compatible with Letta schema generation

## Verification
- Function signature includes new optional parameters
- Docstring clearly documents rescheduling use case and new parameters
- Type hints are correct
- Backward compatibility: existing function calls without new parameters still work
- Letta schema generation still works correctly
- Validation error message is clear when `event_id` provided without `event_participant_id`

## Files Modified
- `letta/scheduling_orchestrator/orchestrate_scheduling.py` (add parameters, update docstring)
- `docs/delivery/22/22-1.md` (this file)
- `docs/delivery/22/tasks.md` (update task status)

