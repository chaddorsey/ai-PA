# 22-10 Update agent data with original event metadata

[Back to task list](./tasks.md)

## Description
Include original event reference in agent_data.event_registry and proposal metadata. Enable agent to perform follow-up actions on original event. The original event should be included in the event_registry with full EventMetadata so the agent can access it for modifications, cancellations, or other actions.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-01-21 18:00:00 | Created | N/A | Proposed | Task file created | AI_Agent |
| 2025-01-21 18:00:00 | Status Change | Proposed | InProgress | Starting implementation | AI_Agent |
| 2025-01-21 18:15:00 | Status Change | InProgress | Review | Completed: Updated build_event_registry to include original event from rescheduling proposals. Original event is added to registry with full EventMetadata (title, owner, start_utc, end_utc, participants, etc.). Agent can now access original event via event_registry[original_event_id] for follow-up actions. | AI_Agent |

## Requirements
- Include original event in agent_data.event_registry when rescheduling
- Original event should have full EventMetadata (event_id, title, owner, start_utc, end_utc, participants, etc.)
- Original event should be accessible via event_registry[original_event_id]
- Ensure original event metadata is complete and accurate
- Agent should be able to identify which event is being rescheduled from the registry

## Implementation Plan
1. Update build_event_registry to include original event from proposals
2. Extract original event details from proposal.original_event_details
3. Create EventMetadata for original event
4. Add to event_registry with original_event_id as key
5. Ensure original event appears in agent_data.event_registry

## Verification
- Original event is included in agent_data.event_registry for rescheduling operations
- EventMetadata is complete with all required fields
- Agent can access original event via event_registry[original_event_id]
- Original event is clearly identifiable in the registry

## Files Modified
- `letta/scheduling_orchestrator/agent_data_builder.py` (update build_event_registry to include original event)
- `docs/delivery/22/22-10.md` (this file)
- `docs/delivery/22/tasks.md` (update task status)

