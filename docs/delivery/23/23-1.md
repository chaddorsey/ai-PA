# [23-1] Update breaking SDK changes in code

[Back to task list](./tasks.md)

## Description

Update all code that uses Letta SDK to be compatible with SDK v1.0 breaking changes. This includes replacing `modify()` with `update()`, handling list pagination changes (page objects with `.items`), and updating property names to snake_case where applicable.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-01-21 14:00:00 | Created | N/A | Proposed | Task file created | AI_Agent |
| 2025-01-21 14:05:00 | Status Change | Proposed | InProgress | Starting implementation - updating SDK breaking changes | AI_Agent |
| 2025-01-21 14:30:00 | Status Change | InProgress | Review | Completed: Updated modify()→update(), added list pagination handling, verified all files compile | AI_Agent |

## Requirements

### Functional Requirements
- All `client.agents.modify()` calls updated to `client.agents.update()`
- All list method calls handle pagination correctly (access `.items` property)
- All property names use snake_case where applicable
- Code remains backward compatible (works with current server)
- Defensive coding handles both old and new API responses

### Technical Requirements
- Update `letta/attach_scheduling_tool_to_agent.py`:
  - Replace `modify()` with `update()` (line 100)
  - Handle list pagination for `client.tools.list()` (line 64)
- Update `letta/re_register_scheduling_tool.py`:
  - Handle list pagination for `client.tools.list()` (line 47)
- Update `letta/upload_granola_transcripts.py`:
  - Handle list pagination for `client.folders.list()` (line 66)
  - Handle list pagination for `client.embeddingModels.list()` (line 81)
- Update `letta/attach_folder_to_agent.py`:
  - Handle list pagination for `client.folders.list()` (line 51)
- Verify compatibility of other files using Letta client

## Implementation Plan

1. **Create helper function for list pagination**
   - Create defensive helper that handles both old (list) and new (page object) responses
   - Place in a common location or inline where needed

2. **Update `attach_scheduling_tool_to_agent.py`**
   - Replace `client.agents.modify()` with `client.agents.update()` on line 100
   - Update `client.tools.list()` to handle pagination on line 64
   - Test script still works

3. **Update `re_register_scheduling_tool.py`**
   - Update `client.tools.list()` to handle pagination on line 47
   - Test script still works

4. **Update `upload_granola_transcripts.py`**
   - Update `client.folders.list()` to handle pagination on line 66
   - Update `client.embeddingModels.list()` to handle pagination on line 81
   - Test script still works

5. **Update `attach_folder_to_agent.py`**
   - Update `client.folders.list()` to handle pagination on line 51
   - Test script still works

6. **Verify other files**
   - Check `proper_cartesia_agent.py` and `voice_agent/proper_cartesia_agent.py` for compatibility
   - Check `register_scheduling_tool.py` for compatibility
   - Verify no other files need updates

7. **Test all updated scripts**
   - Run each script against current server (should still work)
   - Verify no syntax errors
   - Verify functionality preserved

## Test Plan

### Objective
Verify that all code updates work correctly and maintain backward compatibility with current server.

### Test Scope
- All updated Python scripts
- List pagination handling
- Method rename compatibility

### Key Test Scenarios
1. **Tool attachment script**
   - Run `attach_scheduling_tool_to_agent.py`
   - Verify tool attachment works
   - Verify list operations work

2. **Tool registration scripts**
   - Run `register_scheduling_tool.py`
   - Run `re_register_scheduling_tool.py`
   - Verify tool registration works

3. **Folder operations**
   - Run `attach_folder_to_agent.py`
   - Run `upload_granola_transcripts.py` (if applicable)
   - Verify folder operations work

4. **Backward compatibility**
   - All scripts work with current Letta server (0.12.1)
   - No errors from API changes

### Success Criteria
- All scripts execute without errors
- All functionality preserved
- Code ready for server upgrade

## Verification
- All `modify()` calls replaced with `update()`
- All list methods handle pagination correctly
- All scripts tested and working
- No syntax or import errors
- Code is backward compatible with current server

## Files Modified
- `letta/attach_scheduling_tool_to_agent.py` (update modify→update, list pagination)
- `letta/re_register_scheduling_tool.py` (list pagination)
- `letta/upload_granola_transcripts.py` (list pagination)
- `letta/attach_folder_to_agent.py` (list pagination)
- `docs/delivery/23/23-1.md` (this file)
- `docs/delivery/23/tasks.md` (update task status)

