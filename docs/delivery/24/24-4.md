# [24-4] Implement event filtering and processing

## Description
Implement event filtering according to gold-standard rules: exclude "Email & Tasks" (9:00-11:00 AM) and "Hold" events from the meeting list unless they are overlapped by real meetings. Include all events where Chad is a participant. Treat "Chad out" as busy time. Handle overlapping events correctly and sort events chronologically.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|-----------|-------------|-----------|---------|------|
| 2025-01-21 16:20:00 | Created | N/A | Proposed | Task file created | AI_Agent |
| 2025-01-21 16:25:00 | Status Change | Proposed | InProgress | Started implementation | AI_Agent |
| 2025-01-21 16:35:00 | Status Change | InProgress | Done | Event filtering implemented with gold-standard rules | AI_Agent |

## Requirements

1. **Include All Events**: Include ALL events where Chad is a participant (calendar owner)
2. **Exclude "Email & Tasks"**: Exclude "Email & Tasks" events (9:00-11:00 AM) from meeting list UNLESS overlapped by a real meeting
3. **Exclude "Hold" Events**: Exclude "Hold" events from meeting list UNLESS overlapped by a real meeting
4. **Treat "Chad out" as Busy**: Include "Chad out" events in the schedule (they represent busy time)
5. **Handle Overlaps**: If a real meeting overlaps "Email & Tasks" or "Hold", include the real meeting in the list
6. **Chronological Sorting**: Sort all events by start time
7. **Time Parsing**: Parse ISO datetime strings to datetime objects for comparison

## Implementation Plan

1. Create helper function to parse ISO datetime strings to datetime objects
2. Create helper function to check if two events overlap
3. Create function to identify "Email & Tasks" events (title match and 9:00-11:00 AM time)
4. Create function to identify "Hold" events (title match)
5. Create function to identify "Chad out" events (title match)
6. Implement filtering logic:
   - Separate events into categories (email_tasks, hold, chad_out, real_meetings)
   - Check for overlaps between real meetings and email_tasks/hold events
   - Build final list: all real meetings + overlapped email_tasks/hold + chad_out
7. Sort events chronologically by start time

## Verification

- [ ] "Email & Tasks" events are excluded unless overlapped
- [ ] "Hold" events are excluded unless overlapped
- [ ] "Chad out" events are included
- [ ] Real meetings that overlap email_tasks/hold are included
- [ ] Events are sorted chronologically
- [ ] All events where Chad is a participant are considered
- [ ] Time parsing handles timezone correctly

## Files Modified

- `letta/daily_briefing/generate_daily_briefing.py` (modified)

