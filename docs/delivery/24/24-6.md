# [24-6] Implement Markdown formatting

## Description
Generate Markdown-formatted briefing following the exact gold-standard format specified in the requirements. Format events chronologically with proper bold/italic styling, format available time blocks correctly, and ensure all times are displayed in Eastern timezone.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|-----------|-------------|-----------|---------|------|
| 2025-01-21 16:50:00 | Created | N/A | Proposed | Task file created | AI_Agent |
| 2025-01-21 16:55:00 | Status Change | Proposed | InProgress | Started implementation | AI_Agent |
| 2025-01-21 17:05:00 | Status Change | InProgress | Done | Markdown formatting implemented with gold-standard format | AI_Agent |

## Requirements

1. **Header Format**: `# Today's Schedule (updated [Day] [Month] [DD] at [H:MM AM/PM])`
   - Day abbreviated (Mon., Tue., Wed., etc.)
   - Month abbreviated (Jan., Feb., etc.)
   - Date and time in Eastern timezone

2. **Schedule Section**:
   - Bold section title: `**Today's Schedule**`
   - Bullet format: `• **start–end** — **Bold meeting title** (*attendee names italicized in parentheses*)`
   - Solo blocks italicized: `• **Email & Tasks** (*Chad Dorsey*) — *9:00–11:00 AM*`
   - Events listed in chronological order

3. **Available Time Section**:
   - Header: `### Available Time Remaining — **<total>**`
   - Total formatted as "Xh, Y min" (e.g., "3h, 15 min")
   - Individual blocks: `- start – end (X min left)` for first block, `- start – end (X min)` for subsequent blocks
   - All parenthetical times italicized
   - Blocks listed on separate lines

4. **Time Formatting**: All times in "H:MM AM/PM" format in Eastern timezone

## Implementation Plan

1. Create function to format event as Markdown bullet
2. Create function to format available time blocks
3. Build complete Markdown briefing string
4. Ensure proper formatting for:
   - Event times (start–end)
   - Attendee names (italicized in parentheses)
   - Solo blocks (italicized)
   - Available time blocks (with proper "left" vs no "left")
5. Handle edge cases (no events, no available time, etc.)

## Verification

- [ ] Header format matches gold-standard exactly
- [ ] Events formatted with bold titles and italicized attendees
- [ ] Solo blocks are italicized
- [ ] Available time total formatted correctly
- [ ] First available block uses "left", others don't
- [ ] All times in Eastern timezone
- [ ] Chronological ordering maintained
- [ ] No trailing separator line

## Files Modified

- `letta/daily_briefing/generate_daily_briefing.py` (modified)

