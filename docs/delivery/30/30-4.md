# 30-4 PA Web Service - Frontend UI

[Back to task list](./tasks.md)

## Description

Build the frontend user interface with dark theme styling, chat interface, agent selection dropdown, and dashboard layout. Follow auto-madden design patterns.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2026-01-07 21:00:00 | Created | N/A | Proposed | Task file created | AI_Agent |

## Requirements

1. Create dark theme CSS following auto-madden design language
2. Build chat interface with message list and input box
3. Implement agent selection dropdown
4. Create dashboard layout for memory blocks (placeholder)
5. Handle loading states and error display
6. Make responsive for mobile devices

## Implementation Plan

### 1. HTML Structure

```html
<!DOCTYPE html>
<html>
<head>
    <title>PA Assistant</title>
    <link rel="stylesheet" href="/static/css/styles.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>Personal Assistant</h1>
            <select id="agent-select">
                <option value="">Loading agents...</option>
            </select>
        </header>

        <main>
            <div class="chat-panel">
                <div id="messages"></div>
                <div class="input-area">
                    <textarea id="message-input" placeholder="Type your message..."></textarea>
                    <button id="send-btn">Send</button>
                </div>
            </div>

            <aside class="dashboard-panel">
                <h2>Agent Info</h2>
                <div id="memory-blocks"></div>
            </aside>
        </main>
    </div>
    <script src="/static/js/chat.js"></script>
</body>
</html>
```

### 2. CSS Theme (Dark)

```css
:root {
    --bg-primary: #0f0f1a;
    --bg-secondary: #1a1a2e;
    --text-primary: #f0f0f0;
    --text-secondary: #a0a0a0;
    --accent: #3b82f6;
    --border: #2a2a3e;
}

body {
    background: var(--bg-primary);
    color: var(--text-primary);
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

.chat-panel { /* styles */ }
.message { /* styles */ }
.message.user { /* right-aligned */ }
.message.assistant { /* left-aligned */ }
```

### 3. JavaScript Chat Client

```javascript
// chat.js
class ChatClient {
    constructor() {
        this.messages = [];
        this.selectedAgent = null;
        this.init();
    }

    init() {
        this.loadAgents();
        this.setupEventListeners();
    }

    async loadAgents() { /* fetch from /api/agents */ }

    async sendMessage(text) { /* send to backend, handle SSE */ }

    appendMessage(role, content) { /* add to DOM */ }
}
```

## Verification

- [ ] UI renders correctly in Chrome/Firefox/Safari
- [ ] Dark theme colors applied consistently
- [ ] Agent dropdown populates (after API implemented)
- [ ] Messages display in chat area
- [ ] Input area accepts text and sends on Enter/button click
- [ ] Loading spinner shows during requests
- [ ] Error messages display appropriately
- [ ] Responsive layout works on mobile

## Files Modified

- `pa-web-ui/templates/index.html` - Main UI template
- `pa-web-ui/static/css/styles.css` - Dark theme styles
- `pa-web-ui/static/js/chat.js` - Chat interface logic
- `pa-web-ui/static/js/api.js` - API client wrapper

## Test Plan

1. Load UI in browser
2. Verify dark theme renders correctly
3. Test message input and display (mock data initially)
4. Test agent dropdown selection
5. Test mobile responsiveness (Chrome DevTools)
6. Test keyboard navigation (Enter to send)
7. Test error state display
