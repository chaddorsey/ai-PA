# 20-2 Update Letta tools for new Slack analytics endpoint

[Back to task list](../tasks.md)

## Description
Adjust Letta Slack analytics tooling so the trigger export function targets the new dedicated service, adds configuration flexibility, and ensures tool registration reflects the updated endpoint.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-11-12 09:32:00 | Status Change | N/A | Proposed | Task file created and documented | AI_Agent |

## Requirements
- Update tool source code to reference service hostname `slack-analytics-mcp-server` (or configurable env var) on port `8087`.
- Ensure Letta tool registration scripts (`register_slack_analytics.py`, shell API variants) match the new endpoint.
- Add configuration knobs (environment variables) to override host/port as needed.
- Validate tool messaging for success and error conditions remains informative.

## Implementation Plan
1. Refactor tool code to read Slack analytics service base URL from environment with default fallback.
2. Update registration scripts and templates to embed the refactored source code and redeploy tool definitions.
3. Adjust any documentation or helper scripts referencing the old Calendly endpoint.
4. Test tool invocation against running service to confirm end-to-end flow.

## Test Plan
- Run unit-style invocation of `trigger_slack_analytics_export` in isolation with mocked `urllib.request.urlopen` to confirm URL construction.
- With service running, execute tool through Letta CLI or API and verify successful export response.
- Confirm error handling returns descriptive message when service is unreachable or returns failure payload.

## Verification
- Tool source returns success response when service responds with success JSON.
- Registration scripts complete without errors and list tool pointing to new endpoint.
- Manual Letta invocation demonstrates working export and accessible Slack file.

## Files Modified
- `letta/register_slack_analytics.py`
- `letta/register_slack_tools_api.py`
- `letta/register_slack_tools_http.sh`
- Related documentation as required
