# 1-13 Implement and register searchTasks

[Back to task list](./tasks.md)

## Description

Implement comprehensive full-text search functionality with fuzzy matching to complete the final missing capability for PBI 1. This task adds `searchTasks` to the OmniFocus plugin and registers it as an MCP tool to enable AI-powered task search and discovery.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-01-10 14:45:00 | Created | N/A | Proposed | Task file created for searchTasks implementation | Chad |
| 2025-01-10 14:45:00 | Status Update | Proposed | InProgress | Starting implementation of search functionality | Chad |
| 2025-01-10 15:15:00 | Status Update | InProgress | Done | Search functionality implemented and registered successfully | Chad |

## Requirements

### Functional Requirements
1. **Full-Text Search**: Search task names and notes content for query terms
2. **Fuzzy Matching**: Support typo tolerance for improved usability
3. **Scoped Search**: Enable searching within specific projects or tags
4. **Scoring System**: Rank results by relevance and match quality
5. **Performance Optimization**: Cache search results for repeated queries

### Technical Requirements
1. **Plugin Implementation**: Add `searchTasks` method to OmniFocus plugin
2. **MCP Registration**: Register as `searchTasks` tool in server.ts
3. **Parameter Validation**: Support all required and optional parameters
4. **Error Handling**: Graceful handling of invalid queries and parameters
5. **Cache Integration**: Use existing performance caching system

## Implementation Plan

### Phase 1: Plugin Implementation
- [x] Add `searchTasks` method to omnifocus-mcp.omnijs
- [x] Implement fuzzy matching algorithm with single-character tolerance
- [x] Add scoring system with relevance ranking
- [x] Support scoped searching (all/project/tag)
- [x] Integrate with existing performance caching system
- [x] Add comprehensive parameter validation

### Phase 2: MCP Tool Registration
- [x] Register `searchTasks` tool in server.ts
- [x] Define comprehensive Zod schema for parameters
- [x] Add proper parameter descriptions for AI understanding
- [x] Test end-to-end functionality through bridge

### Phase 3: Testing and Validation
- [x] Build and deploy updated plugin and server
- [x] Verify fuzzy matching works with typos
- [x] Test scoped searching within projects and tags
- [x] Validate performance with caching

## Test Plan

### Success Criteria
- **Basic Search**: Query "programming" returns all tasks with that term
- **Fuzzy Matching**: Query "programing" (typo) still finds "programming" tasks
- **Scoped Search**: Searching within specific project returns only those tasks
- **Scoring**: Results ranked by relevance (name matches before note matches)
- **Performance**: Search completes within 500ms for typical queries

### Test Scenarios
1. **Simple Text Search**: Search for common task keywords
2. **Fuzzy Matching**: Test with deliberate typos and variations
3. **Project Scoping**: Search within specific project contexts
4. **Tag Scoping**: Search within specific tag contexts
5. **Mixed Content**: Search across both task names and notes
6. **Performance Testing**: Verify caching improves repeated queries

## Verification

### Implementation Verification
- [x] `searchTasks` method added to plugin with comprehensive functionality
- [x] Fuzzy matching algorithm implemented with character-level tolerance
- [x] Scoring system ranks results by relevance and match type
- [x] Scoped searching works for projects and tags
- [x] Performance caching integrated with smart cache keys
- [x] MCP tool registered with complete parameter schema
- [x] End-to-end functionality working through bridge

### Functional Verification
- Search functionality enables natural language task discovery
- Fuzzy matching handles common typos and variations
- Scoped searching allows focused queries within contexts
- Results ranked meaningfully by relevance
- Performance acceptable for interactive use

## Files Modified

### Plugin Files
- `extra-files/omnifocus-mcp.omnijs`: Added `searchTasks` method with fuzzy matching

### Server Files  
- `server.ts`: Registered `searchTasks` MCP tool with comprehensive schema

### Bridge Files
- No changes required (generic command routing handles new method)

## Notes

This completes the final missing capability for PBI 1. With searchTasks implemented, all core CRUD operations and enhanced queries are now available:

- **Complete CRUD**: Create, Read, Update, Delete all working
- **Enhanced Queries**: Tag filtering, multi-dimensional queries, and search
- **Advanced Features**: Fuzzy matching, scoped search, performance optimization

The search implementation includes sophisticated features like relevance scoring, fuzzy matching for typos, and integration with the existing performance caching system. This enables natural language task discovery through AI conversations. 