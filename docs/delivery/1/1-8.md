# 1-8 Register completeTask MCP tool

[Back to task list](./tasks.md)

## Description

Register the existing `completeTask` plugin method as an MCP tool in the server (`server.ts`) with Zod schema validation. This task exposes the already-implemented task completion capability through the MCP interface, enabling AI-driven task completion through simple conversational commands.

The plugin method is fully implemented and handles task completion with proper OmniFocus integration. This task only requires server-side tool registration with parameter validation for the task ID.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2024-12-29 15:00:00 | Created | N/A | Proposed | Task file created from revised PBI-1 breakdown | Chad |
| 2025-01-15 17:55:00 | Status Change | Proposed | Done | Task already complete - completeTask MCP tool already registered in server.ts | AI Agent |

## Requirements

### Functional Requirements
1. **Tool Registration**: Register `completeTask` as an MCP tool with simple parameter schema
2. **Parameter Validation**: Zod schema for task ID validation
3. **Documentation**: Clear tool description for AI understanding of completion action
4. **Response Formatting**: Simple success confirmation response
5. **Error Handling**: Convert completion errors to user-friendly messages

### Non-Functional Requirements
1. **API Consistency**: Follow established MCP tool patterns
2. **Validation Robustness**: Handle invalid task IDs gracefully
3. **Performance**: Completion operations complete within 200ms

### Technical Requirements
1. **Leverage Existing Implementation**: ✅ Plugin `completeTask` method already complete
2. **Zod Integration**: Simple schema for task ID validation
3. **Type Safety**: Full TypeScript integration

## Implementation Plan

### Phase 1: Schema Definition
1. **Create simple Zod schema**:
   ```typescript
   const completeTaskSchema = z.object({
     taskId: z.string().describe("UUID of task to mark complete")
   });
   ```

2. **Define tool metadata**: Clear description for completion action

### Phase 2: Tool Registration
1. **Register tool**: Use `server.tool()` with schema and handler
2. **Implement handler**: Call existing plugin method through bridge
3. **Add error handling**: Handle invalid task IDs and completion errors

### Phase 3: Testing and Validation
1. **Parameter validation testing**: Test valid and invalid task IDs
2. **Integration testing**: End-to-end testing through Claude
3. **State verification**: Ensure tasks are marked complete in OmniFocus

## Verification

### Test Plan
1. **Schema Validation Testing**:
   - Test valid task ID formats
   - Test invalid task ID formats and error handling
   - Test missing task ID parameter

2. **Completion Functionality Testing**:
   - Test marking incomplete tasks complete
   - Test attempting to complete already completed tasks
   - Test invalid/non-existent task IDs

3. **Integration Testing**:
   - End-to-end task completion through Claude Desktop
   - Test conversational completion patterns ("mark task X complete")
   - Verify completion status appears immediately in OmniFocus

4. **Error Handling Testing**:
   - Test invalid task ID formats
   - Test non-existent task IDs
   - Test completion errors and error message clarity

### Success Criteria
- Tool successfully registered and appears in MCP tool list
- AI can mark tasks complete through conversation
- Task ID validation works correctly
- Completed tasks update status immediately in OmniFocus
- Success/error responses are clear and appropriate
- Tool integrates seamlessly with existing completion workflows

## Files Modified

- `server.ts`: Add `completeTask` tool registration with Zod schema
- Dependencies: None (plugin method already implemented) 