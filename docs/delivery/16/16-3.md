# 16-3 Emit welcome notification

## Description
Send an informational notification during MCP session initialization to advertise the help tool and documentation resources.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-10-08 14:55:00 | Created | N/A | Proposed | Task created for welcome notification | AI_Agent |
| 2025-10-08 15:05:00 | Status Change | Proposed | InProgress | Began implementing welcome notification for session initialization | AI_Agent |

## Requirements
- After session attachment, emit a single notification with a friendly message referencing the help tool and primary documentation link.
- Ensure the notification uses the existing logging/message channel so clients display it.
- Avoid repeating the notification within the same session.

## Implementation Plan
- Update `streamMessages` or initialization flow in `server-mcp-simplified.ts` to send the new message.
- Reference the exact help tool name and documentation URL/path in the notification text.
- Verify idempotence to prevent duplicates if the session reconnects.

## Verification
- Initialize a session and confirm the notification appears alongside the session ID header.
- Repeat calls within the same session to ensure the message is not re-sent unexpectedly.

## Files Modified
- `omnifocus-mcp-letta/server-mcp-simplified.ts`

[Back to task list](./tasks.md)
