## OmniFocus 3.13.1 (v151.27.0)

[Alert](https://omni-automation.com/omnifocus/OF-API.html#Alert) • [Application](https://omni-automation.com/omnifocus/OF-API.html#Application) • [ApplyResult](https://omni-automation.com/omnifocus/OF-API.html#ApplyResult) • [Array](https://omni-automation.com/omnifocus/OF-API.html#Array) • [FolderArray](https://omni-automation.com/omnifocus/OF-API.html#FolderArray) • [ProjectArray](https://omni-automation.com/omnifocus/OF-API.html#ProjectArray) • [SectionArray](https://omni-automation.com/omnifocus/OF-API.html#SectionArray) • [Library](https://omni-automation.com/omnifocus/OF-API.html#Library) • [TagArray](https://omni-automation.com/omnifocus/OF-API.html#TagArray) • [Tags](https://omni-automation.com/omnifocus/OF-API.html#Tags) • [TaskArray](https://omni-automation.com/omnifocus/OF-API.html#TaskArray) • [Inbox](https://omni-automation.com/omnifocus/OF-API.html#Inbox) • [Audio](https://omni-automation.com/omnifocus/OF-API.html#Audio) • [Audio.Alert](https://omni-automation.com/omnifocus/OF-API.html#Audio.Alert) • [Calendar](https://omni-automation.com/omnifocus/OF-API.html#Calendar) • [Color](https://omni-automation.com/omnifocus/OF-API.html#Color) • [ColorSpace](https://omni-automation.com/omnifocus/OF-API.html#ColorSpace) • [Console](https://omni-automation.com/omnifocus/OF-API.html#Console) • [Credentials](https://omni-automation.com/omnifocus/OF-API.html#Credentials) • [Crypto](https://omni-automation.com/omnifocus/OF-API.html#Crypto) • [Crypto.SHA256](https://omni-automation.com/omnifocus/OF-API.html#Crypto.SHA256) • [Crypto.SHA384](https://omni-automation.com/omnifocus/OF-API.html#Crypto.SHA384) • [Crypto.SHA512](https://omni-automation.com/omnifocus/OF-API.html#Crypto.SHA512) • [Data](https://omni-automation.com/omnifocus/OF-API.html#Data) • [Database](https://omni-automation.com/omnifocus/OF-API.html#Database) • [DatabaseObject](https://omni-automation.com/omnifocus/OF-API.html#DatabaseObject) • [DatedObject](https://omni-automation.com/omnifocus/OF-API.html#DatedObject) • [ActiveObject](https://omni-automation.com/omnifocus/OF-API.html#ActiveObject) • [Folder](https://omni-automation.com/omnifocus/OF-API.html#Folder) • [Tag](https://omni-automation.com/omnifocus/OF-API.html#Tag) • [Task](https://omni-automation.com/omnifocus/OF-API.html#Task) • [Perspective.Custom](https://omni-automation.com/omnifocus/OF-API.html#Perspective.Custom) • [Task.Notification](https://omni-automation.com/omnifocus/OF-API.html#Task.Notification) • [Project](https://omni-automation.com/omnifocus/OF-API.html#Project) • [DateComponents](https://omni-automation.com/omnifocus/OF-API.html#DateComponents) • [DateRange](https://omni-automation.com/omnifocus/OF-API.html#DateRange) • [Decimal](https://omni-automation.com/omnifocus/OF-API.html#Decimal) • [Device](https://omni-automation.com/omnifocus/OF-API.html#Device) • [DeviceType](https://omni-automation.com/omnifocus/OF-API.html#DeviceType) • [Document](https://omni-automation.com/omnifocus/OF-API.html#Document) • [DatabaseDocument](https://omni-automation.com/omnifocus/OF-API.html#DatabaseDocument) • [Email](https://omni-automation.com/omnifocus/OF-API.html#Email) • [Error](https://omni-automation.com/omnifocus/OF-API.html#Error) • [FilePicker](https://omni-automation.com/omnifocus/OF-API.html#FilePicker) • [FileSaver](https://omni-automation.com/omnifocus/OF-API.html#FileSaver) • [FileWrapper](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper) • [FileWrapper.ReadingOptions](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.ReadingOptions) • [FileWrapper.Type](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.Type) • [FileWrapper.WritingOptions](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.WritingOptions) • [Folder.ChildInsertionLocation](https://omni-automation.com/omnifocus/OF-API.html#Folder.ChildInsertionLocation) • [Folder.Status](https://omni-automation.com/omnifocus/OF-API.html#Folder.Status) • [ForecastDay](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay) • [ForecastDay.Kind](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay.Kind) • [ForecastDay.Status](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay.Status) • [Form](https://omni-automation.com/omnifocus/OF-API.html#Form) • [Form.Field](https://omni-automation.com/omnifocus/OF-API.html#Form.Field) • [Form.Field.Checkbox](https://omni-automation.com/omnifocus/OF-API.html#Form.Field.Checkbox) • [Form.Field.Date](https://omni-automation.com/omnifocus/OF-API.html#Form.Field.Date) • [Form.Field.MultipleOptions](https://omni-automation.com/omnifocus/OF-API.html#Form.Field.MultipleOptions) • [Form.Field.Option](https://omni-automation.com/omnifocus/OF-API.html#Form.Field.Option) • [Form.Field.Password](https://omni-automation.com/omnifocus/OF-API.html#Form.Field.Password) • [Form.Field.String](https://omni-automation.com/omnifocus/OF-API.html#Form.Field.String) • [Formatter](https://omni-automation.com/omnifocus/OF-API.html#Formatter) • [Formatter.Date](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Date) • [Formatter.Decimal](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Decimal) • [Formatter.Duration](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Duration) • [Formatter.Date.Style](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Date.Style) • [Function](https://omni-automation.com/omnifocus/OF-API.html#Function) • [Image](https://omni-automation.com/omnifocus/OF-API.html#Image) • [LigatureStyle](https://omni-automation.com/omnifocus/OF-API.html#LigatureStyle) • [Locale](https://omni-automation.com/omnifocus/OF-API.html#Locale) • [MenuItem](https://omni-automation.com/omnifocus/OF-API.html#MenuItem) • [NamedStyle.List](https://omni-automation.com/omnifocus/OF-API.html#NamedStyle.List) • [NamedStylePosition](https://omni-automation.com/omnifocus/OF-API.html#NamedStylePosition) • [Notification](https://omni-automation.com/omnifocus/OF-API.html#Notification) • [ObjectIdentifier](https://omni-automation.com/omnifocus/OF-API.html#ObjectIdentifier) • [Pasteboard](https://omni-automation.com/omnifocus/OF-API.html#Pasteboard) • [Pasteboard.Item](https://omni-automation.com/omnifocus/OF-API.html#Pasteboard.Item) • [Perspective](https://omni-automation.com/omnifocus/OF-API.html#Perspective) • [Perspective.BuiltIn](https://omni-automation.com/omnifocus/OF-API.html#Perspective.BuiltIn) • [PlugIn](https://omni-automation.com/omnifocus/OF-API.html#PlugIn) • [PlugIn.Action](https://omni-automation.com/omnifocus/OF-API.html#PlugIn.Action) • [PlugIn.Handler](https://omni-automation.com/omnifocus/OF-API.html#PlugIn.Handler) • [PlugIn.Library](https://omni-automation.com/omnifocus/OF-API.html#PlugIn.Library) • [Preferences](https://omni-automation.com/omnifocus/OF-API.html#Preferences) • [Project.ReviewInterval](https://omni-automation.com/omnifocus/OF-API.html#Project.ReviewInterval) • [Project.Status](https://omni-automation.com/omnifocus/OF-API.html#Project.Status) • [Promise](https://omni-automation.com/omnifocus/OF-API.html#Promise) • [Selection](https://omni-automation.com/omnifocus/OF-API.html#Selection) • [Settings](https://omni-automation.com/omnifocus/OF-API.html#Settings) • [SharePanel](https://omni-automation.com/omnifocus/OF-API.html#SharePanel) • [Speech](https://omni-automation.com/omnifocus/OF-API.html#Speech) • [Speech.Boundary](https://omni-automation.com/omnifocus/OF-API.html#Speech.Boundary) • [Speech.Synthesizer](https://omni-automation.com/omnifocus/OF-API.html#Speech.Synthesizer) • [Speech.Utterance](https://omni-automation.com/omnifocus/OF-API.html#Speech.Utterance) • [Speech.Voice](https://omni-automation.com/omnifocus/OF-API.html#Speech.Voice) • [Speech.Voice.Gender](https://omni-automation.com/omnifocus/OF-API.html#Speech.Voice.Gender) • [StringEncoding](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) • [Style](https://omni-automation.com/omnifocus/OF-API.html#Style) • [NamedStyle](https://omni-automation.com/omnifocus/OF-API.html#NamedStyle) • [Style.Attribute](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) • [Tag.ChildInsertionLocation](https://omni-automation.com/omnifocus/OF-API.html#Tag.ChildInsertionLocation) • [Tag.Status](https://omni-automation.com/omnifocus/OF-API.html#Tag.Status) • [Task.ChildInsertionLocation](https://omni-automation.com/omnifocus/OF-API.html#Task.ChildInsertionLocation) • [Task.Notification.Kind](https://omni-automation.com/omnifocus/OF-API.html#Task.Notification.Kind) • [Task.RepetitionMethod](https://omni-automation.com/omnifocus/OF-API.html#Task.RepetitionMethod) • [Task.RepetitionRule](https://omni-automation.com/omnifocus/OF-API.html#Task.RepetitionRule) • [Task.Status](https://omni-automation.com/omnifocus/OF-API.html#Task.Status) • [Text](https://omni-automation.com/omnifocus/OF-API.html#Text) • [Text.FindOption](https://omni-automation.com/omnifocus/OF-API.html#Text.FindOption) • [Text.Position](https://omni-automation.com/omnifocus/OF-API.html#Text.Position) • [Text.Range](https://omni-automation.com/omnifocus/OF-API.html#Text.Range) • [TextAlignment](https://omni-automation.com/omnifocus/OF-API.html#TextAlignment) • [TextComponent](https://omni-automation.com/omnifocus/OF-API.html#TextComponent) • [TimeZone](https://omni-automation.com/omnifocus/OF-API.html#TimeZone) • [Timer](https://omni-automation.com/omnifocus/OF-API.html#Timer) • [ToolbarItem](https://omni-automation.com/omnifocus/OF-API.html#ToolbarItem) • [Tree](https://omni-automation.com/omnifocus/OF-API.html#Tree) • [ContentTree](https://omni-automation.com/omnifocus/OF-API.html#ContentTree) • [SidebarTree](https://omni-automation.com/omnifocus/OF-API.html#SidebarTree) • [TreeNode](https://omni-automation.com/omnifocus/OF-API.html#TreeNode) • [TypeIdentifier](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) • [URL](https://omni-automation.com/omnifocus/OF-API.html#URL) • [URL.Access](https://omni-automation.com/omnifocus/OF-API.html#URL.Access) • [URL.Bookmark](https://omni-automation.com/omnifocus/OF-API.html#URL.Bookmark) • [URL.Components](https://omni-automation.com/omnifocus/OF-API.html#URL.Components) • [URL.FetchRequest](https://omni-automation.com/omnifocus/OF-API.html#URL.FetchRequest) • [URL.FetchResponse](https://omni-automation.com/omnifocus/OF-API.html#URL.FetchResponse) • [URL.QueryItem](https://omni-automation.com/omnifocus/OF-API.html#URL.QueryItem) • [UnderlineAffinity](https://omni-automation.com/omnifocus/OF-API.html#UnderlineAffinity) • [UnderlinePattern](https://omni-automation.com/omnifocus/OF-API.html#UnderlinePattern) • [UnderlineStyle](https://omni-automation.com/omnifocus/OF-API.html#UnderlineStyle) • [Version](https://omni-automation.com/omnifocus/OF-API.html#Version) • [Window](https://omni-automation.com/omnifocus/OF-API.html#Window) • [DocumentWindow](https://omni-automation.com/omnifocus/OF-API.html#DocumentWindow) • [XML](https://omni-automation.com/omnifocus/OF-API.html#XML) • [XML.Document](https://omni-automation.com/omnifocus/OF-API.html#XML.Document) • [XML.Document.Configuration](https://omni-automation.com/omnifocus/OF-API.html#XML.Document.Configuration) • [XML.Element](https://omni-automation.com/omnifocus/OF-API.html#XML.Element) • [XML.WhitespaceBehavior](https://omni-automation.com/omnifocus/OF-API.html#XML.WhitespaceBehavior) • [XML.WhitespaceBehavior.Type](https://omni-automation.com/omnifocus/OF-API.html#XML.WhitespaceBehavior.Type)

# `Alert`

An alert interface for displaying information to the user, blocking further interaction until the alert is dismissed.

## Constructors

### `new Alert(title: String, message: String)` → [`Alert`](https://omni-automation.com/omnifocus/OF-API.html#Alert)

Create a new alert panel with the given title and text contents.

## Instance Functions

### `function show(callback: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function)(‍option: Number‍) or null)` → [`Promise`](https://omni-automation.com/omnifocus/OF-API.html#Promise) of `Number`

Displays the alert. If no options have yet been added, a default “OK” option is added. Once the user selects an option, the alert is dismissed. If a callback function was supplied, it is invoked with the zero-based index of the selected option as its argument. A `Promise` is returned as well, which may also be used to collect the result of the `Alert`.

### `function addOption(string: String)`

Adds an option button to the alert.

# `Application`

## Instance Functions

### `function openDocument(from: [Document](https://omni-automation.com/omnifocus/OF-API.html#Document) or null, url: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL), completed: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function)(‍documentOrError: [Document](https://omni-automation.com/omnifocus/OF-API.html#Document) or [Error](https://omni-automation.com/omnifocus/OF-API.html#Error), alreadyOpen: Boolean‍))`

Attempts to open the specified document and return a reference to it asynchronously. If the document is already open, the reference is passed along. Note that due to platform sandboxing restrictions, opening the document may fail if the application doesn’t have currently permission to access the given `URL`. The document, if any, that is associated with the calling script can be passed along to help grant permission to open the new document.
The passed in function will be passed two argument. The first will be either either the `Document` or an `Error`. On success, the second argument is a `Boolean` specifying whether the document was already open.

## Instance Properties

### `var buildVersion` → [`Version`](https://omni-automation.com/omnifocus/OF-API.html#Version) *read-only*

The internal build version number for the app. See also `userVersion`.

### `var commandKeyDown` → `Boolean` *read-only*

Whether the Command key is currently down.

### `var controlKeyDown` → `Boolean` *read-only*

Whether the Control key is currently down.

### `var name` → `String` *read-only*

Application name.

### `var optionKeyDown` → `Boolean` *read-only*

Whether the Option key is currently down.

### `var platformName` → `String` *read-only*

Returns a string describing the current platform, currently `"iOS"` or `"macOS"`.

### `var shiftKeyDown` → `Boolean` *read-only*

Whether the Shift key is currently down.

### `var userVersion` → [`Version`](https://omni-automation.com/omnifocus/OF-API.html#Version) *read-only*

The user-visible version number for the app. See also `buildVersion`.

### `var version` → `String` *read-only*

Deprecated: Recommend using either `userVersion` or `buildVersion`.

For backwards compatibility with existing scripts, this returns the same result as `buildVersion.versionString`. We recommend using either the user-visible `userVersion` or the internal `buildVersion` instead, which are more clear about which version they’re returning and provide their results as `Version` objects which can be semantically compared with other `Version` objects.

# `ApplyResult`

## Class Properties

### `var SkipChildren` → [`ApplyResult`](https://omni-automation.com/omnifocus/OF-API.html#ApplyResult) *read-only*

The descendants of the current item are skipped.

### `var SkipPeers` → [`ApplyResult`](https://omni-automation.com/omnifocus/OF-API.html#ApplyResult) *read-only*

The unvisited peers of the current item are skipped.

### `var Stop` → [`ApplyResult`](https://omni-automation.com/omnifocus/OF-API.html#ApplyResult) *read-only*

The call to `apply` terminates with no further items being visited.

### `var all` → `Array` of [`ApplyResult`](https://omni-automation.com/omnifocus/OF-API.html#ApplyResult) *read-only*

# `Array`

The built-in JavaScript [`Array`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array) constructor.

# `FolderArray` : [`Array`](https://omni-automation.com/omnifocus/OF-API.html#Array)

An `Array` containing `Folder` objects.

## Instance Functions

### `function byName(name: String)` → [`Folder`](https://omni-automation.com/omnifocus/OF-API.html#Folder) or `null`

Returns the first `Folder` contained directly in this array with the given name.

# `ProjectArray` : [`Array`](https://omni-automation.com/omnifocus/OF-API.html#Array)

An `Array` containing `Project` objects.

## Instance Functions

### `function byName(name: String)` → [`Project`](https://omni-automation.com/omnifocus/OF-API.html#Project) or `null`

Returns the first `Project` contained directly in this array with the given name.

# `SectionArray` : [`Array`](https://omni-automation.com/omnifocus/OF-API.html#Array)

An `Array` containing `Project` and `Folder` objects.

## Instance Functions

### `function byName(name: String)` → [`Project`](https://omni-automation.com/omnifocus/OF-API.html#Project) or [`Folder`](https://omni-automation.com/omnifocus/OF-API.html#Folder) or `null`

Returns the first `Project` or `Folder` contained directly in this array with the given name.

# `Library` : [`SectionArray`](https://omni-automation.com/omnifocus/OF-API.html#SectionArray)

An `Array` of folders and projects at the top level of the database. (This can be referenced via the top-level global variable `library`.)

## Instance Functions

### `function apply(function: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function))` → [`ApplyResult`](https://omni-automation.com/omnifocus/OF-API.html#ApplyResult) or `null`

Calls the given function for each `Folder` and `Project` in the `Library` and recursively into any child folders. Note that the tasks in projects are not descended into.

## Instance Properties

### `var beginning` → [`Folder.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Folder.ChildInsertionLocation) *read-only*

Returns a location referring to the beginning of the top-level projects and folders in the database. (Reference this using `library.beginning`.)

### `var ending` → [`Folder.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Folder.ChildInsertionLocation) *read-only*

Returns a location referring to the ending of the top-level projects and folders in the database. (Reference this using `library.ending`.)

# `TagArray` : [`Array`](https://omni-automation.com/omnifocus/OF-API.html#Array)

An `Array` containing `Tag` objects.

## Instance Functions

### `function byName(name: String)` → [`Tag`](https://omni-automation.com/omnifocus/OF-API.html#Tag) or `null`

Returns the first `Tag` contained directly in this array with the given name.

# `Tags` : [`TagArray`](https://omni-automation.com/omnifocus/OF-API.html#TagArray)

An `Array` of tags at the top level of the database.

## Instance Functions

### `function apply(function: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function))` → [`ApplyResult`](https://omni-automation.com/omnifocus/OF-API.html#ApplyResult) or `null`

Calls the given function for each `Tag` in the `Library` and recursively into any child tags.

## Instance Properties

### `var beginning` → [`Tag.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Tag.ChildInsertionLocation) *read-only*

Returns a location referring to the beginning of the top-level tags in the database.

### `var ending` → [`Tag.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Tag.ChildInsertionLocation) *read-only*

Returns a location referring to the ending of the top-level tags in the database.

# `TaskArray` : [`Array`](https://omni-automation.com/omnifocus/OF-API.html#Array)

An `Array` containing `Task` objects.

## Instance Functions

### `function byName(name: String)` → [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task) or `null`

Returns the first `Task` contained directly in this array with the given name.

# `Inbox` : [`TaskArray`](https://omni-automation.com/omnifocus/OF-API.html#TaskArray)

An `Array` of tasks that are in the inbox.

## Instance Functions

### `function apply(function: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function))` → [`ApplyResult`](https://omni-automation.com/omnifocus/OF-API.html#ApplyResult) or `null`

Calls the given function for each `Task` in the `Inbox` and recursively into any child tasks.

## Instance Properties

### `var beginning` → [`Task.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Task.ChildInsertionLocation) *read-only*

A location that can be used when adding, duplicating, or moving tasks.

### `var ending` → [`Task.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Task.ChildInsertionLocation) *read-only*

A location that can be used when adding, duplicating, or moving tasks.

# `Audio`

## Class Functions

### `function playAlert(alert: [Audio.Alert](https://omni-automation.com/omnifocus/OF-API.html#Audio.Alert) or null, completed: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function)(‍‍) or null)`

Play the specified `Audio.Alert`. On macOS, if no alert is specified, the user’s default alert sound will be played. On iOS, there is no default alert sound and nothing will be done without specifying an alert.

# `Audio.Alert`

## Constructors

### `new Audio.Alert(url: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL))` → [`Audio.Alert`](https://omni-automation.com/omnifocus/OF-API.html#Audio.Alert)

# `Calendar`

## Class Properties

### `var buddhist` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

### `var chinese` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

### `var coptic` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

### `var current` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

The user’s preferred calendar

### `var ethiopicAmeteAlem` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

### `var ethiopicAmeteMihret` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

### `var gregorian` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

The Gregorian calendar.

### `var hebrew` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

### `var indian` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

### `var islamic` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

### `var islamicCivil` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

### `var islamicTabular` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

### `var islamicUmmAlQura` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

### `var iso8601` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

### `var japanese` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

### `var persian` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

### `var republicOfChina` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

## Instance Functions

### `function dateByAddingDateComponents(date: Date, components: [DateComponents](https://omni-automation.com/omnifocus/OF-API.html#DateComponents))` → `Date` or `null`

Returns a new `Date` by adding the given `DateComponents`, or null if no date could be calculated.

### `function dateFromDateComponents(components: [DateComponents](https://omni-automation.com/omnifocus/OF-API.html#DateComponents))` → `Date` or `null`

Returns a new `Date` from the given `DateComponents`, or null if no date could be calculated.

### `function dateComponentsFromDate(date: Date)` → [`DateComponents`](https://omni-automation.com/omnifocus/OF-API.html#DateComponents)

Returns a new `DateComponents` for the given `Date`.

### `function dateComponentsBetweenDates(start: Date, end: Date)` → [`DateComponents`](https://omni-automation.com/omnifocus/OF-API.html#DateComponents)

Returns the difference from the start `Date` to the end `Date` as a `DateComponents`.

### `function startOfDay(date: Date)` → `Date`

Returns a `Date` for the first moment of the day containing the given `Date` according to this `Calendar`.

## Instance Properties

### `var identifier` → `String` *read-only*

The ISO identifier for the calendar.

### `var locale` → [`Locale`](https://omni-automation.com/omnifocus/OF-API.html#Locale) or `null` *read-only*

The locale of the calendar.

### `var timeZone` → [`TimeZone`](https://omni-automation.com/omnifocus/OF-API.html#TimeZone) *read-only*

The time zone of the calendar.

# `Color`

## Class Functions

### `function RGB(r: Number, g: Number, b: Number, a: Number or null)` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color)

Makes a new color in the `RGB` colorspace, with the given components. If the alpha component is not given, 1.0 is used.

### `function HSB(h: Number, s: Number, b: Number, a: Number or null)` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color)

Makes a new color in the `HSB` colorspace, with the given components. If the alpha component is not given, 1.0 is used.

### `function White(w: Number, a: Number or null)` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color)

Makes a new color in the `White` colorspace, with the given components. If the alpha component is not given, 1.0 is used.

## Class Properties

### `var black` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) *read-only*

A color in the `White` colorspace with white component of 0.0.

### `var blue` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) *read-only*

A color in the `RGB` colorspace with components (0, 0, 1, 1).

### `var brown` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) *read-only*

A color in the `RGB` colorspace with components (0.6, 0.4, 0.2, 1).

### `var clear` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) *read-only*

A color in the `White` colorspace with white component of 0.0 and alpha of 0.0 (“transparent black”).

### `var cyan` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) *read-only*

A color in the `RGB` colorspace with components (0, 1, 1, 1).

### `var darkGray` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) *read-only*

A color in the `White` colorspace with white component of 0.333.

### `var gray` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) *read-only*

A color in the `White` colorspace with white component of 0.5.

### `var green` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) *read-only*

A color in the `RGB` colorspace with components (0, 1, 0, 1).

### `var lightGray` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) *read-only*

A color in the `White` colorspace with white component of 0.667.

### `var magenta` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) *read-only*

A color in the `RGB` colorspace with components (1, 0, 1, 1).

### `var orange` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) *read-only*

A color in the `RGB` colorspace with components (1, 0.5, 0, 1).

### `var purple` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) *read-only*

A color in the `RGB` colorspace with components (1, 0, 1, 1).

### `var red` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) *read-only*

A color in the `RGB` colorspace with components (1, 0, 0, 1).

### `var white` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) *read-only*

A color in the `White` colorspace with white component of 1.0.

### `var yellow` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) *read-only*

A color in the `RGB` colorspace with components (1, 1, 0, 1).

## Instance Functions

### `function blend(otherColor: [Color](https://omni-automation.com/omnifocus/OF-API.html#Color), fraction: Number)` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) or `null`

Returns a new color that is a linear combination of the receiver and `fraction` of the other color (so, a fraction of 1.0 would just return the `otherColor`. If the colors cannot be blended (for example, if they cannot be converted to the same colorspace), then `null` is returned.

## Instance Properties

### `var alpha` → `Number` *read-only*

Returns the alpha component of the color.

### `var blue` → `Number` *read-only*

Returns the blue component of the color, after converting to a `RGB` colorspace.

### `var brightness` → `Number` *read-only*

Returns the brightness component of the color, after converting to a `HSB` colorspace.

### `var colorSpace` → [`ColorSpace`](https://omni-automation.com/omnifocus/OF-API.html#ColorSpace) *read-only*

Returns the colorspace of the instance.

### `var green` → `Number` *read-only*

Returns the green component of the color, after converting to a `RGB` colorspace.

### `var hue` → `Number` *read-only*

Returns the hue component of the color, after converting to a `HSB` colorspace.

### `var red` → `Number` *read-only*

Returns the red component of the color, after converting to a `RGB` colorspace.

### `var saturation` → `Number` *read-only*

Returns the saturation component of the color, after converting to a `HSB` colorspace.

### `var white` → `Number` *read-only*

Returns the white component of the color, after converting to a `White` colorspace.

# `ColorSpace`

## Class Properties

### `var CMYK` → [`ColorSpace`](https://omni-automation.com/omnifocus/OF-API.html#ColorSpace) *read-only*

A colorspace with cyan, magenta, yellow, black, and alpha components.

### `var HSB` → [`ColorSpace`](https://omni-automation.com/omnifocus/OF-API.html#ColorSpace) *read-only*

A colorspace with hue, saturation, and value (or brightness) components.

### `var Named` → [`ColorSpace`](https://omni-automation.com/omnifocus/OF-API.html#ColorSpace) *read-only*

A space for named colors, like system defined colors, or specific color palette spaces.

### `var Pattern` → [`ColorSpace`](https://omni-automation.com/omnifocus/OF-API.html#ColorSpace) *read-only*

A colorspace that wraps a pattern image.

### `var RGB` → [`ColorSpace`](https://omni-automation.com/omnifocus/OF-API.html#ColorSpace) *read-only*

The sRGB colorspace with red, green, blue, and alpha components.

### `var White` → [`ColorSpace`](https://omni-automation.com/omnifocus/OF-API.html#ColorSpace) *read-only*

A colorspace with white and alpha components.

### `var all` → `Array` of [`ColorSpace`](https://omni-automation.com/omnifocus/OF-API.html#ColorSpace) *read-only*

# `Console`

The `Console` allows scripts to log debugging, warning, or error information where it can be viewed in the system console or in the console output area. A single instance of `Console` is available to scripts as the `console` global variable.

## Instance Functions

### `function log(message: [Object](https://omni-automation.com/omnifocus/OF-API.html#Object), additional: Array of [Object](https://omni-automation.com/omnifocus/OF-API.html#Object) or null)`

Appends a line to the application console formed by concatenating the given `message` (after converting it to a `String`), any additional arguments separated by spaces, and finally a newline.

### `function error(message: [Object](https://omni-automation.com/omnifocus/OF-API.html#Object), additional: Array of [Object](https://omni-automation.com/omnifocus/OF-API.html#Object) or null)`

### `function info(message: [Object](https://omni-automation.com/omnifocus/OF-API.html#Object), additional: Array of [Object](https://omni-automation.com/omnifocus/OF-API.html#Object) or null)`

### `function warn(message: [Object](https://omni-automation.com/omnifocus/OF-API.html#Object), additional: Array of [Object](https://omni-automation.com/omnifocus/OF-API.html#Object) or null)`

Just calls `Console.log`, currently.

### `function clear()`

Clears the console in the user-visible window.

# `Credentials`

The `Credentials` class allows storage of private username and password pairs, `URL.Bookmark` instances, and possibly other sensitive information in the future. Instances are tied to a single plug-in and single application, and may only be created in plug-ins when they are being loaded.

For example, when a `PlugIn.Action` is being created, you could use the following pattern:

```
 (() => {
     let credentials = new Credentials();

     var action = new PlugIn.Action(function(selection) {
         // ... use the captured credentials ...
     });

     return action;
 })();

```

Attempts to create `Credential` instances elsewhere will throw an error. Care should be taken to store instances in anonymous closures as above, and not pass them to or store them on other objects.

Credentials are keyed off a service identifier, which your plug-in can define however it likes.

## Constructors

### `new Credentials()` → [`Credentials`](https://omni-automation.com/omnifocus/OF-API.html#Credentials)

Creates a new `Credentials` instance for the currently loading plug-in. Throws an error if called outside of plug-in loading.

## Instance Functions

### `function read(service: String)` → Object or `null`

Looks up the current credentials for a given service identifier. If credentials have previously been stored, an object will be returned containing `"user"` an `"password"` properties.

### `function write(service: String, username: String, password: String)`

Creates or updates an existing credential, storing the username and password for this service securely in the Keychain.

### `function remove(service: String)`

Deletes any currently stored credentials for the specified service, either a username and password or a URL bookmark.

### `function readBookmark(service: String)` → [`URL.Bookmark`](https://omni-automation.com/omnifocus/OF-API.html#URL.Bookmark) or `null`

Reads the entry for the given service identifier and attempts to return it as a `URL.Bookmark`, or `null` if no such entry exists.

### `function writeBookmark(service: String, bookmark: [URL.Bookmark](https://omni-automation.com/omnifocus/OF-API.html#URL.Bookmark))`

Stores the `URL.Bookmark` persistently for later access.

# `Crypto`

`Crypto` provides access to some of Apple’s [CryptoKit](https://developer.apple.com/documentation/cryptokit)

## Class Functions

### `function randomData(length: Number)` → [`Data`](https://omni-automation.com/omnifocus/OF-API.html#Data)

Copy `length` bytes of cryptographically secure random data.

# `Crypto.SHA256`

The SHA–256 hash function.

## Constructors

### `new Crypto.SHA256()` → [`Crypto.SHA256`](https://omni-automation.com/omnifocus/OF-API.html#Crypto.SHA256)

Create a new SHA–256 digest.

## Instance Functions

### `function update(data: [Data](https://omni-automation.com/omnifocus/OF-API.html#Data))`

Incrementally update the digest with the given data.

### `function finalize()` → [`Data`](https://omni-automation.com/omnifocus/OF-API.html#Data)

Finalize any remaining digest process and return the result of the hash function.

# `Crypto.SHA384`

The SHA–384 hash function.

## Constructors

### `new Crypto.SHA384()` → [`Crypto.SHA384`](https://omni-automation.com/omnifocus/OF-API.html#Crypto.SHA384)

Create a new SHA–384 digest.

## Instance Functions

### `function update(data: [Data](https://omni-automation.com/omnifocus/OF-API.html#Data))`

Incrementally update the digest with the given data.

### `function finalize()` → [`Data`](https://omni-automation.com/omnifocus/OF-API.html#Data)

Finalize any remaining digest process and return the result of the hash function.

# `Crypto.SHA512`

The SHA–512 hash function.

## Constructors

### `new Crypto.SHA512()` → [`Crypto.SHA512`](https://omni-automation.com/omnifocus/OF-API.html#Crypto.SHA512)

Create a new SHA–512 digest.

## Instance Functions

### `function update(data: [Data](https://omni-automation.com/omnifocus/OF-API.html#Data))`

Incrementally update the digest with the given data.

### `function finalize()` → [`Data`](https://omni-automation.com/omnifocus/OF-API.html#Data)

Finalize any remaining digest process and return the result of the hash function.

# `Data`

A generic bag of bytes. Mainly useful to be interpreted / converted to some other type.

## Class Functions

### `function fromString(string: String, encoding: [StringEncoding](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) or null)` → [`Data`](https://omni-automation.com/omnifocus/OF-API.html#Data)

Convert the string to a `Data` using the given encoding, or UTF8 if none is specified.

### `function fromBase64(string: String)` → [`Data`](https://omni-automation.com/omnifocus/OF-API.html#Data)

## Instance Functions

### `function toString(encoding: [StringEncoding](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) or null)` → `String`

Convert to a `String`, assuming that this `Data` using the specified encoding, or UTF8 if none is given.

### `function toBase64()` → `String`

Convert to a Base–64 encoded string.

## Instance Properties

### `var length` → `Number` *read-only*

Number of bytes in this data.

### `var toObject` → [`Object`](https://omni-automation.com/omnifocus/OF-API.html#Object) or `null` *read-only*

# `Database`

## Instance Functions

### `function tagNamed(name: String)` → [`Tag`](https://omni-automation.com/omnifocus/OF-API.html#Tag) or `null`

Returns the first top-level `Tag` with the given name, or `null`.

### `function folderNamed(name: String)` → [`Folder`](https://omni-automation.com/omnifocus/OF-API.html#Folder) or `null`

Returns the first top-level `Folder` with the given name, or `null`.

### `function projectNamed(name: String)` → [`Project`](https://omni-automation.com/omnifocus/OF-API.html#Project) or `null`

Returns the first top-level `Project` with the given name, or `null`.

### `function projectsMatching(search: String)` → `Array` of [`Project`](https://omni-automation.com/omnifocus/OF-API.html#Project)

Returns each existing `Project` that Smart Matches the given `search`. The result will be in the same order and have the same projects as would be found when searching this string in the Quick Open window.

### `function foldersMatching(search: String)` → `Array` of [`Folder`](https://omni-automation.com/omnifocus/OF-API.html#Folder)

Returns each existing `Folder` that Smart Matches the given `search`. The result will be in the same order and have the same folders as would be found when searching this string in the Quick Open window.

### `function tagsMatching(search: String)` → `Array` of [`Tag`](https://omni-automation.com/omnifocus/OF-API.html#Tag)

Returns each existing `Tag` that Smart Matches the `search`. The result will be in the same order and have the same tags as would be found when searching this string in the Quick Open window.

### `function taskNamed(name: String)` → [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task) or `null`

Returns the first top-level `Task` in the inbox with the given name, or `null`.

### `function save()`

Saves any unsaved changes to disk. If sync is enabled and there were unsaved changes, this also triggers a sync request.

### `function moveTasks(tasks: Array of [Task](https://omni-automation.com/omnifocus/OF-API.html#Task), position: [Project](https://omni-automation.com/omnifocus/OF-API.html#Project), [Task](https://omni-automation.com/omnifocus/OF-API.html#Task), or [Task.ChildInsertionLocation](https://omni-automation.com/omnifocus/OF-API.html#Task.ChildInsertionLocation))`

Moves tasks to a different location.

### `function duplicateTasks(tasks: Array of [Task](https://omni-automation.com/omnifocus/OF-API.html#Task), position: [Project](https://omni-automation.com/omnifocus/OF-API.html#Project), [Task](https://omni-automation.com/omnifocus/OF-API.html#Task), or [Task.ChildInsertionLocation](https://omni-automation.com/omnifocus/OF-API.html#Task.ChildInsertionLocation))` → [`TaskArray`](https://omni-automation.com/omnifocus/OF-API.html#TaskArray)

Makes copies of the tasks and returns the new copies. The order of the inputs is not considered and the copies are returned in library order of the originals.

### `function convertTasksToProjects(tasks: Array of [Task](https://omni-automation.com/omnifocus/OF-API.html#Task), position: [Folder](https://omni-automation.com/omnifocus/OF-API.html#Folder) or [Folder.ChildInsertionLocation](https://omni-automation.com/omnifocus/OF-API.html#Folder.ChildInsertionLocation))` → `Array` of [`Project`](https://omni-automation.com/omnifocus/OF-API.html#Project)

Converts tasks to new projects at the specified location.

For example, to convert each top-level inbox item into a new project at the end of your library and capture the resulting projects:

```
 const newProjects = convertTasksToProjects(inbox, library.ending);

```

### `function moveSections(sections: Array of [Project](https://omni-automation.com/omnifocus/OF-API.html#Project) or [Folder](https://omni-automation.com/omnifocus/OF-API.html#Folder), position: [Folder](https://omni-automation.com/omnifocus/OF-API.html#Folder) or [Folder.ChildInsertionLocation](https://omni-automation.com/omnifocus/OF-API.html#Folder.ChildInsertionLocation))`

Moves sections to a different location.

### `function duplicateSections(sections: Array of [Project](https://omni-automation.com/omnifocus/OF-API.html#Project) or [Folder](https://omni-automation.com/omnifocus/OF-API.html#Folder), position: [Folder](https://omni-automation.com/omnifocus/OF-API.html#Folder) or [Folder.ChildInsertionLocation](https://omni-automation.com/omnifocus/OF-API.html#Folder.ChildInsertionLocation))` → [`SectionArray`](https://omni-automation.com/omnifocus/OF-API.html#SectionArray)

Makes copies of the sections and returns the new copies. The order of the inputs is not considered and the copies are returned in library order of the originals.

### `function moveTags(tags: Array of [Tag](https://omni-automation.com/omnifocus/OF-API.html#Tag), position: [Tag](https://omni-automation.com/omnifocus/OF-API.html#Tag) or [Tag.ChildInsertionLocation](https://omni-automation.com/omnifocus/OF-API.html#Tag.ChildInsertionLocation))`

Moves tags to a different location.

### `function duplicateTags(tags: Array of [Tag](https://omni-automation.com/omnifocus/OF-API.html#Tag), position: [Tag](https://omni-automation.com/omnifocus/OF-API.html#Tag) or [Tag.ChildInsertionLocation](https://omni-automation.com/omnifocus/OF-API.html#Tag.ChildInsertionLocation))` → [`TagArray`](https://omni-automation.com/omnifocus/OF-API.html#TagArray)

Makes copies of the tags and returns the new copies. The order of the inputs is not considered and the copies are returned in library order of the originals.

### `function cleanUp()`

Processes inbox items that have the required information to move into their proposed containers, performs any delayed filtering, and deletes empty items.

### `function undo()`

Undoes the last undoable action, or throws an error if there are no undoable actions.

### `function redo()`

Redoes the next redoable action, or throws an error if there are no redoable actions.

### `function deleteObject(object: [DatabaseObject](https://omni-automation.com/omnifocus/OF-API.html#DatabaseObject))`

Removes the object from the Database.

### `function copyTasksToPasteboard(tasks: Array of [Task](https://omni-automation.com/omnifocus/OF-API.html#Task), pasteboard: [Pasteboard](https://omni-automation.com/omnifocus/OF-API.html#Pasteboard))`

Copies the given tasks to the pasteboard in a variety of formats.

### `function canPasteTasks(pasteboard: [Pasteboard](https://omni-automation.com/omnifocus/OF-API.html#Pasteboard))` → `Boolean`

Returns true if the pasteboard contains a type that can be imported as tasks.

### `function pasteTasksFromPasteboard(pasteboard: [Pasteboard](https://omni-automation.com/omnifocus/OF-API.html#Pasteboard))` → `Array` of [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task)

Reads the most relevant pasteboard type and imports them as tasks. The tasks should then be moved to the desired destination.

## Instance Properties

### `var app` → [`Application`](https://omni-automation.com/omnifocus/OF-API.html#Application) *read-only*

Returns the shared `Application`.

### `var canRedo` → `Boolean` *read-only*

Returns true if there are redoable actions.

### `var canUndo` → `Boolean` *read-only*

Returns true if there are undoable actions.

### `var console` → [`Console`](https://omni-automation.com/omnifocus/OF-API.html#Console) *read-only*

Returns the shared `Console`.

### `var document` → [`DatabaseDocument`](https://omni-automation.com/omnifocus/OF-API.html#DatabaseDocument) or `null` *read-only*

### `var flattenedFolders` → [`FolderArray`](https://omni-automation.com/omnifocus/OF-API.html#FolderArray) *read-only*

Returns a flat array of all folders in the database, sorted by their order in the database.

### `var flattenedProjects` → [`ProjectArray`](https://omni-automation.com/omnifocus/OF-API.html#ProjectArray) *read-only*

Returns a flat array of all projects in the database, sorted by their order in the database.

### `var flattenedSections` → [`SectionArray`](https://omni-automation.com/omnifocus/OF-API.html#SectionArray) *read-only*

Returns a flat array of all folders and project in the database, sorted by their order in the database.

### `var flattenedTags` → [`TagArray`](https://omni-automation.com/omnifocus/OF-API.html#TagArray) *read-only*

Returns a flat array of all tags in the database, sorted by their order in the database.

### `var flattenedTasks` → [`TaskArray`](https://omni-automation.com/omnifocus/OF-API.html#TaskArray) *read-only*

Returns a flat array of all tasks in the database, including inbox items, root tasks for projects, task groups and individual tasks. Tasks are sorted by their order in the database, with the inbox preceeding the library.

### `var folders` → [`FolderArray`](https://omni-automation.com/omnifocus/OF-API.html#FolderArray) *read-only*

Returns the top-level folders in the database.

### `var inbox` → [`Inbox`](https://omni-automation.com/omnifocus/OF-API.html#Inbox) *read-only*

Returns a copy of the `Task`s currently in the inbox.

### `var library` → [`Library`](https://omni-automation.com/omnifocus/OF-API.html#Library) *read-only*

Returns the top-level folders and projects in the database.

### `var projects` → [`ProjectArray`](https://omni-automation.com/omnifocus/OF-API.html#ProjectArray) *read-only*

Returns the top-level folders in the database.

### `var settings` → [`Settings`](https://omni-automation.com/omnifocus/OF-API.html#Settings) *read-only*

### `var tags` → [`Tags`](https://omni-automation.com/omnifocus/OF-API.html#Tags) *read-only*

Returns the top-level tags in the database.

# `DatabaseObject`

## Instance Properties

### `var id` → [`ObjectIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#ObjectIdentifier) *read-only*

Returns the identifier for this object.

# `DatedObject` : [`DatabaseObject`](https://omni-automation.com/omnifocus/OF-API.html#DatabaseObject)

## Instance Properties

### `var added` → `Date` or `null`

Returns the date the object was first saved. If the object is newly inserted, this will be `null`. For newly inserted objects, the `added` property may be set (but once an object is saved for the first time, the property is read-only).

### `var modified` → `Date` or `null`

Returns the date the object was most recently modified. If the object is newly inserted, this will be `null`. For newly inserted objects, the `modified` property may be set (but once an object is saved for the first time, the property is read-only).

# `ActiveObject` : [`DatedObject`](https://omni-automation.com/omnifocus/OF-API.html#DatedObject)

## Instance Properties

### `var active` → `Boolean`

If `true`, then this object is considered active, otherwise the object is considered dropped.

### `var effectiveActive` → `Boolean` *read-only*

Returns `true` if this object and all its containers are active.

# `Folder` : [`ActiveObject`](https://omni-automation.com/omnifocus/OF-API.html#ActiveObject)

## Class Functions

### `function byIdentifier(identifier: String)` → [`Folder`](https://omni-automation.com/omnifocus/OF-API.html#Folder) or `null`

Returns the `Folder` with the specified identifier, or `null` if no such folder exists.

## Constructors

### `new Folder(name: String, position: [Folder](https://omni-automation.com/omnifocus/OF-API.html#Folder) or [Folder.ChildInsertionLocation](https://omni-automation.com/omnifocus/OF-API.html#Folder.ChildInsertionLocation) or null)` → [`Folder`](https://omni-automation.com/omnifocus/OF-API.html#Folder)

## Instance Functions

### `function folderNamed(name: String)` → [`Folder`](https://omni-automation.com/omnifocus/OF-API.html#Folder) or `null`

Returns the first child `Folder` with the given name that is contained directly in this folder, or `null`.

### `function projectNamed(name: String)` → [`Project`](https://omni-automation.com/omnifocus/OF-API.html#Project) or `null`

Returns the first child `Project` of this folder with the given name, or `null`.

### `function sectionNamed(name: String)` → [`Project`](https://omni-automation.com/omnifocus/OF-API.html#Project) or [`Folder`](https://omni-automation.com/omnifocus/OF-API.html#Folder) or `null`

Returns the first child `Folder` or `Project` in this folder with the given name, or `null`.

### `function childNamed(name: String)` → [`Project`](https://omni-automation.com/omnifocus/OF-API.html#Project) or [`Folder`](https://omni-automation.com/omnifocus/OF-API.html#Folder) or `null`

An alias for `sectionNamed`.

### `function apply(function: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function))` → [`ApplyResult`](https://omni-automation.com/omnifocus/OF-API.html#ApplyResult) or `null`

Calls the given function for this `Folder` and recursively into any child folders and projects. The tasks within any projects are not descended into.

## Instance Properties

### `var after` → [`Folder.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Folder.ChildInsertionLocation) *read-only*

Returns a location referring to the position just after this folder within its containing `Folder` or `Database`.

### `var before` → [`Folder.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Folder.ChildInsertionLocation) *read-only*

Returns a location referring to the position just before this folder within its containing `Folder` or `Database`.

### `var beginning` → [`Folder.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Folder.ChildInsertionLocation) *read-only*

Returns a location referring to the the beginning of the contained projects and folders in this folder.

### `var children` → [`SectionArray`](https://omni-automation.com/omnifocus/OF-API.html#SectionArray) *read-only*

An alias for `sections`.

### `var ending` → [`Folder.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Folder.ChildInsertionLocation) *read-only*

Returns a location referring to the the ending of the contained projects and folders in this folder.

### `var flattenedChildren` → [`SectionArray`](https://omni-automation.com/omnifocus/OF-API.html#SectionArray) *read-only*

An alias for `flattenedSections`.

### `var flattenedFolders` → [`FolderArray`](https://omni-automation.com/omnifocus/OF-API.html#FolderArray) *read-only*

Returns a flat array of all folders in this folder, sorted by their order in the database.

### `var flattenedProjects` → [`ProjectArray`](https://omni-automation.com/omnifocus/OF-API.html#ProjectArray) *read-only*

Returns a flat array of all projects in this folder, sorted by their order in the database.

### `var flattenedSections` → [`SectionArray`](https://omni-automation.com/omnifocus/OF-API.html#SectionArray) *read-only*

Returns a flat array of all folders and project in this folder, sorted by their order in the database.

### `var folders` → [`FolderArray`](https://omni-automation.com/omnifocus/OF-API.html#FolderArray) *read-only*

Returns the folders contained directly as children of this folder.

### `var name` → `String`

The name of the folder.

### `var parent` → [`Folder`](https://omni-automation.com/omnifocus/OF-API.html#Folder) or `null` *read-only*

The parent `Folder` which contains this folder.

### `var projects` → [`ProjectArray`](https://omni-automation.com/omnifocus/OF-API.html#ProjectArray) *read-only*

Returns the projects contained directly as children of this folder.

### `var sections` → [`SectionArray`](https://omni-automation.com/omnifocus/OF-API.html#SectionArray) *read-only*

Returns a sorted list of the folders and projects contained directly within this folder.

### `var status` → [`Folder.Status`](https://omni-automation.com/omnifocus/OF-API.html#Folder.Status)

The folder’s status.

# `Tag` : [`ActiveObject`](https://omni-automation.com/omnifocus/OF-API.html#ActiveObject)

## Class Functions

### `function byIdentifier(identifier: String)` → [`Tag`](https://omni-automation.com/omnifocus/OF-API.html#Tag) or `null`

Returns the `Tag` with the specified identifier, or `null` if no such tag exists.

## Class Properties

### `var forecastTag` → [`Tag`](https://omni-automation.com/omnifocus/OF-API.html#Tag) or `null` *read-only*

The Forecast Tag, if it is set.

## Constructors

### `new Tag(name: String, position: [Tag](https://omni-automation.com/omnifocus/OF-API.html#Tag) or [Tag.ChildInsertionLocation](https://omni-automation.com/omnifocus/OF-API.html#Tag.ChildInsertionLocation) or null)` → [`Tag`](https://omni-automation.com/omnifocus/OF-API.html#Tag)

## Instance Functions

### `function tagNamed(name: String)` → [`Tag`](https://omni-automation.com/omnifocus/OF-API.html#Tag) or `null`

Returns the first child `Tag` with the given name that is contained directly in this tag, or `null`.

### `function childNamed(name: String)` → [`Tag`](https://omni-automation.com/omnifocus/OF-API.html#Tag) or `null`

An alias for `tagNamed`.

### `function apply(function: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function))` → [`ApplyResult`](https://omni-automation.com/omnifocus/OF-API.html#ApplyResult) or `null`

Calls the given function for this `Tag` and recursively into any child tags.

## Instance Properties

### `var after` → [`Tag.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Tag.ChildInsertionLocation) *read-only*

Returns a location referring to the position just after this tag.

### `var allowsNextAction` → `Boolean`

If set and the tag is active, tasks with this tag applied cannot be the next task of a project.

### `var availableTasks` → [`TaskArray`](https://omni-automation.com/omnifocus/OF-API.html#TaskArray) *read-only*

Returns a sorted list of the tasks associated with this tag that are currently available. Recent changes may not be reflected until a `cleanUp` is performed on the database.

### `var before` → [`Tag.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Tag.ChildInsertionLocation) *read-only*

Returns a location referring to the position just before this tag.

### `var beginning` → [`Tag.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Tag.ChildInsertionLocation) *read-only*

Returns a location referring to the beginning of the contained tags in this tag.

### `var children` → [`TagArray`](https://omni-automation.com/omnifocus/OF-API.html#TagArray) *read-only*

An alias for `tags`.

### `var ending` → [`Tag.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Tag.ChildInsertionLocation) *read-only*

Returns a location referring to the ending of the contained tags in this tag.

### `var flattenedChildren` → [`TagArray`](https://omni-automation.com/omnifocus/OF-API.html#TagArray) *read-only*

An alias for `flattenedTags`.

### `var flattenedTags` → [`TagArray`](https://omni-automation.com/omnifocus/OF-API.html#TagArray) *read-only*

Returns a flat array of all tags contained within this tag. Tags are sorted by their order in the database.

### `var name` → `String`

### `var parent` → [`Tag`](https://omni-automation.com/omnifocus/OF-API.html#Tag) or `null` *read-only*

The parent `Tag` which contains this tag.

### `var projects` → [`ProjectArray`](https://omni-automation.com/omnifocus/OF-API.html#ProjectArray) *read-only*

A convenience property that returns only `Project`s for the root tasks associated with this `Tag`.

### `var remainingTasks` → [`TaskArray`](https://omni-automation.com/omnifocus/OF-API.html#TaskArray) *read-only*

Returns a sorted list of the tasks associated with this tag that remaing to be completed. Recent changes may not be reflected until a `cleanUp` is performed on the database.

### `var status` → [`Tag.Status`](https://omni-automation.com/omnifocus/OF-API.html#Tag.Status)

The current status of the tag as a whole, which is derived from `allowsNextAction` and `active`.

### `var tags` → [`TagArray`](https://omni-automation.com/omnifocus/OF-API.html#TagArray) *read-only*

Returns a sorted list of the tags contained directly within this tag, sorted by their library order.

### `var tasks` → [`TaskArray`](https://omni-automation.com/omnifocus/OF-API.html#TaskArray) *read-only*

Returns a sorted list of the tasks associated with this tag.

# `Task` : [`ActiveObject`](https://omni-automation.com/omnifocus/OF-API.html#ActiveObject)

## Class Functions

### `function byParsingTransportText(text: String, singleTask: Boolean or null)` → `Array` of [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task)

Returns an array of tasks by parsing the transport text formatted input. Optionally, only the first task can be requested (but will still be returned in an array).

### `function byIdentifier(identifier: String)` → [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task) or `null`

Returns the `Task` with the specified identifier, or `null` if no such task exists.

## Constructors

### `new Task(name: String, position: [Project](https://omni-automation.com/omnifocus/OF-API.html#Project), [Task](https://omni-automation.com/omnifocus/OF-API.html#Task), or [Task.ChildInsertionLocation](https://omni-automation.com/omnifocus/OF-API.html#Task.ChildInsertionLocation) or null)` → [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task)

Returns a new `Task` at the given location. If a project or task is given as a location, the new task is placed at the end of the children of that parent. If no location is specified, then the task is created at the end of the inbox.

## Instance Functions

### `function taskNamed(name: String)` → [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task) or `null`

Returns the first child `Task` with the given name that is contained directly in this task, or `null`.

### `function childNamed(name: String)` → [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task) or `null`

An alias for `taskNamed`.

### `function appendStringToNote(stringToAppend: String)`

Appends `stringToAppend` to the end of the `Task`’s `note`.

### `function addLinkedFileURL(url: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL))`

Links a file URL to this task. In order to be considered a file URL, `url` must have the `file` scheme. That is, `url` must be of the form `file://path-to-file`. The file at `url` will not be added to database, rather a bookmark leading to it will be added. In order to add files to a task, use the `addAttachment` function. Linking files is especially useful for large files, as including large files in the database can degrade app performance.

### `function removeLinkedFileWithURL(url: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL))`

Removes the first link to a file with the given `url`. This removes the bookmark that leads to the file at `url`. If the file itself is present in the database, use the `removeAttachmentAtIndex` function instead.

### `function addAttachment(attachment: [FileWrapper](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper))`

Adds `attachment` as an attachment to the task. If the attachment is large, consider using the `addLinkedFileURL` function instead. Including large attachments in the database can degrade app performance.

### `function removeAttachmentAtIndex(index: Number)`

Removes the attachment at `index` from this task’s `attachments` array.

### `function addTag(tag: [Tag](https://omni-automation.com/omnifocus/OF-API.html#Tag))`

Adds a `Tag` to this task, appending it to the end of the list of associated tags. If the tag is already present, no change is made. The `Database` function `moveTags` can be used to control the ordering of tags within the task.

### `function addTags(tags: Array of [Tag](https://omni-automation.com/omnifocus/OF-API.html#Tag))`

Adds multiple `Tag`s to this task, appending them to the end of the list of associated tags. For any tags already associated with the `Task`, no change is made. The `Database` function `moveTags` can be used to control the ordering of tags within the task.

### `function removeTag(tag: [Tag](https://omni-automation.com/omnifocus/OF-API.html#Tag))`

Removes a `Tag` from this task. If the tag is not associated with this task, no change is made.

### `function removeTags(tags: Array of [Tag](https://omni-automation.com/omnifocus/OF-API.html#Tag))`

Removes multiple `Tag`s from this task. If a tag is not associated with this task, no change is made.

### `function clearTags()`

Removes multiple `Tag`s from this task. If a tag is not associated with this task, no change is made.

### `function markComplete(date: Date or null)` → [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task)

If the task is not completed, marks it as complete with the given completion date (or the current date if no date is specified). For repeating tasks, this makes a clone of the task and marks that clone as completed. In either case, the task that has been marked completed is returned.

### `function markIncomplete()`

If the task is completed, marks it as incomplete.

### `function drop(allOccurrences: Boolean)`

Drops this `Task`. If true is passed in for `allOccurrences` then this task will not repeat, even if it has a `repititionRule` set on it. If false is passed in for `allOccurrences`, this task will repeat as normal.

### `function apply(function: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function))` → [`ApplyResult`](https://omni-automation.com/omnifocus/OF-API.html#ApplyResult) or `null`

Calls the given function for this `Task` and recursively into any child task.

### `function addNotification(info: Number or Date)` → [`Task.Notification`](https://omni-automation.com/omnifocus/OF-API.html#Task.Notification)

Add a notification from the specification in `info`. Supplying a `Date` creates an absolute notification that will fire at that date. Supplying a `Double` will create a due-relative notification. Specifying a due relative notification when this task’s effectiveDueDate is not set will result in an error.

### `function removeNotification(notification: [Task.Notification](https://omni-automation.com/omnifocus/OF-API.html#Task.Notification))`

Remove an active notification for this task. Supplying a notification that is not in this task’s `notifications` array, or a notification that has `task` to something other than this task results in an error.

## Instance Properties

### `var after` → [`Task.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Task.ChildInsertionLocation) *read-only*

The location after this task within its parent task’s children. If this task has no parent task, then this is the position adjacent to it in its container.

### `var assignedContainer` → [`Project`](https://omni-automation.com/omnifocus/OF-API.html#Project), [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task), or [`Inbox`](https://omni-automation.com/omnifocus/OF-API.html#Inbox) or `null`

For tasks in the inbox, the tentatively assigned project or parent task, which will be applied on cleanup.

### `var attachments` → `Array` of [`FileWrapper`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper)

An array of `FileWrapper` objects representing the attachments associated with the task.

### `var before` → [`Task.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Task.ChildInsertionLocation) *read-only*

The location before this task within its parent task’s children. If this task has no parent task, then this is the position adjacent to it in its container.

### `var beginning` → [`Task.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Task.ChildInsertionLocation) *read-only*

The location at the beginning of this task’s children.

### `var children` → [`TaskArray`](https://omni-automation.com/omnifocus/OF-API.html#TaskArray) *read-only*

An alias for `tasks`.

### `var completed` → `Boolean` *read-only*

True if the task has been marked completed. Note that a task may be effectively considered completed if a containing task is marked completed.

### `var completedByChildren` → `Boolean`

If set, the Task will be automatically marked completed when its last child Task is marked completed.

### `var completionDate` → `Date` or `null` *read-only*

If set, the Task is completed.

### `var containingProject` → [`Project`](https://omni-automation.com/omnifocus/OF-API.html#Project) or `null` *read-only*

The `Project` that this `Task` is contained in, either as the root of the project or indirectly from a parent task. If this task is in the inbox, then this will be `null`.

### `var deferDate` → `Date` or `null`

If set, the Task is not actionable until this date.

### `var dropDate` → `Date` or `null` *read-only*

If set, the Task is dropped.

### `var dueDate` → `Date` or `null`

If set, the Task should be completed by this date.

### `var effectiveCompletedDate` → `Date` or `null` *read-only*

Returns the computed effective completion date for the `Task`, based on its local `completionDate` and those of its containers.

### `var effectiveDeferDate` → `Date` or `null` *read-only*

Returns the computed effective defer date for the `Task`, based on its local `deferDate` and those of its containers.

### `var effectiveDropDate` → `Date` or `null` *read-only*

Returns the computed effective drop date for the `Task`, based on its local `dropDate` and those of its containers.

### `var effectiveDueDate` → `Date` or `null` *read-only*

Returns the computed effective due date for the `Task`, based on its local `dateDue` and those of its containers.

### `var effectiveFlagged` → `Boolean` *read-only*

Returns the computed effective flagged status for the `Task`, based on its local `flagged` and those of its containers.

### `var ending` → [`Task.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Task.ChildInsertionLocation) *read-only*

The location at the end of this task’s children.

### `var estimatedMinutes` → `Number` or `null`

The estimated number of minutes this task will take to finish, or `null` if no estimate has been made.

### `var flagged` → `Boolean`

The flagged status of the task.

### `var flattenedChildren` → [`TaskArray`](https://omni-automation.com/omnifocus/OF-API.html#TaskArray) *read-only*

An alias for `flattenedTasks`.

### `var flattenedTasks` → [`TaskArray`](https://omni-automation.com/omnifocus/OF-API.html#TaskArray) *read-only*

Returns a flat array of all tasks contained within this task. Tasks are sorted by their order in the database.

### `var hasChildren` → `Boolean` *read-only*

Returns `true` if this task has children, more efficiently than checking if `children` is empty.

### `var inInbox` → `Boolean` *read-only*

True if the task is a direct child of the inbox, but not if the task is contained by another task that is in the inbox.

### `var linkedFileURLs` → `Array` of [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) *read-only*

The list of file URLs linked to this task. The files at these URLs are not present in the database, rather the database holds bookmarks leading to these files. These links can be read on iOS, but not written to.

### `var name` → `String`

The title of the task.

### `var note` → `String`

The tasks’s note.

### `var notifications` → `Array` of [`Task.Notification`](https://omni-automation.com/omnifocus/OF-API.html#Task.Notification) *read-only*

An array of the notifications that are active for this task.

### `var parent` → [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task) or `null` *read-only*

The parent `Task` which contains this task.

### `var project` → [`Project`](https://omni-automation.com/omnifocus/OF-API.html#Project) or `null` *read-only*

The `Project` that this `Task` is the root task of, or `null` if this task is in the inbox or contained by another task.

### `var repetitionRule` → [`Task.RepetitionRule`](https://omni-automation.com/omnifocus/OF-API.html#Task.RepetitionRule) or `null`

The object holding the repetition properties for this task, or null if it is not repeating.

### `var sequential` → `Boolean`

If `true`, then children of this task form a dependency chain. For example, the first task blocks the second one until the first is completed.

### `var shouldUseFloatingTimeZone` → `Boolean`

When set, the `dueDate` and `deferDate` properties will use floating time zones. (Note: if a `Task` has no due or defer dates assigned, this property will revert to the database’s default setting.)

### `var tags` → [`TagArray`](https://omni-automation.com/omnifocus/OF-API.html#TagArray) *read-only*

Returns the `Tag`s associated with this `Task`.

### `var taskStatus` → [`Task.Status`](https://omni-automation.com/omnifocus/OF-API.html#Task.Status) *read-only*

Returns the current status of the task.

### `var tasks` → [`TaskArray`](https://omni-automation.com/omnifocus/OF-API.html#TaskArray) *read-only*

Returns all the tasks contained directly in this task, sorted by their library order.

# `Perspective.Custom` : [`DatedObject`](https://omni-automation.com/omnifocus/OF-API.html#DatedObject)

## Class Functions

### `function byName(name: String)` → [`Perspective.Custom`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.Custom) or `null`

A custom perspective with the given name, if one exists. If there are multiple perspectives with the same name, it is not defined which will be returned.

### `function byIdentifier(identifier: String)` → [`Perspective.Custom`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.Custom) or `null`

Returns the custom perspective with the specified identifier, or `null` if no such perspective exists.

## Class Properties

### `var all` → `Array` of [`Perspective.Custom`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.Custom) *read-only*

Returns all the custom perspectives.

## Instance Functions

### `function fileWrapper()` → [`FileWrapper`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper)

Returns an archived file wrapper for the custom perspective. The file wrapper’s preferred filename will be the name of the perspective with an appropriate file extension applied. Its contents will include a plist representing the perspective’s settings, along with any image attachments needed to display its icon.

### `function writeFileRepresentationIntoDirectory(parentURL: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL))` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL)

Writes the perspective’s `fileWrapper()` within a given parent directory URL, returning the URL of the saved FileWrapper. This function requires sandboxed access to the parent folder; it may be easier to work with the perspective’s `fileWrapper()`, which can be accessed directly or saved to disk using `FileSaver`.

## Instance Properties

### `var identifier` → `String` *read-only*

The unique identifier of the custom perspective.

### `var name` → `String` *read-only*

The name of the custom perspective.

# `Task.Notification` : [`DatedObject`](https://omni-automation.com/omnifocus/OF-API.html#DatedObject)

## Instance Properties

### `var absoluteFireDate` → `Date`

The absolute date at which this notification will fire, if its `kind` is `absolute`. Getting or setting this property throws an error if this notification’s `kind` is not `absolute`.

### `var initialFireDate` → `Date` *read-only*

The time at which this notification will fire. For due or defer-relative notifications, this date will change with its `task` object’s due and defer dates.

### `var isSnoozed` → `Boolean` *read-only*

Whether or not this notification has been snoozed.

### `var kind` → [`Task.Notification.Kind`](https://omni-automation.com/omnifocus/OF-API.html#Task.Notification.Kind) *read-only*

This notification’s kind. A `kind` of `unknown` indicates that the notification is in an invalid state.

### `var nextFireDate` → `Date` or `null` *read-only*

The next time at which this notification will fire. This will only have a value if the `initialFireDate` is not yet reached, or this notification’s `repeatInterval` is greater than 0.

### `var relativeFireOffset` → `Number`

The relative offset in minutes at which this notification will fire from the specified date on its `task`. Getting or setting this property throws an error if this notification’s `kind` is not either `dueRelative` or `deferRelative`.

### `var repeatInterval` → `Number`

How often in seconds this notification will fire once its `initialFireDate` is reached. Setting this to 0 or any negative number will cease repetition of this notification.

### `var task` → [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task) or `null` *read-only*

The `Task` object this notification will fire for.

### `var usesFloatingTimeZone` → `Boolean` *read-only*

Whether or not the notification’s fire date uses floating time zones. This can only return true if the notification’s `kind` is absolute`. This can be changed by setting`shouldUseFloatingTimeZone`on this notification's`task`

# `Project` : [`DatabaseObject`](https://omni-automation.com/omnifocus/OF-API.html#DatabaseObject)

## Class Functions

### `function byIdentifier(identifier: String)` → [`Project`](https://omni-automation.com/omnifocus/OF-API.html#Project) or `null`

Returns the `Project` with the specified identifier, or `null` if no such project exists.

## Constructors

### `new Project(name: String, position: [Folder](https://omni-automation.com/omnifocus/OF-API.html#Folder) or [Folder.ChildInsertionLocation](https://omni-automation.com/omnifocus/OF-API.html#Folder.ChildInsertionLocation) or null)` → [`Project`](https://omni-automation.com/omnifocus/OF-API.html#Project)

## Instance Functions

### `function taskNamed(name: String)` → [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task) or `null`

Returns the first top-level `Task` in this project the given name, or `null`.

### `function appendStringToNote(stringToAppend: String)`

Appends `stringToAppend` to the end of the `Project`’s root `Task`’s `note`.

### `function addAttachment(attachment: [FileWrapper](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper))`

Adds `attachment` as an attachment to the `Project`’s root `Task`. If the attachment is large, consider using the `addLinkedFileURL` function instead. Including large attachments in the database can degrade app performance.

### `function removeAttachmentAtIndex(index: Number)`

Removes the attachment at `index` from this `Project`’s root `Task`’s `attachments` array.

### `function markComplete(date: Date or null)` → [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task)

If the project is not completed, marks it as complete with the given completion date (or the current date if no date is specified). For repeating project, this makes a clone of the project and marks that clone as completed. In either case, the project that has been marked completed is returned.

### `function markIncomplete()`

If the project is completed, marks it as incomplete.

### `function addNotification(info: Number or Date)` → [`Task.Notification`](https://omni-automation.com/omnifocus/OF-API.html#Task.Notification)

Add a notification to the project from the specification in `info`. Supplying a `Date` creates an absolute notification that will fire at that date. Supplying a `Double` will create a due-relative notification. Specifying a due-relative notification when this project’s `task`’s effectiveDueDate is not set will result in an error.

### `function removeNotification(notification: [Task.Notification](https://omni-automation.com/omnifocus/OF-API.html#Task.Notification))`

Remove an active notification for this project. Supplying a notification that is not in this task’s `notifications` array, or a notification that has `task` to something other than this project’s `task` results in an error.

### `function addTag(tag: [Tag](https://omni-automation.com/omnifocus/OF-API.html#Tag))`

Adds a `Tag` to this project, appending it to the end of the list of associated tags. If the tag is already present, no change is made. The `Database` function `moveTags` can be used to control the ordering of tags within the task.

### `function addTags(tags: Array of [Tag](https://omni-automation.com/omnifocus/OF-API.html#Tag))`

Adds multiple `Tag`s to this project, appending them to the end of the list of associated tags. For any tags already associated with the `Task`, no change is made. The `Database` function `moveTags` can be used to control the ordering of tags within the task.

### `function removeTag(tag: [Tag](https://omni-automation.com/omnifocus/OF-API.html#Tag))`

Removes a `Tag` from this project. If the tag is not associated with this project, no change is made.

### `function removeTags(tags: Array of [Tag](https://omni-automation.com/omnifocus/OF-API.html#Tag))`

Removes multiple `Tag`s from this project. If a tag is not associated with this project, no change is made.

### `function clearTags()`

Removes multiple `Tag`s from this project. If a tag is not associated with this project, no change is made.

### `function addLinkedFileURL(url: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL))`

Links a file URL to this task. In order to be considered a file URL, `url` must have the `file` scheme. That is, `url` must be of the form `file://path-to-file`. The file at `url` will not be added to database, rather a bookmark leading to it will be added. In order to add files to a task, use the `addAttachment` function. Linking files is especially useful for large files, as including large files in the database can degrade app performance. This function throws an error if invoked on iOS.

### `function removeLinkedFileWithURL(url: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL))`

Removes the first link to a file with the given `url`. This removes the bookmark that leads to the file at `url`. If the file itself is present in the database, use the `removeAttachmentAtIndex` function instead.

## Instance Properties

### `var after` → [`Folder.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Folder.ChildInsertionLocation) *read-only*

Returns a location referring to the position just after this project within its containing `Folder` or `Database`.

### `var attachments` → `Array` of [`FileWrapper`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper)

An array of `FileWrapper` objects representing the attachments associated with the `Project`’s root `Task`.

### `var before` → [`Folder.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Folder.ChildInsertionLocation) *read-only*

Returns a location referring to the position just before this project within its containing `Folder` or `Database`.

### `var beginning` → [`Task.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Task.ChildInsertionLocation) *read-only*

Returns a location referring to the position after the last `Task` directly contained in the root `task` of this project.

### `var children` → [`TaskArray`](https://omni-automation.com/omnifocus/OF-API.html#TaskArray) *read-only*

An alias for `tasks`.

### `var completed` → `Boolean` *read-only*

True if the project has been marked completed. Note that a project may be effectively considered completed if a containing project is marked completed.

### `var completedByChildren` → `Boolean`

If set, the project will be automatically marked completed when its last child Task is marked completed.

### `var completionDate` → `Date` or `null`

If set, the project is completed.

### `var containsSingletonActions` → `Boolean`

If set to `true`, the project contains single tasks, and has no next task.

### `var defaultSingletonActionHolder` → `Boolean`

If set to `true`, this is the `Project` that inbox tasks that have enough information specified (as selected by the user’s preferences) will be filed into upon a clean-up.

### `var deferDate` → `Date` or `null`

If set, the project is not actionable until this date.

### `var dropDate` → `Date` or `null`

If set, the project is dropped.

### `var dueDate` → `Date` or `null`

If set, the project should be completed by this date.

### `var effectiveCompletedDate` → `Date` or `null` *read-only*

Returns the computed effective completion date for the `Project`, based on its local `completionDate` and those of its containers.

### `var effectiveDeferDate` → `Date` or `null` *read-only*

Returns the computed effective defer date for the `Project`, based on its local `deferDate` and those of its containers.

### `var effectiveDropDate` → `Date` or `null` *read-only*

Returns the computed effective drop date for the `Project`, based on its local `dropDate` and those of its containers.

### `var effectiveDueDate` → `Date` or `null` *read-only*

Returns the computed effective due date for the `Project`, based on its local `dateDue` and those of its containers.

### `var effectiveFlagged` → `Boolean` *read-only*

Returns the computed effective flagged status for the `Project`, based on its local `flagged` and those of its containers.

### `var ending` → [`Task.ChildInsertionLocation`](https://omni-automation.com/omnifocus/OF-API.html#Task.ChildInsertionLocation) *read-only*

Returns a location referring to the position before the first `Task` directly contained in the root `task` of this project.

### `var estimatedMinutes` → `Number` or `null`

The estimated number of minutes this `Project` will take to finish, or `null` if no estimate has been made.

### `var flagged` → `Boolean`

The flagged status of the project.

### `var flattenedChildren` → [`TaskArray`](https://omni-automation.com/omnifocus/OF-API.html#TaskArray) *read-only*

An alias for `flattenedTasks`.

### `var flattenedTasks` → [`TaskArray`](https://omni-automation.com/omnifocus/OF-API.html#TaskArray) *read-only*

Returns a flat array of all tasks contained within this `Project`’s root `Task`. Tasks are sorted by their order in the database.

### `var hasChildren` → `Boolean` *read-only*

Returns `true` if this `Project`’s root `Task` has children, more efficiently than checking if `children` is empty.

### `var lastReviewDate` → `Date` or `null`

The date on which the last review was performed. See also `nextReviewDate`.

### `var linkedFileURLs` → `Array` of [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) *read-only*

The list of file URLs linked to this project’s root task. The files at these URLs are not present in the database, rather the database holds bookmarks leading to these files. These links can be read on iOS, but not written to.

### `var name` → `String`

The name of the `Project`s root task.

### `var nextReviewDate` → `Date` or `null`

The scheduled date for the next review. See also `nextReviewDate`.

### `var nextTask` → [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task) or `null` *read-only*

Returns the very next task that can be completed in the project, or `null` if there is none or the project contains singleton actions.

### `var note` → `String`

The `Project`’s root `Task`’s note.

### `var notifications` → `Array` of [`Task.Notification`](https://omni-automation.com/omnifocus/OF-API.html#Task.Notification) *read-only*

An array of the notifications that are active for this project.

### `var parentFolder` → [`Folder`](https://omni-automation.com/omnifocus/OF-API.html#Folder) or `null` *read-only*

The `Folder` which contains this project.

### `var repetitionRule` → [`Task.RepetitionRule`](https://omni-automation.com/omnifocus/OF-API.html#Task.RepetitionRule) or `null`

The object holding the repetition properties for this project, or null if it is not repeating.

### `var reviewInterval` → [`Project.ReviewInterval`](https://omni-automation.com/omnifocus/OF-API.html#Project.ReviewInterval)

The object holding the review repetition properties for this project. See also `lastReviewDate and`nextReviewDate`.

### `var sequential` → `Boolean`

If `true`, then children of this project form a dependency chain. For example, the first task blocks the second one until the first is completed.

### `var shouldUseFloatingTimeZone` → `Boolean`

When set, the `dueDate` and `deferDate` properties will use floating time zones. (Note: if a `Project` has no due or defer dates assigned, this property will revert to the database’s default setting.)

### `var status` → [`Project.Status`](https://omni-automation.com/omnifocus/OF-API.html#Project.Status)

The current status of the project as a whole. This does not reflect the status of individual tasks within the project root task – a project may be marked with the `Done` status and its individual tasks will be left with the completion state they had, in case the status is changed again to `Active`.

### `var tags` → [`TagArray`](https://omni-automation.com/omnifocus/OF-API.html#TagArray) *read-only*

Returns the `Tag`s associated with this `Project`.

### `var task` → [`Task`](https://omni-automation.com/omnifocus/OF-API.html#Task) *read-only*

Returns the root task of the project, which holds the bulk of the project information, as well as being the container for tasks within the project. If you wish to copy the project or move it to a location that requires tasks, you would use this task as the object to be copied or moved.

### `var taskStatus` → [`Task.Status`](https://omni-automation.com/omnifocus/OF-API.html#Task.Status) *read-only*

Returns the current status of the project.

### `var tasks` → [`TaskArray`](https://omni-automation.com/omnifocus/OF-API.html#TaskArray) *read-only*

Returns all the tasks contained directly in this `Project`’s root `Task`, sorted by their library order.

# `DateComponents`

## Constructors

### `new DateComponents()` → [`DateComponents`](https://omni-automation.com/omnifocus/OF-API.html#DateComponents)

## Instance Properties

### `var date` → `Date` or `null` *read-only*

### `var day` → `Number` or `null`

### `var era` → `Number` or `null`

### `var hour` → `Number` or `null`

### `var minute` → `Number` or `null`

### `var month` → `Number` or `null`

### `var nanosecond` → `Number` or `null`

### `var second` → `Number` or `null`

### `var timeZone` → [`TimeZone`](https://omni-automation.com/omnifocus/OF-API.html#TimeZone) or `null`

### `var year` → `Number` or `null`

# `DateRange`

## Instance Properties

### `var end` → `Date` *read-only*

### `var name` → `String` *read-only*

### `var start` → `Date` *read-only*

# `Decimal`

The `Decimal` class provides support for operating on base–10 numbers, which may not be exactly representable by types like the built-in JavaScript `Number` class. Note that `Decimal` does not use the built-in arithmetic operations; for example, to add two `Decimal` instances, you must use the `add()` function.

## Class Functions

### `function fromString(string: String)` → [`Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Decimal)

Parses the given string into a `Decimal`. If the string cannot be parsed, `notANumber` is returned.

## Class Properties

### `var maximum` → [`Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Decimal) *read-only*

Returns the maximum representable `Decimal` value.

### `var minimum` → [`Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Decimal) *read-only*

Returns the minimum representable `Decimal` value.

### `var notANumber` → [`Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Decimal) *read-only*

Returns a `Decimal` that represents a non-number value. Any arithmetic operations involving non-number values will return `notANumber`.

### `var one` → [`Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Decimal) *read-only*

Returns a `Decimal` representing one.

### `var zero` → [`Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Decimal) *read-only*

Returns a `Decimal` representing zero.

## Instance Functions

### `function toString()` → `String`

Converts the `Decimal` to a `String` representation.

### `function add(number: [Decimal](https://omni-automation.com/omnifocus/OF-API.html#Decimal))` → [`Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Decimal)

Generates a new `Decimal` by adding the argument and the receiver.

### `function subtract(number: [Decimal](https://omni-automation.com/omnifocus/OF-API.html#Decimal))` → [`Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Decimal)

Generates a new `Decimal` by subtracting the argument from the receiver.

### `function multiply(number: [Decimal](https://omni-automation.com/omnifocus/OF-API.html#Decimal))` → [`Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Decimal)

Generates a new `Decimal` by multiplying the argument and the receiver.

### `function divide(number: [Decimal](https://omni-automation.com/omnifocus/OF-API.html#Decimal))` → [`Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Decimal)

Generates a new `Decimal` by dividing the receiver by the argument.

### `function compare(number: [Decimal](https://omni-automation.com/omnifocus/OF-API.html#Decimal))` → `Number`

Compares the receiver and argument. If the receiver is less than the argument, –1 is returned. If the receiver is greater than the argument, 1 is returned. Otherwise, 0 is returned. `notANumber` is considered less than any valid number, and equal to itself.

### `function equals(number: [Decimal](https://omni-automation.com/omnifocus/OF-API.html#Decimal))` → `Boolean`

Returns `true` if the receiver and argument represent the same number (or both are `notANumber`), and `false` otherwise.

# `Device`

## Class Properties

### `var current` → [`Device`](https://omni-automation.com/omnifocus/OF-API.html#Device) *read-only*

The device the current application is running on.

## Instance Properties

### `var iOS` → `Boolean` *read-only*

A convenience that returns `true` on iPhone and iPad devices

### `var iPad` → `Boolean` *read-only*

A convenience that returns `true` only on iPad devices, but not on iPhone devices.

### `var mac` → `Boolean` *read-only*

A convenience that returns `true` only on Mac devices.

### `var operatingSystemVersion` → [`Version`](https://omni-automation.com/omnifocus/OF-API.html#Version) *read-only*

The current operation system version running on the device

### `var type` → [`DeviceType`](https://omni-automation.com/omnifocus/OF-API.html#DeviceType) or `null` *read-only*

The general type of the current device

# `DeviceType`

## Class Properties

### `var all` → `Array` of [`DeviceType`](https://omni-automation.com/omnifocus/OF-API.html#DeviceType) *read-only*

### `var iPad` → [`DeviceType`](https://omni-automation.com/omnifocus/OF-API.html#DeviceType) *read-only*

An iPad

### `var iPhone` → [`DeviceType`](https://omni-automation.com/omnifocus/OF-API.html#DeviceType) *read-only*

An iPhone

### `var mac` → [`DeviceType`](https://omni-automation.com/omnifocus/OF-API.html#DeviceType) *read-only*

A Mac device

# `Document`

## Class Functions

### `function makeNew(resultFunction: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function)(‍document: [Document](https://omni-automation.com/omnifocus/OF-API.html#Document) or [Error](https://omni-automation.com/omnifocus/OF-API.html#Error)‍) or null)` → [`Promise`](https://omni-automation.com/omnifocus/OF-API.html#Promise) of [`Document`](https://omni-automation.com/omnifocus/OF-API.html#Document)

Create a new document, which can be populated with data and then presented. On iOS, if the document is not presented by the time the `resultFunction` returns, it will be closed. On macOS, the document will be left around and accessible to the running script. `resultFunction` is executed before any functions tethered to the result Promise are executed. Returns a `Promise` that will yield the new document or an error.

### `function makeNewAndShow(resultFunction: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function)(‍document: [Document](https://omni-automation.com/omnifocus/OF-API.html#Document) or [Error](https://omni-automation.com/omnifocus/OF-API.html#Error)‍) or null)` → [`Promise`](https://omni-automation.com/omnifocus/OF-API.html#Promise) of [`Document`](https://omni-automation.com/omnifocus/OF-API.html#Document)

Create a new document and presents it. Returns a `Promise` that will yield the new document or an error.

## Instance Functions

### `function close(didCancel: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function)(‍document: [Document](https://omni-automation.com/omnifocus/OF-API.html#Document)‍) or null)`

Close this document. If for some reason the document cannot be closed, the `didCancel` function may be called at some point in the future, with the original document as the single argument. For example, on the Mac the user may review unsaved changes and may cancel the close operation. If the document is closed, the `didCancel` function will not be called at all.

### `function save()`

Save this document.

### `function fileWrapper(type: String or null)` → [`FileWrapper`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper)

Deprecated: Please use `makeFileWrapper()` instead.
Returns a new `FileWrapper` representing the contents of the document formatted as the specified type, or its current `fileType` if a `null` is passed for the type.

### `function makeFileWrapper(baseName: String, type: String or null)` → [`Promise`](https://omni-automation.com/omnifocus/OF-API.html#Promise) of [`FileWrapper`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper)

Generates a `FileWrapper` representing the contents of the document formatted as the specified type, or its current `fileType` if a `null` is passed for the type. Returns a `Promise` that will yield the file wrapper or an error. The returned file wrapper will have a name based off the given `baseName` and the default path extension for the requested file type.

### `function undo()`

Undo the last done action.

### `function redo()`

Redo the last undone action.

### `function show(completed: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function)(‍‍) or null)`

Presents the document, ordering the window forward on macOS, and possibly closing the existing document and opening the new on on iOS. Calls the completion function once the document is shown.

## Instance Properties

### `var canRedo` → `Boolean` *read-only*

Whether there are currently any actions that can be redone.

### `var canUndo` → `Boolean` *read-only*

Whether there are currently any actions that can be undone.

### `var fileType` → `String` or `null` *read-only*

The file type identifier the document uses when saving, if set.

### `var name` → `String` or `null` *read-only*

Document name.

### `var writableTypes` → `Array` of `String` *read-only*

A list of all of the file types that this document can be written as.

# `DatabaseDocument` : [`Document`](https://omni-automation.com/omnifocus/OF-API.html#Document)

## Instance Functions

### `function newWindow()` → [`Promise`](https://omni-automation.com/omnifocus/OF-API.html#Promise) of [`DocumentWindow`](https://omni-automation.com/omnifocus/OF-API.html#DocumentWindow)

Returns a `Promise` that will yield either a newly created and displayed `Window` or an error. On macOS, this method respects the System Preference governing new window behavior (tab vs. window). That preference is accessible at `System Preferences` > `Dock` > `Prefer tabs when opening documents`.

### `function newTabOnWindow(window: [DocumentWindow](https://omni-automation.com/omnifocus/OF-API.html#DocumentWindow))` → [`Promise`](https://omni-automation.com/omnifocus/OF-API.html#Promise) of [`DocumentWindow`](https://omni-automation.com/omnifocus/OF-API.html#DocumentWindow)

Returns a `Promise` that will yield either a new tab adjacent to `window` or an error. This is not available on iOS.

## Instance Properties

### `var windows` → `Array` of [`DocumentWindow`](https://omni-automation.com/omnifocus/OF-API.html#DocumentWindow) *read-only*

# `Email`

A set of parameters for generating an email.

## Constructors

### `new Email()` → [`Email`](https://omni-automation.com/omnifocus/OF-API.html#Email)

## Instance Functions

### `function generate()`

Presents the generated email to the user for them to send (or discard). On iOS, any included attachment `FileWrapper`s that are directories will be converted to Zip files.

## Instance Properties

### `var blindCarbonCopy` → `String` or `Array` of `String` or `null`

### `var body` → `String` or `null`

### `var carbonCopy` → `String` or `Array` of `String` or `null`

### `var fileWrappers` → `Array` of [`FileWrapper`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper)

### `var receiver` → `String` or `Array` of `String` or `null`

### `var subject` → `String` or `null`

# `Error`

The built-in JavaScript [`Error`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Error) constructor.

## Instance Properties

### `var causedByUserCancelling` → `Boolean` *read-only*

Returns true for errors that are caused by the user cancelling an operation. For example, if the user selects the Cancel button in a `FilePicker`, the `Promise` will signal an error that reflects this.

# `FilePicker`

A `FilePicker` allows the user to select `URL`s for files via the system-supplied file picking interface.

## Constructors

### `new FilePicker()` → [`FilePicker`](https://omni-automation.com/omnifocus/OF-API.html#FilePicker)

Returns a new `FilePicker` with default settings.

## Instance Functions

### `function show()` → [`Promise`](https://omni-automation.com/omnifocus/OF-API.html#Promise) of `Array` of [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL)

Presents the system file selection interface to the user, allowing them to choose one or more files of the given types. The returned `Promise` will yield the chosen `URL`s on success. If the user cancels chosing, the `Promise` will be rejected. Note that even when picking a single file or folder, the result will be an array of `URL`s.

## Instance Properties

### `var folders` → `Boolean`

If `true`, then folders may be selected, but not files. In this case, `types` is ignored. Defaults to `false`.

### `var message` → `String`

A message to display describing what files are being picked. This is currently only supported on macOS.

### `var multiple` → `Boolean`

If `true`, then multiple files may be selected. Defaults to `false`.

### `var types` → `Array` of [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) or `null`

The file types that will be allowed. If `null`, all file types will be allowed. Defaults to `null`.

# `FileSaver`

A `FileSaver` allows the user to save a `FileWrapper` to a `URL`s via the system-supplied file picking interface.

## Constructors

### `new FileSaver()` → [`FileSaver`](https://omni-automation.com/omnifocus/OF-API.html#FileSaver)

Returns a new `FileSaver` with default settings.

## Instance Functions

### `function show(fileWrapper: [FileWrapper](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper))` → [`Promise`](https://omni-automation.com/omnifocus/OF-API.html#Promise) of [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL)

Presents the system file saving interface to the user, allowing them to choose a location and file name to save the file wrapper. The returned `Promise` will yield the chosen `URL` on success. If the user cancels chosing, the `Promise` will be rejected.

## Instance Properties

### `var message` → `String`

A message to display describing what file is being saved. This is currently only supported on macOS.

### `var nameLabel` → `String`

The label shown next to the user-editable file name field. This is currently only supported on macOS.

### `var prompt` → `String`

The prompt shown on the the save button. This is currently only supported on macOS.

### `var types` → `Array` of [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) or `null`

The file types that will be allowed. If `null`, all file types will be allowed. Defaults to `null`.

# `FileWrapper`

## Class Functions

### `function withContents(name: String or null, contents: [Data](https://omni-automation.com/omnifocus/OF-API.html#Data))` → [`FileWrapper`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper)

Returns a new `FileWrapper` that represents a flat file containing the given data.

### `function withChildren(name: String or null, children: Array of [FileWrapper](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper))` → [`FileWrapper`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper)

Returns a new `FileWrapper` that represents a directory with the given child file wrappers. Each child file wrapper must have a unique name specified.

### `function fromURL(url: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL), options: Array of [FileWrapper.ReadingOptions](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.ReadingOptions) or null)` → [`FileWrapper`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper)

Reads a `FileWrapper` from an existing URL.

## Instance Functions

### `function childNamed(name: String)` → [`FileWrapper`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper) or `null`

Returns the child file wrapper with the specified name, or `null` if the receiver is not a directory or doesn’t have a child with that name.

### `function filenameForChild(child: [FileWrapper](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper))` → `String` or `null`

Returns the unique file name that will be used for the given child `FileWrapper`, or `null` if this file wrapper is not a child of the receiver.

### `function write(url: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL), options: Array of [FileWrapper.WritingOptions](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.WritingOptions) or null, originalContentsURL: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL) or null)`

Writes the `FileWrapper` to the given `URL`.
NOTE: Any existing file or folder at the desination `URL` will be replaced. Care must be taken when developing scripts to avoid unintended data loss.

## Instance Properties

### `var children` → `Array` of [`FileWrapper`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper) *read-only*

Returns an `Array` of child `FileWrappers`, if this represents a directory. Otherwise, an empty array is returned.

### `var contents` → [`Data`](https://omni-automation.com/omnifocus/OF-API.html#Data) or `null` *read-only*

Returns the regular file contents of the wrapper, if this represents a regular file. Otherwise, `null` is returned.

### `var destination` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null` *read-only*

Returns the destination if this represents a symbolic link. Otherwise, `null` is returned.

### `var filename` → `String` or `null`

Returns the actual file name that was last read for this file wrapper. Depending on the names of other sibling wrappers, this may not be what file name will be written.

### `var preferredFilename` → `String` or `null`

Returns the preferred file name that should be used when writing the file wrapper if no other file in the same parent directory wrapper is in use.

### `var type` → [`FileWrapper.Type`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.Type) *read-only*

Returns the type of this `FileWrapper`.

# `FileWrapper.ReadingOptions`

## Class Properties

### `var Immediate` → [`FileWrapper.ReadingOptions`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.ReadingOptions) *read-only*

Whether the contents are read immediately, or (by default) as the file wrappers are accessed.

### `var WihtoutMapping` → [`FileWrapper.ReadingOptions`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.ReadingOptions) *read-only*

Allow disabling file mapping.

### `var all` → `Array` of [`FileWrapper.ReadingOptions`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.ReadingOptions) *read-only*

# `FileWrapper.Type`

## Class Properties

### `var Directory` → [`FileWrapper.Type`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.Type) *read-only*

A `FileWrapper` that represents a directory with zero or more child wrappers.

### `var File` → [`FileWrapper.Type`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.Type) *read-only*

A `FileWrapper` that represents a regular file with data contents.

### `var Link` → [`FileWrapper.Type`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.Type) *read-only*

A `FileWrapper` that represents a symbolic link to another location.

### `var all` → `Array` of [`FileWrapper.Type`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.Type) *read-only*

# `FileWrapper.WritingOptions`

## Class Properties

### `var Atomic` → [`FileWrapper.WritingOptions`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.WritingOptions) *read-only*

Write the entire `FileWrapper` atomically, so that either the entire file package is replaced or none of it is.

### `var UpdateNames` → [`FileWrapper.WritingOptions`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.WritingOptions) *read-only*

On successful writing, update the filename of each file wrapper recursively so that following writes can use performance optimizations using hard links.

### `var all` → `Array` of [`FileWrapper.WritingOptions`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper.WritingOptions) *read-only*

# `Folder.ChildInsertionLocation`

A location specified relative to an existing `Folder`, `Project`, or `Database`. These cannot be instantiated directly, rather they are returned from properties like `Folder.beginning`, `Project.before`, or `Database.ending`.

# `Folder.Status`

## Class Properties

### `var Active` → [`Folder.Status`](https://omni-automation.com/omnifocus/OF-API.html#Folder.Status) *read-only*

The folder is active.

### `var Dropped` → [`Folder.Status`](https://omni-automation.com/omnifocus/OF-API.html#Folder.Status) *read-only*

The folder has been dropped.

### `var all` → `Array` of [`Folder.Status`](https://omni-automation.com/omnifocus/OF-API.html#Folder.Status) *read-only*

# `ForecastDay`

An object representing one of the selectable days in the forecast perspective.

## Class Properties

### `var badgeCountsIncludeDeferredItems` → `Boolean`

Determines whether or not badges on Forecast days include items that are not yet available.

## Instance Functions

### `function badgeKind()` → [`ForecastDay.Status`](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay.Status)

The status of the badge on this forecast day.

## Instance Properties

### `var badgeCount` → `Number` *read-only*

The number of available tasks on this forecast day.

### `var date` → `Date` *read-only*

The date this forecast day represents. If this day’s kind is `Past` or `DistantFuture` the date returned will be years from the current time.

### `var deferredCount` → `Number` *read-only*

The number of remaining deferred tasks on this forecast day.

### `var kind` → [`ForecastDay.Kind`](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay.Kind) *read-only*

### `var name` → `String` *read-only*

# `ForecastDay.Kind`

## Class Properties

### `var Day` → [`ForecastDay.Kind`](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay.Kind) *read-only*

The node represents a specific day in the Forecast week or month grid.

### `var DistantFuture` → [`ForecastDay.Kind`](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay.Kind) *read-only*

The node represents all days more than a year from now.

### `var FutureMonth` → [`ForecastDay.Kind`](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay.Kind) *read-only*

The node represents a month within the next year.

### `var Past` → [`ForecastDay.Kind`](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay.Kind) *read-only*

The node represents all days in the past.

### `var Today` → [`ForecastDay.Kind`](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay.Kind) *read-only*

The node represents today.

### `var all` → `Array` of [`ForecastDay.Kind`](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay.Kind) *read-only*

# `ForecastDay.Status`

## Class Properties

### `var Available` → [`ForecastDay.Status`](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay.Status) *read-only*

There is at least one available task on the node’s day, but no task is due soon or overdue. The node’s badgeCount is the number of available tasks.

### `var DueSoon` → [`ForecastDay.Status`](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay.Status) *read-only*

There is at least one available task on the node’s day, and at least one task due that day is due soon, but no tasks due that day are overdue. The node’s badgeCount is the number of available tasks.

### `var NoneAvailable` → [`ForecastDay.Status`](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay.Status) *read-only*

There are no available tasks on the node’s day. The node’s badgeCount is guaranteed to be zero.

### `var Overdue` → [`ForecastDay.Status`](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay.Status) *read-only*

There is at least one available task on the node’s day, and at least one task due that day is overdue. The node’s badgeCount is the number of available tasks.

### `var all` → `Array` of [`ForecastDay.Status`](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay.Status) *read-only*

# `Form`

`Form` provides a mechanism to collect input from the user. Each form contains one or more instances of subclasses of `Field`, which are given a key. As the form is filled out, `values` object is populated with the values from the user interface.

## Constructors

### `new Form()` → [`Form`](https://omni-automation.com/omnifocus/OF-API.html#Form)

## Instance Functions

### `function addField(field: [Form.Field](https://omni-automation.com/omnifocus/OF-API.html#Form.Field), index: Number or null)`

Adds the new `Field` to the `Form`, at the indicated position, or at the end if no position is specified. If the field has a default value, it will be added to the `values` result object immediately.

### `function removeField(field: [Form.Field](https://omni-automation.com/omnifocus/OF-API.html#Form.Field))`

Removes the `Field from the`Form`. Any entry in the`values` for this field will be removed as well.

### `function show(title: String, confirmTitle: String)` → [`Promise`](https://omni-automation.com/omnifocus/OF-API.html#Promise) of [`Form`](https://omni-automation.com/omnifocus/OF-API.html#Form)

Present the `Form` to the user, and return a `Promise` to be fullfilled or rejected when the user commits or cancels the form.

## Instance Properties

### `var fields` → `Array` of [`Form.Field`](https://omni-automation.com/omnifocus/OF-API.html#Form.Field) *read-only*

The current `Field` instances in the form, which will be visible to the user entering input.

### `var validate` → [`Function`](https://omni-automation.com/omnifocus/OF-API.html#Function)`(`‍Form: [`Form`](https://omni-automation.com/omnifocus/OF-API.html#Form)‍`)` → `Boolean` or `null`

A function to check whether the entered values are acceptable. The form to validate is passed as the argument and the function is expected to return a boolean result or null to perform default validation. If an `Error` is thrown, it’s message will be displayed in the form as the reason for validation failure. Note that the validation function may add or remove fields and update entries in the `values` object (which will cause the interface to be updated). This is called any time the user edits values, or a field is added or removed. If no `validate` function is specified or it returns `null`, some per-field default validation will be performed (see `Form.Field.Option`. If the `validate` function returns a boolean result, no default validation will be performed.

### `var values` → [`Object`](https://omni-automation.com/omnifocus/OF-API.html#Object) *read-only*

An object with the collected values for each field, stored under the key for that field.

# `Form.Field`

A single entry for a user input value in a `Form`. Each field can only be added to a single `Form`. This class cannot be constructed directly.

## Instance Properties

### `var displayName` → `String` or `null` *read-only*

Human readable string used as the label for this field.

### `var key` → `String` *read-only*

Key to use when storing the value for this field in the containing form’s `values` object.

# `Form.Field.Checkbox` : [`Form.Field`](https://omni-automation.com/omnifocus/OF-API.html#Form.Field)

## Constructors

### `new Form.Field.Checkbox(key: String, displayName: String or null, value: Boolean or null)` → [`Form.Field.Checkbox`](https://omni-automation.com/omnifocus/OF-API.html#Form.Field.Checkbox)

Returns a new `Checkbox` field, optionally with an initial value (which will be `false` if no value is specified).

# `Form.Field.Date` : [`Form.Field`](https://omni-automation.com/omnifocus/OF-API.html#Form.Field)

## Constructors

### `new Form.Field.Date(key: String, displayName: String or null, value: Date or null, formatter: [Formatter.Date](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Date) or null)` → [`Form.Field.Date`](https://omni-automation.com/omnifocus/OF-API.html#Form.Field.Date)

Returns a new `Date` field, optionally with an initial value, and optionally a date formatter. If no formatter is specified, a default one will be created that follows the user’s date formatting preferences to display and determine component ordering when parsing dates. Relative dates like “1d”, “tomorrow”, “now” can also be entered.

# `Form.Field.MultipleOptions` : [`Form.Field`](https://omni-automation.com/omnifocus/OF-API.html#Form.Field)

## Constructors

### `new Form.Field.MultipleOptions(key: String, displayName: String or null, options: Array of [Object](https://omni-automation.com/omnifocus/OF-API.html#Object), names: Array of String or null, selected: Array of [Object](https://omni-automation.com/omnifocus/OF-API.html#Object))` → [`Form.Field.MultipleOptions`](https://omni-automation.com/omnifocus/OF-API.html#Form.Field.MultipleOptions)

Returns a new `MultipleOptions` field, allowing the user to pick multiple items from a list of option objects. A list of names may also be given, which must have the same length as the options array if so. If no names are given, the objects are converted to strings for display. An array of zero or more initially selected objects (which must be members of the options array) may also be given. An empty array is valid input for the initially selected items. Additionally, it is valid for `MultipleOptions` fields to have a value that is an empty array.

# `Form.Field.Option` : [`Form.Field`](https://omni-automation.com/omnifocus/OF-API.html#Form.Field)

## Constructors

### `new Form.Field.Option(key: String, displayName: String or null, options: Array of [Object](https://omni-automation.com/omnifocus/OF-API.html#Object), names: Array of String or null, selected: [Object](https://omni-automation.com/omnifocus/OF-API.html#Object) or null, nullOptionTitle: String or null)` → [`Form.Field.Option`](https://omni-automation.com/omnifocus/OF-API.html#Form.Field.Option)

Returns a new `Option` field, allowing the user to pick from a list of option objects. A list of names may also be given, which must have the same length as the options array if so. If no names are given, the objects are converted to strings for display. An initially selected object (which must be a member of the options array) may also be given. If the field is not configured to allow a `null` value and no initially `selected` value is specified, the user must select a value before the field is considered valid under the default form validation.

## Instance Properties

### `var allowsNull` → `Boolean`

If set to `true`, an option will be added to allow selecting `null`.

### `var nullOptionTitle` → `String` or `null`

If `null` is allowed, this will be used for the title of that option. Otherwise a default title will be used.

# `Form.Field.Password` : [`Form.Field`](https://omni-automation.com/omnifocus/OF-API.html#Form.Field)

A field for text-based input, optionally using a `Formatter` to convert the string value into a different type.

## Constructors

### `new Form.Field.Password(key: String, displayName: String or null, value: String or null)` → [`Form.Field.Password`](https://omni-automation.com/omnifocus/OF-API.html#Form.Field.Password)

Returns a new `Password` field, optionally with an initial value. The displayed text will be obscured.

# `Form.Field.String` : [`Form.Field`](https://omni-automation.com/omnifocus/OF-API.html#Form.Field)

A field for text-based input, optionally using a `Formatter` to convert the string value into a different type.

## Constructors

### `new Form.Field.String(key: String, displayName: String or null, value: [Object](https://omni-automation.com/omnifocus/OF-API.html#Object) or null, formatter: [Formatter](https://omni-automation.com/omnifocus/OF-API.html#Formatter) or null)` → [`Form.Field.String`](https://omni-automation.com/omnifocus/OF-API.html#Form.Field.String)

Returns a new `String` field, optionally with an initial value and formatter. If a formatter is specified, the value should be of the output type from the formatter or null. If no formatter is specified, the value should be a string or null.

# `Formatter`

# `Formatter.Date` : [`Formatter`](https://omni-automation.com/omnifocus/OF-API.html#Formatter)

## Class Functions

### `function withStyle(dateStyle: [Formatter.Date.Style](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Date.Style), timeStyle: [Formatter.Date.Style](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Date.Style) or null)` → [`Formatter.Date`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Date)

A formatter that will display dates according to the specified date and time formats selected in system settings.

### `function withFormat(format: String)` → [`Formatter.Date`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Date)

Returns a formatter with a specific ICU date format and the user’s current locale, calendar, and timeZone. See <http://userguide.icu-project.org/formatparse/datetime/> for details on date format strings.

## Class Properties

### `var iso8601` → [`Formatter.Date`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Date) *read-only*

Return a date formatter that produces ISO–8601 formatted dates, using the Gregorian calendar and the UTC time zone.

## Instance Functions

### `function stringFromDate(date: Date)` → `String`

### `function dateFromString(string: String)` → `Date` or `null`

## Instance Properties

### `var calendar` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar)

### `var dateFormat` → `String` *read-only*

### `var locale` → [`Locale`](https://omni-automation.com/omnifocus/OF-API.html#Locale)

### `var timeZone` → [`TimeZone`](https://omni-automation.com/omnifocus/OF-API.html#TimeZone)

# `Formatter.Decimal` : [`Formatter`](https://omni-automation.com/omnifocus/OF-API.html#Formatter)

This formatter class formats and parses `Decimal`-valued strings (note, *not* `Number` values).

## Class Functions

### `function currency(code: String or null)` → [`Formatter.Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Decimal)

Returns a new formatter that will display the value as a currency value. An ISO currency code may be specified to pick a specific currency, or null may be passed to use the default currency for the user’s locale. If the argument is not a valid currency code, an error will be thrown.

## Class Properties

### `var currencyCodes` → `Array` of `String` *read-only*

Deprecated: Please use the `currencyCode` property on `Locale` instead.
Returns the list of known ISO currency codes

### `var custom` → [`Formatter.Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Decimal) *read-only*

Returns a new formatter that can be configured with custom settings.

### `var decimal` → [`Formatter.Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Decimal) *read-only*

Returns a new number formatter that will use both a decimal separator.

### `var percent` → [`Formatter.Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Decimal) *read-only*

Returns a new number formatter that will display the value as a percentage.

### `var percentWithDecimal` → [`Formatter.Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Decimal) *read-only*

Returns a new number formatter that will display the value as a percentage with a decimal separator.

### `var plain` → [`Formatter.Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Decimal) *read-only*

Returns a new number formatter that will not use any separators.

### `var thousandsAndDecimal` → [`Formatter.Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Decimal) *read-only*

Returns a new number formatter that will use both a thousands and decimal separator.

## Instance Functions

### `function stringFromDecimal(number: [Decimal](https://omni-automation.com/omnifocus/OF-API.html#Decimal))` → `String` or `null`

Format a `Decimal` as a string, based on the rules set on the formatter.

### `function decimalFromString(string: String)` → [`Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Decimal) or `null`

Parses a `Decimal` from a string, based on the rules set on the formatter. Returns `null` if the value was not recognized.

## Instance Properties

### `var decimalSeparator` → `String`

The string to display between the whole portion of a number and the decimal portion.

### `var negativeFormat` → `String`

A format string to use for negative values.

### `var positiveFormat` → `String`

A format string to use for positive values.

### `var thousandsSeparator` → `String` or `null`

The string to display between groups of digits representing powers of a thousand.

### `var zeroSymbol` → `String` or `null`

The string to use when displaying a zero value. If this is `null`, the `positiveFormat` is used.

# `Formatter.Duration` : [`Formatter`](https://omni-automation.com/omnifocus/OF-API.html#Formatter)

## Constructors

### `new Formatter.Duration()` → [`Formatter.Duration`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Duration)

## Instance Functions

### `function stringFromDecimal(number: [Decimal](https://omni-automation.com/omnifocus/OF-API.html#Decimal))` → `String` or `null`

### `function decimalFromString(string: String)` → [`Decimal`](https://omni-automation.com/omnifocus/OF-API.html#Decimal) or `null`

## Instance Properties

### `var hoursPerDay` → `Number`

### `var hoursPerWeek` → `Number`

### `var useVerboseFormat` → `Boolean`

# `Formatter.Date.Style`

## Class Properties

### `var Full` → [`Formatter.Date.Style`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Date.Style) *read-only*

Use the user’s “full” format as selected in system settings.

### `var Long` → [`Formatter.Date.Style`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Date.Style) *read-only*

Use the user’s “long” format as selected in system settings.

### `var Medium` → [`Formatter.Date.Style`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Date.Style) *read-only*

Use the user’s “medium” format as selected in system settings.

### `var Short` → [`Formatter.Date.Style`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Date.Style) *read-only*

Use the user’s “short” format as selected in system settings.

### `var all` → `Array` of [`Formatter.Date.Style`](https://omni-automation.com/omnifocus/OF-API.html#Formatter.Date.Style) *read-only*

# `Function`

The built-in JavaScript [`Function`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function) constructor.

# `Image`

## Class Functions

### `function symbolNamed(name: String)` → [`Image`](https://omni-automation.com/omnifocus/OF-API.html#Image) or `null`

Returns an image given a symbol name.

# `LigatureStyle`

## Class Properties

### `var All` → [`LigatureStyle`](https://omni-automation.com/omnifocus/OF-API.html#LigatureStyle) *read-only*

Use all of the available ligatures.

### `var Essential` → [`LigatureStyle`](https://omni-automation.com/omnifocus/OF-API.html#LigatureStyle) *read-only*

Use ligatures that are required for proper rendering of text.

### `var Standard` → [`LigatureStyle`](https://omni-automation.com/omnifocus/OF-API.html#LigatureStyle) *read-only*

Use the default ligatures for the given script.

### `var all` → `Array` of [`LigatureStyle`](https://omni-automation.com/omnifocus/OF-API.html#LigatureStyle) *read-only*

# `Locale`

## Class Properties

### `var identifiers` → `Array` of `String` *read-only*

The list of known ISO locale identifiers.

## Constructors

### `new Locale(identifier: String)` → [`Locale`](https://omni-automation.com/omnifocus/OF-API.html#Locale)

## Instance Properties

### `var calendar` → [`Calendar`](https://omni-automation.com/omnifocus/OF-API.html#Calendar) *read-only*

The calendar for the locale.

### `var currencyCode` → `String` or `null` *read-only*

The currency code for the locale.

### `var identifier` → `String` *read-only*

The ISO locale identifier for this object.

# `MenuItem`

## Instance Properties

### `var checked` → `Boolean`

If true, a checkmark is displayed next to the `MenuItem`’s label.

### `var image` → [`Image`](https://omni-automation.com/omnifocus/OF-API.html#Image) or `null`

An optional image to be displayed with the `MenuItem`.

### `var label` → `String`

The string displayed to describe the `MenuItem`’s action.

# `NamedStyle.List`

## Instance Functions

### `function add(name: String or null)` → [`NamedStyle`](https://omni-automation.com/omnifocus/OF-API.html#NamedStyle)

Makes a new `NamedStyle` at the end of the `NamedStyleList`, and optionally assigns it a name.

### `function byName(name: String)` → [`NamedStyle`](https://omni-automation.com/omnifocus/OF-API.html#NamedStyle) or `null`

Returns the first named style that has the specified `name`, or `null` if none do.

### `function byIdentifier(identifier: String)` → [`NamedStyle`](https://omni-automation.com/omnifocus/OF-API.html#NamedStyle) or `null`

Returns the single named style with the specified `identifier`, or `null` if no style has that `identifier`.

### `function moveStyles(styles: Array of [NamedStyle](https://omni-automation.com/omnifocus/OF-API.html#NamedStyle), position: [NamedStylePosition](https://omni-automation.com/omnifocus/OF-API.html#NamedStylePosition))`

Reorders the named styles within the `NamedStyleList`. This cannot be used to move styles between documents.

### `function duplicateStyles(styles: Array of [NamedStyle](https://omni-automation.com/omnifocus/OF-API.html#NamedStyle), position: [NamedStylePosition](https://omni-automation.com/omnifocus/OF-API.html#NamedStylePosition))` → `Array` of [`NamedStyle`](https://omni-automation.com/omnifocus/OF-API.html#NamedStyle)

## Instance Properties

### `var all` → `Array` of [`NamedStyle`](https://omni-automation.com/omnifocus/OF-API.html#NamedStyle) *read-only*

Returns the list of all `NamedStyles`. Note that the order determine which attribute values are applied if two named styles have conflicting settings.

### `var beginning` → [`NamedStylePosition`](https://omni-automation.com/omnifocus/OF-API.html#NamedStylePosition) *read-only*

Returns a `NamedStylePosition` that indicates the position before any existing named styles.

### `var end` → [`NamedStylePosition`](https://omni-automation.com/omnifocus/OF-API.html#NamedStylePosition) *read-only*

Returns a `NamedStylePosition` that indicates the position before after existing named styles.

# `NamedStylePosition`

# `Notification`

## Constructors

### `new Notification(title: String)` → [`Notification`](https://omni-automation.com/omnifocus/OF-API.html#Notification)

## Instance Functions

### `function show()` → [`Promise`](https://omni-automation.com/omnifocus/OF-API.html#Promise) of [`Notification`](https://omni-automation.com/omnifocus/OF-API.html#Notification)

Attempts to present the notification and returns a `Promise` which will yield the notification object itself if it is clicked or tapped, or an error if it cannot be presented or is dismissed.

## Instance Properties

### `var subtitle` → `String` or `null`

### `var title` → `String`

# `ObjectIdentifier`

A unique identifier referring to a `DatabaseObject`.

## Instance Properties

### `var objectClass` → [`Object`](https://omni-automation.com/omnifocus/OF-API.html#Object) or `null` *read-only*

Returns the constructor object that would be used for instances of the class for this `ObjectIdentifier`.

### `var primaryKey` → `String` *read-only*

Returns the primary key of the object identifier.

# `Pasteboard`

A pasteboard temporarily holds representations of items of different types for transfer between different applications or different locations in the application.

## Class Functions

### `function makeUnique()` → [`Pasteboard`](https://omni-automation.com/omnifocus/OF-API.html#Pasteboard)

Creates a new unique pasteboard.

## Class Properties

### `var general` → [`Pasteboard`](https://omni-automation.com/omnifocus/OF-API.html#Pasteboard) *read-only*

The `Pasteboard` used for user-initiated copy/paste support.

## Instance Functions

### `function availableType(types: Array of [TypeIdentifier](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier))` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) or `null`

The first type from the provided list which is available on the pasteboard, or `null` if none are available.

### `function addItems(items: Array of [Pasteboard.Item](https://omni-automation.com/omnifocus/OF-API.html#Pasteboard.Item))`

Appends the new items to the pasteboard.

### `function clear()`

Remove all items from the pasteboard.

### `function dataForType(type: [TypeIdentifier](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier))` → [`Data`](https://omni-automation.com/omnifocus/OF-API.html#Data) or `null`

The `Data` representation for the given type in this pasteboard, or `null` if none is available.

### `function setDataForType(data: [Data](https://omni-automation.com/omnifocus/OF-API.html#Data), type: [TypeIdentifier](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier))`

Set the `Data` representation for the given type in this pasteboard, replacing any previously set data.

### `function stringForType(type: [TypeIdentifier](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier))` → `String` or `null`

The `String` representation for the given type in this pasteboard, or `null` if none is available.

### `function setStringForType(string: String, type: [TypeIdentifier](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier))`

Set the `String` representation for the given type in this pasteboard, replacing any previously set data.

## Instance Properties

### `var URL` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null`

Gets or sets the pasteboard content as a single URL.

### `var URLs` → `Array` of [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null`

Gets or sets the pasteboard content as a list of URLs.

### `var color` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) or `null`

Gets or sets the pasteboard content as a single color.

### `var colors` → `Array` of [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color) or `null`

Gets or sets the pasteboard content as a list of colors.

### `var hasColors` → `Boolean` *read-only*

Returns `true` if the pasteboard contains one or more colors.

### `var hasImages` → `Boolean` *read-only*

Returns `true` if the pasteboard contains one or more images.

### `var hasStrings` → `Boolean` *read-only*

Returns `true` if the pasteboard contains one or more strings.

### `var hasURLs` → `Boolean` *read-only*

Returns `true` if the pasteboard contains one or more URLs.

### `var image` → [`Image`](https://omni-automation.com/omnifocus/OF-API.html#Image) or `null`

Gets or sets the pasteboard content as a single image.

### `var images` → `Array` of [`Image`](https://omni-automation.com/omnifocus/OF-API.html#Image) or `null`

Gets or sets the pasteboard content as a list of images.

### `var items` → `Array` of [`Pasteboard.Item`](https://omni-automation.com/omnifocus/OF-API.html#Pasteboard.Item)

The array of individual items on the pasteboard, each potentially with their own set of types.

### `var string` → `String` or `null`

Gets or sets the pasteboard content as a single plain-text string.

### `var strings` → `Array` of `String` or `null`

Gets or sets the pasteboard content as a list of plain-text strings.

### `var types` → `Array` of [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The list of pasteboard types currently available on the pasteboard.

# `Pasteboard.Item`

## Constructors

### `new Pasteboard.Item()` → [`Pasteboard.Item`](https://omni-automation.com/omnifocus/OF-API.html#Pasteboard.Item)

Make a new empty pasteboard item with no contents.

## Instance Functions

### `function dataForType(type: [TypeIdentifier](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier))` → [`Data`](https://omni-automation.com/omnifocus/OF-API.html#Data) or `null`

The `Data` representation for the given type in this pasteboard item, or `null` if none is available.

### `function setDataForType(data: [Data](https://omni-automation.com/omnifocus/OF-API.html#Data), type: [TypeIdentifier](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier))`

Set the `Data` representation for the given type in this pasteboard item, replacing any previously set data.

### `function stringForType(type: [TypeIdentifier](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier))` → `String` or `null`

The `String` representation for the given type in this pasteboard item, or `null` if none is available.

### `function setStringForType(string: String, type: [TypeIdentifier](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier))`

Set the `String` representation for the given type in this pasteboard item, replacing any previously set data.

## Instance Properties

### `var types` → `Array` of [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The list of types available for this pasteboard item.

# `Perspective`

## Class Properties

### `var all` → `Array` of [`Perspective.BuiltIn`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.BuiltIn) or [`Perspective.Custom`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.Custom) *read-only*

Returns all the built-in and custom perspectives.

# `Perspective.BuiltIn`

## Class Properties

### `var Flagged` → [`Perspective.BuiltIn`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.BuiltIn) *read-only*

The flagged items.

### `var Forecast` → [`Perspective.BuiltIn`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.BuiltIn) *read-only*

The upcoming due items.

### `var Inbox` → [`Perspective.BuiltIn`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.BuiltIn) *read-only*

The inbox of tasks.

### `var Nearby` → [`Perspective.BuiltIn`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.BuiltIn) *read-only*

Nearby items on a map (iOS only).

### `var Projects` → [`Perspective.BuiltIn`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.BuiltIn) *read-only*

The library of projects.

### `var Review` → [`Perspective.BuiltIn`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.BuiltIn) *read-only*

The projects needing review.

### `var Search` → [`Perspective.BuiltIn`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.BuiltIn) *read-only*

A search of the database. This perspective cannot be set, but might be reported if the user is searching.

### `var Tags` → [`Perspective.BuiltIn`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.BuiltIn) *read-only*

The hierarchy of tags.

### `var all` → `Array` of [`Perspective.BuiltIn`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.BuiltIn) *read-only*

## Instance Properties

### `var name` → `String` *read-only*

The name of the built in perspective.

# `PlugIn`

## Class Functions

### `function find(identifier: String, minimumVersion: [Version](https://omni-automation.com/omnifocus/OF-API.html#Version) or null)` → [`PlugIn`](https://omni-automation.com/omnifocus/OF-API.html#PlugIn) or `null`

## Class Properties

### `var all` → `Array` of [`PlugIn`](https://omni-automation.com/omnifocus/OF-API.html#PlugIn) *read-only*

## Instance Functions

### `function library(identifier: String)` → [`PlugIn.Library`](https://omni-automation.com/omnifocus/OF-API.html#PlugIn.Library) or `null`

Looks for a `PlugIn.Library` in the receiver and returns it if found.

### `function action(identifier: String)` → [`PlugIn.Action`](https://omni-automation.com/omnifocus/OF-API.html#PlugIn.Action) or `null`

### `function handler(identifier: String)` → [`PlugIn.Handler`](https://omni-automation.com/omnifocus/OF-API.html#PlugIn.Handler) or `null`

### `function resourceNamed(name: String)` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null`

### `function imageNamed(name: String)` → [`Image`](https://omni-automation.com/omnifocus/OF-API.html#Image) or `null`

### `function localizedResourceNamed(filename: String)` → [`FileWrapper`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper) or `null`

## Instance Properties

### `var URL` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null` *read-only*

Returns the original URL from whence this `PlugIn` came, if known.

### `var actions` → `Array` of [`PlugIn.Action`](https://omni-automation.com/omnifocus/OF-API.html#PlugIn.Action) *read-only*

### `var author` → `String` *read-only*

Returns the author for the `PlugIn`.

### `var description` → `String` *read-only*

Returns the description provided for the `PlugIn`.

### `var displayName` → `String` *read-only*

Returns the localized, human-readable name for the `PlugIn`.

### `var handlers` → `Array` of [`PlugIn.Handler`](https://omni-automation.com/omnifocus/OF-API.html#PlugIn.Handler) *read-only*

### `var identifier` → `String` *read-only*

The unique identifier of the `PlugIn`.

### `var libraries` → `Array` of [`PlugIn.Library`](https://omni-automation.com/omnifocus/OF-API.html#PlugIn.Library) *read-only*

### `var version` → [`Version`](https://omni-automation.com/omnifocus/OF-API.html#Version) *read-only*

Returns the current `Version` for the `PlugIn`.

# `PlugIn.Action`

## Constructors

### `new PlugIn.Action(perform: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function))` → [`PlugIn.Action`](https://omni-automation.com/omnifocus/OF-API.html#PlugIn.Action)

Returns a new `PlugIn.Action`. Only used within an action JavaScript file embedded within a PlugIn.

## Instance Properties

### `var description` → `String` *read-only*

### `var label` → `String` *read-only*

Returns the default label to use for interface controls that invoke the action.

### `var longLabel` → `String` *read-only*

Returns the label to use for interface controls that invoke the action, when a long amount of space is available.

### `var mediumLabel` → `String` *read-only*

Returns the label to use for interface controls that invoke the action, when a medium amount of space is available.

### `var name` → `String` *read-only*

Returns the name of the `PlugIn.Action`.

### `var paletteLabel` → `String` *read-only*

Returns the label to use for interface controls that show a prototype of the action control, such as on a macOS toolbar configuration sheet.

### `var perform` → [`Function`](https://omni-automation.com/omnifocus/OF-API.html#Function) *read-only*

### `var plugIn` → [`PlugIn`](https://omni-automation.com/omnifocus/OF-API.html#PlugIn) *read-only*

Returns the `PlugIn` that contains this object.

### `var shortLabel` → `String` *read-only*

Returns the label to use for interface controls that invoke the action, when a short amount of space is available.

### `var validate` → [`Function`](https://omni-automation.com/omnifocus/OF-API.html#Function) or `null`

A function to check whether the action is supported, given the current application state, as determined by the arguments passed (typically including the selection). This optional Function may be configured while the `Action` is being loaded, but after that the `Action` will be frozen.

# `PlugIn.Handler`

## Constructors

### `new PlugIn.Handler(invoke: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function))` → [`PlugIn.Handler`](https://omni-automation.com/omnifocus/OF-API.html#PlugIn.Handler)

Returns a new `PlugIn.Handler`. Only used within an handler JavaScript file embedded within a PlugIn.

## Instance Properties

### `var invoke` → [`Function`](https://omni-automation.com/omnifocus/OF-API.html#Function) *read-only*

The `Function` that will be executed for each handler registered for an event posted by an application object.

### `var name` → `String` *read-only*

Returns the name of the `PlugIn.Handler`.

### `var plugIn` → [`PlugIn`](https://omni-automation.com/omnifocus/OF-API.html#PlugIn) *read-only*

Returns the `PlugIn` that contains this object.

### `var willAttach` → [`Function`](https://omni-automation.com/omnifocus/OF-API.html#Function) or `null`

An optional `Function` that can be set on `PlugIn.Handler` as it is being loaded (but not after). This function is passed the application object that post events to trigger the handler. The return value should be a state object that is JSON archivable (or `undefined` if the handler has no state to maintain across invocations).

### `var willDetach` → [`Function`](https://omni-automation.com/omnifocus/OF-API.html#Function) or `null`

An optional `Function` that can be set on `PlugIn.Handler` as it is being loaded (but not after). Called when a previously attached `PlugIn.Handler` is being detached from an application object. Any return value or thrown error are ignored.

# `PlugIn.Library`

An object that represents a library from a plug-in.

## Constructors

### `new PlugIn.Library(version: [Version](https://omni-automation.com/omnifocus/OF-API.html#Version))` → [`PlugIn.Library`](https://omni-automation.com/omnifocus/OF-API.html#PlugIn.Library)

Returns a new `Library`. Typically only used within a library JavaScript file embedded within a PlugIn.

## Instance Properties

### `var name` → `String` *read-only*

Returns the name of the `PlugIn.Library`.

### `var plugIn` → [`PlugIn`](https://omni-automation.com/omnifocus/OF-API.html#PlugIn) *read-only*

Returns the `PlugIn` that contains this object.

### `var version` → [`Version`](https://omni-automation.com/omnifocus/OF-API.html#Version) *read-only*

Returns the `Version` of this library, as passed to the constructor.

# `Preferences`

## Constructors

### `new Preferences(identifier: String or null)` → [`Preferences`](https://omni-automation.com/omnifocus/OF-API.html#Preferences)

Creates a new `Preferences` instance. The identifier specified may be `null` to create an instance for the currently loading plug-in. If it is `null` and a plug-in is not being loaded, an error will be thrown. Key/value pairs stored in the instance will be prefixed with the identifier and a “.”.

## Instance Functions

### `function read(key: String)` → [`Object`](https://omni-automation.com/omnifocus/OF-API.html#Object) or `null`

Returns the previously stored value for the given key, or `null` if no value is stored.

### `function readBoolean(key: String)` → `Boolean`

Returns the previously stored value as a `Boolean`, or `false` if there is no stored value or it can’t be converted to a `Boolean`.

### `function readString(key: String)` → `String` or `null`

Returns the previously stored value as a `String`, or `null` if there is no stored value or it is not a `String`.

### `function readNumber(key: String)` → `Number`

Returns the previously stored value as a `Number`, or `null` if there is no stored value or it is not a `Number`.

### `function readDate(key: String)` → `Date` or `null`

Returns the previously stored value as a `Date`, or `null` if there is no stored value or it is not a `Date`.

### `function readData(key: String)` → [`Data`](https://omni-automation.com/omnifocus/OF-API.html#Data) or `null`

Returns the previously stored value as a `Data`, or `null` if there is no stored value or it is not a `Data`.

### `function write(key: String, value: Boolean, String, Number, Date, or [Data](https://omni-automation.com/omnifocus/OF-API.html#Data) or null)`

Stores the specified key/value pair, or removes the pair if `value` is `null`.

### `function remove(key: String)`

Removes and previously stored value for the given key.

## Instance Properties

### `var identifier` → `String` *read-only*

The scoping identifier the instance given when created, or the plug-in identifier if none was given.

# `Project.ReviewInterval`

`Project.ReviewInterval` is a value object which represents a simple repetition interval. Because it’s a value object rather than a proxy, changing its properties doesn’t affect any projects directly. To change a project’s review interval, update the value and assign it back to the project’s `reviewInterval` property:

```
 let project = projectNamed("Miscellaneous");
 let reviewInterval = project.reviewInterval;
 reviewInterval.steps = 3;
 reviewInterval.unit = "months";
 project.reviewInterval = reviewInterval;

```

Note: At one time these simple repetition intervals were also used for task repetitions, but over time we replaced those with the more flexible `Task.RepetitionRule`. Eventually we expect to also replace this review interval with flexible repetition rules.

## Instance Properties

### `var steps` → `Number`

The count of `units` to use for this interval (e.g. “14” days or “12” months).

### `var unit` → `String`

The units to use (e.g. “days”, “weeks”, “months”, “years”).

# `Project.Status`

## Class Properties

### `var Active` → [`Project.Status`](https://omni-automation.com/omnifocus/OF-API.html#Project.Status) *read-only*

The project is active.

### `var Done` → [`Project.Status`](https://omni-automation.com/omnifocus/OF-API.html#Project.Status) *read-only*

The project has been marked as completed.

### `var Dropped` → [`Project.Status`](https://omni-automation.com/omnifocus/OF-API.html#Project.Status) *read-only*

The project has been dropped.

### `var OnHold` → [`Project.Status`](https://omni-automation.com/omnifocus/OF-API.html#Project.Status) *read-only*

The project has been put on-hold.

### `var all` → `Array` of [`Project.Status`](https://omni-automation.com/omnifocus/OF-API.html#Project.Status) *read-only*

# `Promise`

The built-in JavaScript [`Promise`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) constructor.

# `Selection`

An object representing the current selection in a `Window`.

## Instance Properties

### `var allObjects` → `Array` of [`Object`](https://omni-automation.com/omnifocus/OF-API.html#Object) *read-only*

Returns all the objects in the selection.

### `var database` → [`Database`](https://omni-automation.com/omnifocus/OF-API.html#Database) or `null` *read-only*

Returns the `Database` object in the selection, if any.

### `var databaseObjects` → `Array` of [`DatabaseObject`](https://omni-automation.com/omnifocus/OF-API.html#DatabaseObject) *read-only*

Returns all the `DatabaseObject` objects in the selection, if any.

### `var document` → [`DatabaseDocument`](https://omni-automation.com/omnifocus/OF-API.html#DatabaseDocument) or `null` *read-only*

The `Document` containing the selection.

### `var folders` → [`FolderArray`](https://omni-automation.com/omnifocus/OF-API.html#FolderArray) *read-only*

Returns all the `Folder` objects in the selection, if any.

### `var projects` → [`ProjectArray`](https://omni-automation.com/omnifocus/OF-API.html#ProjectArray) *read-only*

Returns all the `Project` objects in the selection, if any.

### `var tags` → [`TagArray`](https://omni-automation.com/omnifocus/OF-API.html#TagArray) *read-only*

Returns all the `Tag` objects in the selection, if any.

### `var tasks` → [`TaskArray`](https://omni-automation.com/omnifocus/OF-API.html#TaskArray) *read-only*

Returns all the `Task` objects in the selection, if any.

### `var window` → [`DocumentWindow`](https://omni-automation.com/omnifocus/OF-API.html#DocumentWindow) or `null` *read-only*

The `Window` containing the selection.

# `Settings`

`Settings` represent the database synchronized configuration values. *NOTE:* editing these should be done with care, as storing invalid values may corrupt your database or produce instability in the various client applications.

## Instance Functions

### `function defaultObjectForKey(key: String)` → [`Object`](https://omni-automation.com/omnifocus/OF-API.html#Object) or `null`

### `function hasNonDefaultObjectForKey(key: String)` → `Boolean`

### `function objectForKey(key: String)` → [`Object`](https://omni-automation.com/omnifocus/OF-API.html#Object) or `null`

### `function setObjectForKey(value: [Object](https://omni-automation.com/omnifocus/OF-API.html#Object) or null, key: String)`

### `function boolForKey(key: String)` → `Boolean`

### `function setBoolForKey(value: Boolean, key: String)`

### `function integerForKey(key: String)` → `Number`

### `function setIntegerForKey(value: Number, key: String)`

## Instance Properties

### `var keys` → `Array` of `String` *read-only*

# `SharePanel`

An interface that can display the system share interaction for the given items.

## Constructors

### `new SharePanel(items: Array of [URL](https://omni-automation.com/omnifocus/OF-API.html#URL), String, [Image](https://omni-automation.com/omnifocus/OF-API.html#Image), or [FileWrapper](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper))` → [`SharePanel`](https://omni-automation.com/omnifocus/OF-API.html#SharePanel)

Create a new share panel with the given items.

## Instance Functions

### `function addItem(shareItem: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL), String, [Image](https://omni-automation.com/omnifocus/OF-API.html#Image), or [FileWrapper](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper))`

Appends the item to the end of `items`.

### `function addItems(shareItems: Array of [URL](https://omni-automation.com/omnifocus/OF-API.html#URL), String, [Image](https://omni-automation.com/omnifocus/OF-API.html#Image), or [FileWrapper](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper))`

Appends the contents of the given array to the end of `items`.

### `function removeItem(shareItem: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL), String, [Image](https://omni-automation.com/omnifocus/OF-API.html#Image), or [FileWrapper](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper))`

Removes the first occurrence of the item from `items` if it is present in `items`.

### `function removeItems(shareItems: Array of [URL](https://omni-automation.com/omnifocus/OF-API.html#URL), String, [Image](https://omni-automation.com/omnifocus/OF-API.html#Image), or [FileWrapper](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper))`

Removes the first occurrence of each member of the given array from `items` if that member is present in `items`.

### `function clearItems()`

Sets `items` to an empty array. Note: Calling `show` when `items` is empty results in an error, so be sure to add new items after calling this and before calling `show`.

### `function show()`

Presents the share panel for its items. Calling this when `items` is empty will result in an error.

## Instance Properties

### `var items` → `Array` of [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL), `String`, [`Image`](https://omni-automation.com/omnifocus/OF-API.html#Image), or [`FileWrapper`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper)

The items that will be supplied to the system share interaction upon calling `show`.

# `Speech`

# `Speech.Boundary`

## Class Properties

### `var Immediate` → [`Speech.Boundary`](https://omni-automation.com/omnifocus/OF-API.html#Speech.Boundary) *read-only*

### `var Word` → [`Speech.Boundary`](https://omni-automation.com/omnifocus/OF-API.html#Speech.Boundary) *read-only*

### `var all` → `Array` of [`Speech.Boundary`](https://omni-automation.com/omnifocus/OF-API.html#Speech.Boundary) *read-only*

# `Speech.Synthesizer`

## Constructors

### `new Speech.Synthesizer()` → [`Speech.Synthesizer`](https://omni-automation.com/omnifocus/OF-API.html#Speech.Synthesizer)

## Instance Functions

### `function speakUtterance(utterance: [Speech.Utterance](https://omni-automation.com/omnifocus/OF-API.html#Speech.Utterance))`

Enqueues the utterance. If the utterance is already or enqueued or speaking, throws and error.

### `function stopSpeaking(boundary: [Speech.Boundary](https://omni-automation.com/omnifocus/OF-API.html#Speech.Boundary))` → `Boolean`

### `function pauseSpeaking(boundary: [Speech.Boundary](https://omni-automation.com/omnifocus/OF-API.html#Speech.Boundary))` → `Boolean`

### `function continueSpeaking()` → `Boolean`

## Instance Properties

### `var paused` → `Boolean` *read-only*

### `var speaking` → `Boolean` *read-only*

# `Speech.Utterance`

## Class Properties

### `var defaultSpeechRate` → `Number` *read-only*

### `var maximumSpeechRate` → `Number` *read-only*

### `var minimumSpeechRate` → `Number` *read-only*

## Constructors

### `new Speech.Utterance(string: String)` → [`Speech.Utterance`](https://omni-automation.com/omnifocus/OF-API.html#Speech.Utterance)

## Instance Properties

### `var pitchMultiplier` → `Number`

A value between 0.5 and 2.0, controlling the picth of the utterance.

### `var postUtteranceDelay` → `Number`

### `var preUtteranceDelay` → `Number`

### `var prefersAssistiveTechnologySettings` → `Boolean`

If an assistive technology is on, like VoiceOver, the user’s selected voice, rate and other settings will be used for this speech utterance instead of the default values. If no assistive technologies are on, then the values of the properties on AVSpeechUtterance will be used. Note that querying the properties will not refect the user’s settings.

### `var rate` → `Number`

A value between `Speech.Utterance.minimumSpeechRate` and `Speech.Utterance.maximumSpeechRate` controlling the rate of speech for the utterance.

### `var string` → `String` or `null` *read-only*

### `var voice` → [`Speech.Voice`](https://omni-automation.com/omnifocus/OF-API.html#Speech.Voice) or `null`

The voice to use for this utterance, or `null` in which case the default voice will be used.

### `var volume` → `Number`

A value between 0.0 and 1.0 controller the volume of the utterance.

# `Speech.Voice`

## Class Functions

### `function withLanguage(code: String or null)` → [`Speech.Voice`](https://omni-automation.com/omnifocus/OF-API.html#Speech.Voice) or `null`

Returns a voice for the given BCP–47 language code (such as `en-US` or `fr-CA`), or the default voice if passed `null`. Returns `null` for an invalid langauge code.

### `function withIdentifier(identifier: String)` → [`Speech.Voice`](https://omni-automation.com/omnifocus/OF-API.html#Speech.Voice) or `null`

Returns the voice with the given identifier, or `null` if not found.

## Class Properties

### `var allVoices` → `Array` of [`Speech.Voice`](https://omni-automation.com/omnifocus/OF-API.html#Speech.Voice) *read-only*

### `var currentLanguageCode` → `String` *read-only*

## Instance Properties

### `var gender` → [`Speech.Voice.Gender`](https://omni-automation.com/omnifocus/OF-API.html#Speech.Voice.Gender) *read-only*

### `var identifier` → `String` *read-only*

### `var language` → `String` *read-only*

### `var name` → `String` *read-only*

# `Speech.Voice.Gender`

## Class Properties

### `var Female` → [`Speech.Voice.Gender`](https://omni-automation.com/omnifocus/OF-API.html#Speech.Voice.Gender) *read-only*

### `var Male` → [`Speech.Voice.Gender`](https://omni-automation.com/omnifocus/OF-API.html#Speech.Voice.Gender) *read-only*

### `var Unspecified` → [`Speech.Voice.Gender`](https://omni-automation.com/omnifocus/OF-API.html#Speech.Voice.Gender) *read-only*

### `var all` → `Array` of [`Speech.Voice.Gender`](https://omni-automation.com/omnifocus/OF-API.html#Speech.Voice.Gender) *read-only*

# `StringEncoding`

## Class Properties

### `var ASCII` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var ISO2022JP` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var ISOLatin1` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var ISOLatin2` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var JapaneseEUC` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var MacOSRoman` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var NextStep` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var NonLossyASCII` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var ShiftJIS` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var Symbol` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var UTF16` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var UTF16BigEndian` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var UTF16LittleEndian` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var UTF32` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var UTF32BigEndian` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var UTF32LittleEndian` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var UTF8` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var Unicode` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var WindowsCP1250` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var WindowsCP1251` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var WindowsCP1252` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var WindowsCP1253` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var WindowsCP1254` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var all` → `Array` of [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

# `Style`

## Instance Functions

### `function set(attribute: [Style.Attribute](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute), value: [Object](https://omni-automation.com/omnifocus/OF-API.html#Object) or null)` → `Boolean`

Sets (or clears) the value for the given style attribute.
Styles that cascade from this one will inherit this value, if they don’t define their own value or have a closer ancestor style that does.
Returns true if a change was actually made, false otherwise. Throws an error if the key does not map to a known attribute, or if the value is of the wrong type for the specified attribute.

### `function get(attribute: [Style.Attribute](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute))` → [`Object`](https://omni-automation.com/omnifocus/OF-API.html#Object) or `null`

Looks up the value for the specified style attribute locally, in the cascading and inherited styles, and finally falling back to the default value for the style attribute.

### `function localValueForAttribute(attribute: [Style.Attribute](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute))` → [`Object`](https://omni-automation.com/omnifocus/OF-API.html#Object) or `null`

Looks up the value for the specified style attribute locally, returning null if it is not set.

### `function addNamedStyle(namedStyle: [NamedStyle](https://omni-automation.com/omnifocus/OF-API.html#NamedStyle))`

Adds the specified `NamedStyle` to the set of named styles to include in this `Style`. If the style is already present, or if this would create a loop (adding two `NamedStyles` to each other’s list of named styles), an error will be thrown.

### `function removeNamedStyle(namedStyle: [NamedStyle](https://omni-automation.com/omnifocus/OF-API.html#NamedStyle))`

Removes the specified `NamedStyle` from the set of named styles to include in this `Style`. If the style is not present, an error will be thrown.

### `function influencedBy(otherStyle: [Style](https://omni-automation.com/omnifocus/OF-API.html#Style))` → `Boolean`

Returns `true` if the receiver is influenced, directly or indirectly from the passed `Style`.

### `function setStyle(style: [Style](https://omni-automation.com/omnifocus/OF-API.html#Style))`

Updates all the attributes and inherited styles on the receiver to be the same as the argument `Style`.

### `function clear()`

Removes all the locally applied style attribute values for this `Style`.

## Instance Properties

### `var fontFillColor` → [`Color`](https://omni-automation.com/omnifocus/OF-API.html#Color)

The color used to fill text. Setting the color to `null` will remove the setting for this style.

### `var link` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null` *read-only*

Returns the `URL` link for a style, or `null` if there is no link applied. Note that `get(Style.Attribute.Link)` on the same style will return the default `URL` with an empty `toString()` value when there is no URL applied. If the style represents a file attachment and there is no specific link attribute set, the `URL` for the file attachment will be returned. If the style represents an file attachment that is embedded in the document, `null` will be returned.

### `var locallyDefinedAttributes` → `Array` of [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

Returns an array of the `Style.Attribute`s defined on this `Style`.

### `var namedStyles` → `Array` of [`NamedStyle`](https://omni-automation.com/omnifocus/OF-API.html#NamedStyle) *read-only*

Returns the `NamedStyle`s that are directly associated with this `Style`. If a style attribute lookup doesn’t find a value in the local style, then the named styles will be searched.

# `NamedStyle` : [`Style`](https://omni-automation.com/omnifocus/OF-API.html#Style)

## Instance Functions

### `function remove()`

Removes the `NamedStyle` from the document. Any references to it are disconnected as well.

## Instance Properties

### `var after` → [`NamedStylePosition`](https://omni-automation.com/omnifocus/OF-API.html#NamedStylePosition) *read-only*

Returns a `NamedStylePosition` that indicates the slot after this item.

### `var before` → [`NamedStylePosition`](https://omni-automation.com/omnifocus/OF-API.html#NamedStylePosition) *read-only*

Returns a `NamedStylePosition` that indicates the slot before this item.

### `var identifier` → `String` *read-only*

A unique identifier for the style, which is suitable for long-lived references.

### `var name` → `String`

The name of the style that is presented in the interface.

# `Style.Attribute`

## Class Properties

### `var BackgroundColor` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var BaselineOffset` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var BaselineSuperscript` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var Expansion` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var FontCondensed` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var FontFamily` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var FontFillColor` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var FontFixedPitch` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var FontItalic` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var FontName` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var FontNarrow` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var FontSize` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var FontStrokeColor` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var FontStrokeWidth` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var FontWeight` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var KerningAdjustment` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var LigatureSelection` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var Link` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var Obliqueness` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ParagraphAlignment` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ParagraphBaseWritingDirection` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ParagraphDefaultTabInterval` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ParagraphFirstLineHeadIndent` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ParagraphHeadIndent` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ParagraphLineHeightMultiple` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ParagraphLineSpacing` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ParagraphMaximumLineHeight` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ParagraphMinimumLineHeight` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ParagraphSpacing` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ParagraphSpacingBefore` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ParagraphTabStops` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ParagraphTailIndent` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ShadowBlurRadius` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ShadowColor` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var ShadowOffset` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var StrikethroughAffinity` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var StrikethroughColor` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var StrikethroughPattern` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var StrikethroughStyle` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var UnderlineAffinity` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var UnderlineColor` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var UnderlinePattern` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

### `var UnderlineStyle` → [`Style.Attribute`](https://omni-automation.com/omnifocus/OF-API.html#Style.Attribute) *read-only*

## Instance Properties

### `var defaultValue` → [`Object`](https://omni-automation.com/omnifocus/OF-API.html#Object) *read-only*

Returns the default value that will be used when a style has no local value for this attribute, nor do any of its associated styles.

### `var key` → `String` *read-only*

Returns the string used to identify this attribute when calling `get` or `set` on a `Style` instance.

# `Tag.ChildInsertionLocation`

A location specified relative to an existing `Tag` or `Database`. These cannot be instantiated directly, rather they are returned from properties like `Tag.before` or `Database.beginning`.

# `Tag.Status`

## Class Properties

### `var Active` → [`Tag.Status`](https://omni-automation.com/omnifocus/OF-API.html#Tag.Status) *read-only*

The tag is active.

### `var Dropped` → [`Tag.Status`](https://omni-automation.com/omnifocus/OF-API.html#Tag.Status) *read-only*

The tag has been dropped.

### `var OnHold` → [`Tag.Status`](https://omni-automation.com/omnifocus/OF-API.html#Tag.Status) *read-only*

The tag has been put on-hold.

### `var all` → `Array` of [`Tag.Status`](https://omni-automation.com/omnifocus/OF-API.html#Tag.Status) *read-only*

# `Task.ChildInsertionLocation`

A location specified relative to an existing `Task` or `Database`. These cannot be instantiated directly, rather they are returned from properties like `Task.before`, `Inbox.ending`, or `Project.beginning`. (For a complete list of locations, open the navigation sidebar and use its filter to search for `Task.ChildInsertionLocation`.)

# `Task.Notification.Kind`

## Class Properties

### `var Absolute` → [`Task.Notification.Kind`](https://omni-automation.com/omnifocus/OF-API.html#Task.Notification.Kind) *read-only*

This notification fires on a given date, regardless of its task’s due and defer dates.

### `var DueRelative` → [`Task.Notification.Kind`](https://omni-automation.com/omnifocus/OF-API.html#Task.Notification.Kind) *read-only*

This notification fires at a time relative to its task’s due date.

### `var Unknown` → [`Task.Notification.Kind`](https://omni-automation.com/omnifocus/OF-API.html#Task.Notification.Kind) *read-only*

It is not known what this notification’s fire date is relative to.

### `var all` → `Array` of [`Task.Notification.Kind`](https://omni-automation.com/omnifocus/OF-API.html#Task.Notification.Kind) *read-only*

# `Task.RepetitionMethod`

## Class Properties

### `var DeferUntilDate` → [`Task.RepetitionMethod`](https://omni-automation.com/omnifocus/OF-API.html#Task.RepetitionMethod) *read-only*

### `var DueDate` → [`Task.RepetitionMethod`](https://omni-automation.com/omnifocus/OF-API.html#Task.RepetitionMethod) *read-only*

### `var Fixed` → [`Task.RepetitionMethod`](https://omni-automation.com/omnifocus/OF-API.html#Task.RepetitionMethod) *read-only*

### `var None` → [`Task.RepetitionMethod`](https://omni-automation.com/omnifocus/OF-API.html#Task.RepetitionMethod) *read-only*

The task does not repeat.

### `var all` → `Array` of [`Task.RepetitionMethod`](https://omni-automation.com/omnifocus/OF-API.html#Task.RepetitionMethod) *read-only*

# `Task.RepetitionRule`

A `Task.RepetitionRule` describes a pattern of dates using a ICS formatted recurrence string and a `Task.RepetitionMethod` to describe how those dates are applied to a `Task`.

## Constructors

### `new Task.RepetitionRule(ruleString: String, method: [Task.RepetitionMethod](https://omni-automation.com/omnifocus/OF-API.html#Task.RepetitionMethod))` → [`Task.RepetitionRule`](https://omni-automation.com/omnifocus/OF-API.html#Task.RepetitionRule)

Returns a new `Task.RepetitionRule` with the specified ICS rule string and repetition method. The the rule string is not valid, or combination with the method is not supported, an error will be thrown.

## Instance Functions

### `function firstDateAfterDate(date: Date)` → `Date`

Returns the first date described by the repetition rule that is after the given date.

## Instance Properties

### `var method` → [`Task.RepetitionMethod`](https://omni-automation.com/omnifocus/OF-API.html#Task.RepetitionMethod) *read-only*

The method used to create the repetition rule.

### `var ruleString` → `String` *read-only*

The ICS rule string used to create the repetition rule.

# `Task.Status`

## Class Properties

### `var Available` → [`Task.Status`](https://omni-automation.com/omnifocus/OF-API.html#Task.Status) *read-only*

The task is available to work on.

### `var Blocked` → [`Task.Status`](https://omni-automation.com/omnifocus/OF-API.html#Task.Status) *read-only*

The task is not available to work on currently, due to a future defer date, a preceeding task in a sequential project, or having an on-hold tag associated.

### `var Completed` → [`Task.Status`](https://omni-automation.com/omnifocus/OF-API.html#Task.Status) *read-only*

The task is already completed.

### `var Dropped` → [`Task.Status`](https://omni-automation.com/omnifocus/OF-API.html#Task.Status) *read-only*

The task will not be worked on.

### `var DueSoon` → [`Task.Status`](https://omni-automation.com/omnifocus/OF-API.html#Task.Status) *read-only*

The task is incomplete and due soon.

### `var Next` → [`Task.Status`](https://omni-automation.com/omnifocus/OF-API.html#Task.Status) *read-only*

The task is the first available task in a project.

### `var Overdue` → [`Task.Status`](https://omni-automation.com/omnifocus/OF-API.html#Task.Status) *read-only*

The task is incomplete overdue.

### `var all` → `Array` of [`Task.Status`](https://omni-automation.com/omnifocus/OF-API.html#Task.Status) *read-only*

# `Text`

## Class Functions

### `function makeFileAttachment(fileWrapper: [FileWrapper](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper), style: [Style](https://omni-automation.com/omnifocus/OF-API.html#Style))` → [`Text`](https://omni-automation.com/omnifocus/OF-API.html#Text)

Returns a new `Text` instance that represents a file attachment. The attachment has a single character string content with a special value.

## Constructors

### `new Text(string: String, style: [Style](https://omni-automation.com/omnifocus/OF-API.html#Style))` → [`Text`](https://omni-automation.com/omnifocus/OF-API.html#Text)

Returns a new `Text` instance with the given string contents and `Style` applied to the entire range of text.

## Instance Functions

### `function textInRange(range: [Text.Range](https://omni-automation.com/omnifocus/OF-API.html#Text.Range))` → [`Text`](https://omni-automation.com/omnifocus/OF-API.html#Text)

Returns a copy of the text in the specified range.

### `function styleForRange(range: [Text.Range](https://omni-automation.com/omnifocus/OF-API.html#Text.Range))` → [`Style`](https://omni-automation.com/omnifocus/OF-API.html#Style)

Returns a `Style` instance for the given range of the `Text`.

### `function ranges(component: [TextComponent](https://omni-automation.com/omnifocus/OF-API.html#TextComponent), useEnclosingRange: Boolean or null)` → `Array` of [`Text.Range`](https://omni-automation.com/omnifocus/OF-API.html#Text.Range)

Returns an array of `TextRange`s for the specified component. If `useEnclosingRange` is `true`, than any extra characters that separate follow a component will be included in its range. Any extra characters before the first found component will be included in the first range.

### `function replace(range: [Text.Range](https://omni-automation.com/omnifocus/OF-API.html#Text.Range), with: [Text](https://omni-automation.com/omnifocus/OF-API.html#Text))`

Replaces the sub-range of the receiving `Text` with a copy of the passed in `Text` (which remains unchanged).

### `function append(text: [Text](https://omni-automation.com/omnifocus/OF-API.html#Text))`

Appends the given `Text` to the receiver.

### `function insert(position: [Text.Position](https://omni-automation.com/omnifocus/OF-API.html#Text.Position), text: [Text](https://omni-automation.com/omnifocus/OF-API.html#Text))`

Inserts a copy of the given `Text` at the specified position in the receiver.

### `function remove(range: [Text.Range](https://omni-automation.com/omnifocus/OF-API.html#Text.Range))`

Removes the indicated sub-range of the receiving `Text`.

### `function find(string: String, options: Array of [Text.FindOption](https://omni-automation.com/omnifocus/OF-API.html#Text.FindOption) or null, range: [Text.Range](https://omni-automation.com/omnifocus/OF-API.html#Text.Range) or null)` → [`Text.Range`](https://omni-automation.com/omnifocus/OF-API.html#Text.Range) or `null`

Finds an occurrence of `string` within the `Text` and returns the enclosing `Text.Range` if there is a match. If `range` is passed, only that portion of the `Text` is searched. The supplied `options`, if any, change how the search is performed based on their definition.

## Instance Properties

### `var attachments` → `Array` of [`Text`](https://omni-automation.com/omnifocus/OF-API.html#Text) *read-only*

Returns an array of copies of the blocks of `Text` in the receiver that represent `Attachment`s. Note that editing these instances will not change the original.

### `var attributeRuns` → `Array` of [`Text`](https://omni-automation.com/omnifocus/OF-API.html#Text) *read-only*

Returns an array of copies of the contiguous blocks of `Text` in the receiver that have the same style. Note that editing these instances will not change the original.

### `var characters` → `Array` of [`Text`](https://omni-automation.com/omnifocus/OF-API.html#Text) *read-only*

Returns an array of copies of the characters in the `Text`. Note that editing these instances will not change the original.

### `var end` → [`Text.Position`](https://omni-automation.com/omnifocus/OF-API.html#Text.Position) *read-only*

Returns a `Text.Position` indicating the end of the `Text`.

### `var fileWrapper` → [`FileWrapper`](https://omni-automation.com/omnifocus/OF-API.html#FileWrapper) or `null` *read-only*

Returns the attached file wrapper for the `Text` (or rather, the first character of the text), if any.

### `var paragraphs` → `Array` of [`Text`](https://omni-automation.com/omnifocus/OF-API.html#Text) *read-only*

Returns an array of copies of the paragraphs in the `Text`. Note that editing these instances will not change the original. Paragraphs, if ended by a newline, will contain the newline character.

### `var range` → [`Text.Range`](https://omni-automation.com/omnifocus/OF-API.html#Text.Range) *read-only*

Returns a `Text.Range` that spans the entire `Text`.

### `var sentences` → `Array` of [`Text`](https://omni-automation.com/omnifocus/OF-API.html#Text) *read-only*

Returns an array of copies of the sentences in the `Text`. Note that editing these instances will not change the original.

### `var start` → [`Text.Position`](https://omni-automation.com/omnifocus/OF-API.html#Text.Position) *read-only*

Returns a `Text.Position` indicating the beginning of the `Text`.

### `var string` → `String`

Returns a plain `String` version of the characters in the `Text`. Note that since JavaScript represents Strings as Unicode code points, the length of the returned string may be differnt from the number of `characters` in the `Text` object.

### `var style` → [`Style`](https://omni-automation.com/omnifocus/OF-API.html#Style) *read-only*

Returns a `Style` instance for this `Text` object.

### `var words` → `Array` of [`Text`](https://omni-automation.com/omnifocus/OF-API.html#Text) *read-only*

Returns an array of copies of the words in the `Text`. Note that editing these instances will not change the original.

# `Text.FindOption`

## Class Properties

### `var Anchored` → [`Text.FindOption`](https://omni-automation.com/omnifocus/OF-API.html#Text.FindOption) *read-only*

Matches must be anchored to the beginning (or end if Backwards is include) of the string or search range.

### `var Backwards` → [`Text.FindOption`](https://omni-automation.com/omnifocus/OF-API.html#Text.FindOption) *read-only*

Search starting from the end of the string or range.

### `var CaseInsensitive` → [`Text.FindOption`](https://omni-automation.com/omnifocus/OF-API.html#Text.FindOption) *read-only*

Compare upper and lower case characters as equal.

### `var DiacriticInsensitive` → [`Text.FindOption`](https://omni-automation.com/omnifocus/OF-API.html#Text.FindOption) *read-only*

Ignore diacritics. For example, “ö” is considered the same as “o”.

### `var ForcedOrdering` → [`Text.FindOption`](https://omni-automation.com/omnifocus/OF-API.html#Text.FindOption) *read-only*

Force an ordering between strings that aren’t strictly equal.

### `var Literal` → [`Text.FindOption`](https://omni-automation.com/omnifocus/OF-API.html#Text.FindOption) *read-only*

Perform exact character-by-character matching.

### `var Numeric` → [`Text.FindOption`](https://omni-automation.com/omnifocus/OF-API.html#Text.FindOption) *read-only*

Order numbers by numeric value, not lexigraphically. Only applies to ordered comparisons, not find operations.

### `var RegularExpression` → [`Text.FindOption`](https://omni-automation.com/omnifocus/OF-API.html#Text.FindOption) *read-only*

For find operations, the string is treated as an ICU-compatible regular expression. If set, no other options can be used except for `CaseInsensitive` and `Anchored`.

### `var WidthInsensitive` → [`Text.FindOption`](https://omni-automation.com/omnifocus/OF-API.html#Text.FindOption) *read-only*

Ignore width differences. For example, “a” is considered the same as ‘FULLWIDTH LATIN SMALL LETTER A’ (U+FF41).

### `var all` → `Array` of [`Text.FindOption`](https://omni-automation.com/omnifocus/OF-API.html#Text.FindOption) *read-only*

# `Text.Position`

# `Text.Range`

## Constructors

### `new Text.Range(start: [Text.Position](https://omni-automation.com/omnifocus/OF-API.html#Text.Position), end: [Text.Position](https://omni-automation.com/omnifocus/OF-API.html#Text.Position))` → [`Text.Range`](https://omni-automation.com/omnifocus/OF-API.html#Text.Range)

## Instance Properties

### `var end` → [`Text.Position`](https://omni-automation.com/omnifocus/OF-API.html#Text.Position) *read-only*

Returns the `Text.Position` for the end of the `Text.Range`

### `var isEmpty` → `Boolean` *read-only*

Returns `true` if the `Text.Range` contains no characters.

### `var start` → [`Text.Position`](https://omni-automation.com/omnifocus/OF-API.html#Text.Position) *read-only*

Returns the `Text.Position` for the beginning of the `Text.Range`

# `TextAlignment`

## Class Properties

### `var Center` → [`TextAlignment`](https://omni-automation.com/omnifocus/OF-API.html#TextAlignment) *read-only*

Visually center aligned.

### `var Justified` → [`TextAlignment`](https://omni-automation.com/omnifocus/OF-API.html#TextAlignment) *read-only*

Fully-justified.

### `var Left` → [`TextAlignment`](https://omni-automation.com/omnifocus/OF-API.html#TextAlignment) *read-only*

Visually left aligned.

### `var Natural` → [`TextAlignment`](https://omni-automation.com/omnifocus/OF-API.html#TextAlignment) *read-only*

Use the default alignment based on the characters in the text.

### `var Right` → [`TextAlignment`](https://omni-automation.com/omnifocus/OF-API.html#TextAlignment) *read-only*

Visually right aligned.

### `var all` → `Array` of [`TextAlignment`](https://omni-automation.com/omnifocus/OF-API.html#TextAlignment) *read-only*

# `TextComponent`

## Class Properties

### `var Attachments` → [`TextComponent`](https://omni-automation.com/omnifocus/OF-API.html#TextComponent) *read-only*

The ranges of Text which represent Attachments.

### `var AttributeRuns` → [`TextComponent`](https://omni-automation.com/omnifocus/OF-API.html#TextComponent) *read-only*

The ranges of Text which have the same attributes.

### `var Characters` → [`TextComponent`](https://omni-automation.com/omnifocus/OF-API.html#TextComponent) *read-only*

The individual characters of the Text. Note that some characters (like emoji) consist of multiple Unicode code points.

### `var Paragraphs` → [`TextComponent`](https://omni-automation.com/omnifocus/OF-API.html#TextComponent) *read-only*

The paragraphs of Text. Unlike other options, the line breaking characters that end the paragraph are included.

### `var Sentences` → [`TextComponent`](https://omni-automation.com/omnifocus/OF-API.html#TextComponent) *read-only*

The sentences of the Text.

### `var Words` → [`TextComponent`](https://omni-automation.com/omnifocus/OF-API.html#TextComponent) *read-only*

The words in the Text. Whitespace or other word break characters are not included.

### `var all` → `Array` of [`TextComponent`](https://omni-automation.com/omnifocus/OF-API.html#TextComponent) *read-only*

# `TimeZone`

## Class Properties

### `var abbreviations` → `Array` of `String` *read-only*

The list of known time zone abbreviations.

## Constructors

### `new TimeZone(abbreviation: String)` → [`TimeZone`](https://omni-automation.com/omnifocus/OF-API.html#TimeZone) or `null`

Make a new `TimeZone` with the given abbreviation. Note that the returned `TimeZone` may have a different abbreviation than the passed argument. For example, if one of “PST” or “PDT” is requested that doens’t match the current use of daylight savings time, the one that does match will be returned.

## Instance Properties

### `var abbreviation` → `String` or `null` *read-only*

The abbreviation for the `TimeZone`.

### `var daylightSavingTime` → `Boolean` *read-only*

Returns `true` if the `TimeZone` is currently using daylight savings time.

### `var secondsFromGMT` → `Number` *read-only*

The current difference in seconds between this `TimeZone` and GMT.

# `Timer`

## Class Functions

### `function once(interval: Number, action: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function)(‍timer: [Timer](https://omni-automation.com/omnifocus/OF-API.html#Timer)‍))` → [`Timer`](https://omni-automation.com/omnifocus/OF-API.html#Timer)

Makes a new `Timer` that will fire once, after the specified interval (in seconds from the current time). When the `Timer` fires, the passed in `Function` is called, passing the `Timer` as its argument.

### `function repeating(interval: Number, action: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function)(‍timer: [Timer](https://omni-automation.com/omnifocus/OF-API.html#Timer)‍))` → [`Timer`](https://omni-automation.com/omnifocus/OF-API.html#Timer)

Makes a new `Timer` that will fire repeatedly with the specified interval (in seconds, with the first invocation happening that interval after the current time). When the `Timer` fires, the passed in `Function` is called, passing the `Timer` as its argument.

## Instance Functions

### `function cancel()`

## Instance Properties

### `var interval` → `Number` *read-only*

# `ToolbarItem`

## Instance Properties

### `var image` → [`Image`](https://omni-automation.com/omnifocus/OF-API.html#Image) or `null`

### `var label` → `String`

### `var toolTip` → `String` or `null`

# `Tree`

## Instance Functions

### `function nodeForObject(object: [Object](https://omni-automation.com/omnifocus/OF-API.html#Object))` → [`TreeNode`](https://omni-automation.com/omnifocus/OF-API.html#TreeNode) or `null`

Returns the `TreeNode` that represents the `object` in this `Tree`, or `null` if it cannot be found (possibly filtered out).

### `function nodesForObjects(object: Array of [Object](https://omni-automation.com/omnifocus/OF-API.html#Object))` → `Array` of [`TreeNode`](https://omni-automation.com/omnifocus/OF-API.html#TreeNode)

Returns an array of `TreeNode`s for the objects that are currently in the `Tree`, according to the same filters as `nodeForObject()`. The size of the resulting node array may be smaller (even empty) than the passed in `objects` array.

### `function reveal(nodes: Array of [TreeNode](https://omni-automation.com/omnifocus/OF-API.html#TreeNode))`

Ensures the ancestor nodes of all the specified nodes are expanded.

### `function select(nodes: Array of [TreeNode](https://omni-automation.com/omnifocus/OF-API.html#TreeNode), extending: Boolean or null)`

Selects the specified `TreeNode`s that are visible (nodes with collapsed ancestors cannot be selected). If `extending` is `true`, the existing selection is not cleared.

### `function copyNodes(nodes: Array of [TreeNode](https://omni-automation.com/omnifocus/OF-API.html#TreeNode), to: [Pasteboard](https://omni-automation.com/omnifocus/OF-API.html#Pasteboard))`

Writes a serialized version of the `nodes` to the specified pasteboard.

### `function paste(from: [Pasteboard](https://omni-automation.com/omnifocus/OF-API.html#Pasteboard), parentNode: [TreeNode](https://omni-automation.com/omnifocus/OF-API.html#TreeNode) or null, childIndex: Number or null)`

Attempts to read a serialized version of nodes from the pasteboard and create new items at the specified location in the receiver. If a parent node is not specified, then the root node of the receiver is assumed. If a `childIndex` is not specified, any new children are placed at the end of the parent’s existing children.

## Instance Properties

### `var rootNode` → [`TreeNode`](https://omni-automation.com/omnifocus/OF-API.html#TreeNode) *read-only*

Returns the `rootNode` of the `Editor`.

### `var selectedNodes` → `Array` of [`TreeNode`](https://omni-automation.com/omnifocus/OF-API.html#TreeNode) *read-only*

Returns the list of selected `TreeNode`s, in the order they appear in the tree.

# `ContentTree` : [`Tree`](https://omni-automation.com/omnifocus/OF-API.html#Tree)

# `SidebarTree` : [`Tree`](https://omni-automation.com/omnifocus/OF-API.html#Tree)

# `TreeNode`

## Instance Functions

### `function childAtIndex(childIndex: Number)` → [`TreeNode`](https://omni-automation.com/omnifocus/OF-API.html#TreeNode)

Returns the child node at the given index.

### `function expand(completely: Boolean or null)`

Attempts to expand the `TreeNode`. If `completely` is passed, all the child nodes will be expanded as they allow.

### `function collapse(completely: Boolean or null)`

Attempts to collapse the `TreeNode`. If `completely` is passed, all the child nodes will be collapse as they allow.

### `function expandNote(completely: Boolean or null)`

Attempts to expand the inline note of the `TreeNode`. If `completely` is passed, all the child node notes will be expanded.

### `function collapseNote(completely: Boolean or null)`

Attempts to collapse the inline note of the `TreeNode`. If `completely` is passed, all the child node notes will be collapsed.

### `function reveal()`

Expands all the

### `function apply(function: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function)(‍node: [TreeNode](https://omni-automation.com/omnifocus/OF-API.html#TreeNode)‍) → [_omnijs_AnonymousProxy](https://omni-automation.com/omnifocus/OF-API.html#_omnijs_AnonymousProxy) or null)` → [`_omnijs_AnonymousProxy`](https://omni-automation.com/omnifocus/OF-API.html#_omnijs_AnonymousProxy) or `null`

Calls the supplied function for each `TreeNode` in the receiver (including the receiver), passing that node as the single argument. The supplied function can optionally return a `ApplyResult` to skip enumeration of some elements.

## Instance Properties

### `var canCollapse` → `Boolean` *read-only*

Returns `true` if this `TreeNode` can be collapsed.

### `var canExpand` → `Boolean` *read-only*

Returns `true` if this `TreeNode` can be expanded.

### `var childCount` → `Number` *read-only*

Returns the number of children directly under this node.

### `var children` → `Array` of [`TreeNode`](https://omni-automation.com/omnifocus/OF-API.html#TreeNode) *read-only*

Returns the array of children that are visible under this node, according to any filtering that is being done, and in the order specified by any sorting rules that have been established.

### `var index` → `Number` *read-only*

Returns the index of this `TreeNode` among its siblings, or zero for the `rootNode`.

### `var isExpanded` → `Boolean` *read-only*

Returns `true` if this `TreeNode` is currently expanded.

### `var isNoteExpanded` → `Boolean` *read-only*

Returns `true` if the note of this `TreeNode` is currently expanded.

### `var isRevealed` → `Boolean` *read-only*

Returns `true` if the `TreeNode` is the `rootNode` or all of its ancestor nodes are expanded.

### `var isRootNode` → `Boolean` *read-only*

Returns `true` if this node is the `rootNode` of its tree.

### `var isSelectable` → `Boolean` *read-only*

Returns `true` if this `TreeNode` can be selected. The `rootNode` cannot be selected, nor can nodes that aren’t revealed.

### `var isSelected` → `Boolean`

Set to `true` if this `TreeNode` is in the list of selected nodes for its tree. Attempting to set this to `true` will do nothing if the node is not revealed (or is the root node).

### `var level` → `Number` *read-only*

Returns the nesting level of the `TreeNode`, relative to the root of the tree. The `rootNode` of an `Outline` has level zero, its children have level one, and so on. Note that if only a portion of the model is being shown, this level may not match the level of the underlying `object`.

### `var object` → [`Object`](https://omni-automation.com/omnifocus/OF-API.html#Object) *read-only*

The model object which this node wraps.

### `var parent` → [`TreeNode`](https://omni-automation.com/omnifocus/OF-API.html#TreeNode) or `null` *read-only*

Returns the `TreeNode` that contains this node, or `null` if this is the `rootNode`.

### `var rootNode` → [`TreeNode`](https://omni-automation.com/omnifocus/OF-API.html#TreeNode) *read-only*

Returns the root `TreeNode` for the tree that this node belongs to.

# `TypeIdentifier`

## Class Functions

### `function fromPathExtension(pathExtension: String, isDirectory: Boolean)` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier)

Return a `TypeIdentifier` that matches items that have the given path extension and are (or are not) directories.

## Class Properties

### `var URL` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The URL type.

### `var binaryPropertyList` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The binary property list type.

### `var csv` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The comma-separated text type.

### `var editableTypes` → `Array` of [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The list of `TypeIdentifier`s that can be read and written natively by documents in this application.

### `var gif` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The GIF image type.

### `var image` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

A generic type that all image types conform to.

### `var jpeg` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The JPEG image type.

### `var json` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The JSON type.

### `var ofocus` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The OmniFocus document file type.

### `var pdf` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The PDF type.

### `var plainText` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The plain text type.

### `var png` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The PNG image type.

### `var propertyList` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The generic property list type.

### `var readableTypes` → `Array` of [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The list of `TypeIdentifier`s that can be read by documents in this this application.

### `var rtf` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The RTF type.

### `var rtfd` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The RTFD type.

### `var taskPaper` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The TaskPaper-formatted tasks pasteboard type.

### `var tasks` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The pasteboard type identifier for OmniFocus tasks and projects.

### `var tasksAndFolders` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The pasteboard type identifier for OmniFocus tasks, projects, and folders.

### `var tiff` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The TIFF image type.

### `var writableTypes` → `Array` of [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The list of `TypeIdentifier`s that can be written by documents in this application (though some documents may be exportable only in a subset of these types).

### `var xmlPropertyList` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier) *read-only*

The XML property list type.

## Constructors

### `new TypeIdentifier(identifier: String)` → [`TypeIdentifier`](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier)

Returns a new `TypeIdentifier` with the given identifier.

## Instance Functions

### `function conformsTo(other: [TypeIdentifier](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier))` → `Boolean`

Returns `true` if the instance is the same as the given argument or a more specific type. For example, `TypeIdentifier.png.conformsTo(TypeIdentifier.image)` will be `true`, but `TypeIdentifier.png.conformsTo(TypeIdentifier.plainText)` will be `false`.

## Instance Properties

### `var displayName` → `String` *read-only*

Returns a human-readable description of the type.

### `var identifier` → `String` *read-only*

Returns a unique string for a type identifier, suitable for archiving or encoding in scripts.

### `var pathExtensions` → `Array` of `String` *read-only*

The list of filesystem path extensions used by this type.

# `URL`

## Class Functions

### `function choose(types: Array of String)` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null`

Deprecated: Please use `FilePicker` instead.

Allows the user to choose a file `URL` if possible and returns a new instance, or `null` otherwise.

### `function chooseFolder()` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null`

Deprecated: Please use `FilePicker` instead.

Allows the user to choose a folder `URL` if possible and returns a new instance, or `null` otherwise.

### `function fromString(string: String, relativeToURL: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL) or null)` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null`

Parses the string as a `URL` if possible and returns a new instance, or `null` if the string is not a valid URL. If `baseURL` is not `null`, the result is a relative URL.

### `function fromPath(path: String, isDirectory: Boolean, relativeToURL: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL) or null)` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL)

Returns a new `file` URL with the given path and assumption of whether it is a directory.

### `function tellScript(app: String, js: String, arg: [Object](https://omni-automation.com/omnifocus/OF-API.html#Object) or null)` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null`

Creates a `URL` to invoke the given JS on the given app (url scheme) appropriate for use with the call function.

### `function tellFunction(app: String, jsFunction: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function), arg: [Object](https://omni-automation.com/omnifocus/OF-API.html#Object) or null)` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null`

Creates a `URL` to invoke the given JS function on the given app (url scheme) appropriate for use with the call function.

## Class Properties

### `var currentAppScheme` → `String` *read-only*

Returns the URL scheme for the current app.

### `var documentsDirectory` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) *read-only*

Returns the application’s Documents directory. This is in the application’s sandbox, and on the Mac is **not** the user’s Documents directory. This is accessible by the application without using `access()`.

## Instance Functions

### `function fetch(success: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function)(‍contents: [Data](https://omni-automation.com/omnifocus/OF-API.html#Data)‍), failure: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function)(‍error: [Error](https://omni-automation.com/omnifocus/OF-API.html#Error)‍) or null)`

Get the contents at the destination of this URL.

### `function call(success: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function), failure: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function) or null)`

Invoke an [x-callback-url API](http://x-callback-url.com/) end-point, with the callback functions being invoked when a reply is received. When a reply is received, the parameters of that URL are decoded as JSON, or left as String values if not valid JSON, and stored as properties of a result object. For a successful reply, if the result object has one property, its value is passed as the first argument to the success function. If there are zero or more than one parameters, the full object is passed as the first argument. In both cases, the success function is passed a second argument that is the full object of parameters. The failure callback is always passed the object will all the result parameters, typically `errorCode` and `errorMessage`.

### `function open()`

Ask the system to open this URL.

### `function find(types: Array of [TypeIdentifier](https://omni-automation.com/omnifocus/OF-API.html#TypeIdentifier), recurse: Boolean or null)` → [`Promise`](https://omni-automation.com/omnifocus/OF-API.html#Promise) of `Array` of [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL)

Scan a directory `URL` for files of the given types. If `recurse` is specified and is false, only the immediate contents of the directory will be considered. If `recurse` is not specified or is `true`, the full directory tree will be scanned.

### `function toString()` → `String`

### `function appendingPathComponent(component: String)` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL)

Return a new `URL` with the given string added at the end of the path.

### `function appendingPathExtension(pathExtension: String)` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL)

Returns a new `URL` with the last path component having the given path extension added, including a separating “.”

### `function deletingPathExtension()` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL)

Returns a new `URL` with the path extension (if any) of the last path component removed.

### `function deletingLastPathComponent()` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL)

Returns a new `URL` with the last path component removed.

## Instance Properties

### `var absoluteString` → `String` *read-only*

Returns the absolute string for the `URL`.

### `var absoluteURL` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) *read-only*

Returns the absolute `URL`.

### `var baseURL` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null` *read-only*

Returns the base `URL` if this `URL` is relative, or `null` if it is absolute.

### `var fragment` → `String` or `null` *read-only*

Returns the fragment component of the `URL`, or `null`.

### `var hasDirectoryPath` → `Boolean` *read-only*

Returns `true` if the `URL`’s path represents a directory.

### `var host` → `String` or `null` *read-only*

Returns the host component of the `URL` or `null`.

### `var isFileURL` → `Boolean` *read-only*

Returns `true` if the scheme is `file:`.

### `var lastPathComponent` → `String` *read-only*

Returns the last component of the `URL`’s path or an empty string.

### `var password` → `String` or `null` *read-only*

Returns the password component of the `URL` or `null`.

### `var path` → `String` or `null` *read-only*

Returns the path component of the `URL` or `null`.

### `var pathComponents` → `Array` of `String` *read-only*

Returns the path of the `URL` as an array of components.

### `var pathExtension` → `String` *read-only*

Returns the path extension of the last path component of the `URL` or the empty string.

### `var port` → `Number` or `null` *read-only*

Returns the port component of the `URL` or `null`.

### `var query` → `String` or `null` *read-only*

Returns the query component of the `URL` or `null`.

### `var relativePath` → `String` or `null` *read-only*

Returns the relative path of the URL, or the absolute path if this `URL` is not relative

### `var relativeString` → `String` *read-only*

Returns the relative portion of the `URL` if it is relative, otherwise this returns the absolute string.

### `var scheme` → `String` or `null` *read-only*

Returns the scheme of the `URL`.

### `var string` → `String` *read-only*

String absoluteString representation of this URL.

### `var user` → `String` or `null` *read-only*

Returns the user component of the `URL` or `null`.

# `URL.Access`

A `URL.Access` holds the temporary access given by the `URL.Bookmark.access()` function. These should not be stored longer than needed.

## Instance Properties

### `var url` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) *read-only*

The `URL` being accessed.

# `URL.Bookmark`

A `URL.Bookmark` records the permission to access a given `URL` and will restore that permission, as well as a possibly renamed file, at a later point.

## Class Functions

### `function fromURL(url: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL))` → [`URL.Bookmark`](https://omni-automation.com/omnifocus/OF-API.html#URL.Bookmark)

Creates a `URL.Bookmark` from an existing `URL`, which should have been returned by `FilePicker`. This can then be stored in a `Credentials` object to persistently record the permission to access this `URL`.

## Instance Functions

### `function access()` → [`Promise`](https://omni-automation.com/omnifocus/OF-API.html#Promise) of [`URL.Access`](https://omni-automation.com/omnifocus/OF-API.html#URL.Access)

Attempts to resolve the instance into a `URL` and grant access to it through the returned `Promise`. Access to the `URL` will only last as long as the `URL.Access` object exists (which should not be stored longer than necessary).

# `URL.Components`

`URL.Components` allows for correct generation and interpreation of `URL` instances, dealing with the specific different quoting rules for each specific part of the `URL`.

## Class Functions

### `function fromString(string: String)` → [`URL.Components`](https://omni-automation.com/omnifocus/OF-API.html#URL.Components) or `null`

Parses the string into `URL.Components`. If the string is not a valid URL, `null` is returned.

### `function fromURL(url: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL), resolvingAgainstBaseURL: Boolean)` → [`URL.Components`](https://omni-automation.com/omnifocus/OF-API.html#URL.Components) or `null`

Parses the string represenation of the `URL`. If the `URL`’s string is malformed, `null` is returned. If `resolve` is `true` and the given `url` is relative, its base URL’s components are considered.

## Constructors

### `new URL.Components()` → [`URL.Components`](https://omni-automation.com/omnifocus/OF-API.html#URL.Components)

Returns a new `URL.Components`.

## Instance Functions

### `function urlRelativeTo(base: [URL](https://omni-automation.com/omnifocus/OF-API.html#URL) or null)` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null`

Returns a `URL` relative to the base URL and the components or `null`.

## Instance Properties

### `var fragment` → `String` or `null`

### `var host` → `String` or `null`

### `var password` → `String` or `null`

### `var path` → `String`

### `var port` → `Number` or `null`

### `var query` → `String` or `null`

### `var queryItems` → `Array` of [`URL.QueryItem`](https://omni-automation.com/omnifocus/OF-API.html#URL.QueryItem) or `null`

The query of the `URL.Components` as individual components.

### `var scheme` → `String` or `null`

### `var url` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null` *read-only*

Returns a `URL` for the components or `null`.

### `var user` → `String` or `null`

# `URL.FetchRequest`

## Class Functions

### `function fromString(string: String)` → [`URL.FetchRequest`](https://omni-automation.com/omnifocus/OF-API.html#URL.FetchRequest) or `null`

Parses the string as a `URL` if possible and returns a new instance, or `null` otherwise.

## Constructors

### `new URL.FetchRequest()` → [`URL.FetchRequest`](https://omni-automation.com/omnifocus/OF-API.html#URL.FetchRequest)

Creates a new instance.

## Instance Functions

### `function fetch()` → [`Promise`](https://omni-automation.com/omnifocus/OF-API.html#Promise) of [`URL.FetchResponse`](https://omni-automation.com/omnifocus/OF-API.html#URL.FetchResponse)

Perform the request, returning a `Promise`. On success, the promise will resolve to a `URL.FetchResponse`.

## Instance Properties

### `var allowsConstrainedNetworkAccess` → `Boolean`

Whether connections may use the network when the user has specified Low Data Mode.

### `var allowsExpensiveNetworkAccess` → `Boolean`

Whether connections may use a network interface that the system considers expensive.

### `var bodyData` → [`Data`](https://omni-automation.com/omnifocus/OF-API.html#Data) or `null`

The body of the request, typically used in an HTTP `POST` or `PUT` request. This API is suitable for uploading binary data, or for text which needs to be encoded in a form other than UTF–8. If UTF–8 text is suitable, `bodyString` is likely to be a better choice.

### `var bodyString` → `String` or `null`

The body of the request, typically used in an HTTP `POST` or `PUT` request. The provided string will be transmitted using the UTF–8 encoding.

### `var cache` → `String` or `null`

The cache policy for the request: `default`, `no-store`, `reload`, `no-cache`, `force-cache`, or `only-if-cached`.

### `var headers` → Object

Custom HTTP headers to be sent with this request.

### `var httpShouldHandleCookies` → `Boolean`

Whether to automatically handle cookies.

### `var httpShouldUsePipelining` → `Boolean`

Whether to transmit data before receiving a response from an earlier request.

### `var method` → `String` or `null`

The HTTP request method of the request: `GET`, `POST`, `PUT`, `DELETE`, etc.

### `var url` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null`

The URL for this fetch request. Much of the additional functionality provided by the fetch request API will only work with `http` and `https` URLs. (For example, the `method` and `cache` and `headers` don’t have any effect in the context of a `file` or `omnifocus` URL.)

# `URL.FetchResponse`

`URL.FetchResponse` represents the response from fetching a URL resource, providing additional information about the response such as the HTTP status code and headers along with the actual data for that response. This is a read-only object returned by performing a `URL.FetchRequest`; see that class for more details on actually performing the request.

## Instance Properties

### `var bodyData` → [`Data`](https://omni-automation.com/omnifocus/OF-API.html#Data) or `null` *read-only*

Returns the raw HTTP body data from this response.

### `var bodyString` → `String` or `null` *read-only*

This is a convenience wrapper which interprets the `bodyData` of this response as UTF–8 text. (Note: the current implementation assumes the text is encoded using UTF–8, but ideally it would honor the text encoding as reported by `textEncodingName`.)

### `var headers` → Object *read-only*

Returns the HTTP header fields for this response.

### `var mimeType` → `String` or `null` *read-only*

Returns the HTTP MIME type for this response (e.g. `text/plain`, `application/json`, etc.).

### `var statusCode` → `Number` *read-only*

Returns the HTTP status code for this response (e.g. `200`, `404`, etc.).

### `var textEncodingName` → `String` or `null` *read-only*

Returns the reported text encoding for this response. This name will be the actual string reported by the origin source, or `null` if no encoding was specified.

### `var url` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null` *read-only*

Returns the URL for this response.

# `URL.QueryItem`

## Constructors

### `new URL.QueryItem(name: String, value: String or null)` → [`URL.QueryItem`](https://omni-automation.com/omnifocus/OF-API.html#URL.QueryItem)

Returns a new `URL.QueryItem` with the given name and value.

## Instance Properties

### `var name` → `String` *read-only*

### `var value` → `String` or `null` *read-only*

# `UnderlineAffinity`

## Class Properties

### `var ByWord` → [`UnderlineAffinity`](https://omni-automation.com/omnifocus/OF-API.html#UnderlineAffinity) *read-only*

Underline only the words, but not the space between them.

### `var None` → [`UnderlineAffinity`](https://omni-automation.com/omnifocus/OF-API.html#UnderlineAffinity) *read-only*

Underline the entire range.

### `var all` → `Array` of [`UnderlineAffinity`](https://omni-automation.com/omnifocus/OF-API.html#UnderlineAffinity) *read-only*

# `UnderlinePattern`

## Class Properties

### `var Dash` → [`UnderlinePattern`](https://omni-automation.com/omnifocus/OF-API.html#UnderlinePattern) *read-only*

Dashed line.

### `var DashDot` → [`UnderlinePattern`](https://omni-automation.com/omnifocus/OF-API.html#UnderlinePattern) *read-only*

Alternating dashes and dots.

### `var DashDotDot` → [`UnderlinePattern`](https://omni-automation.com/omnifocus/OF-API.html#UnderlinePattern) *read-only*

Alternating dashes and pairs of dots.

### `var Dot` → [`UnderlinePattern`](https://omni-automation.com/omnifocus/OF-API.html#UnderlinePattern) *read-only*

Dotted line.

### `var Solid` → [`UnderlinePattern`](https://omni-automation.com/omnifocus/OF-API.html#UnderlinePattern) *read-only*

A continuous line.

### `var all` → `Array` of [`UnderlinePattern`](https://omni-automation.com/omnifocus/OF-API.html#UnderlinePattern) *read-only*

# `UnderlineStyle`

## Class Properties

### `var Double` → [`UnderlineStyle`](https://omni-automation.com/omnifocus/OF-API.html#UnderlineStyle) *read-only*

Two lines.

### `var None` → [`UnderlineStyle`](https://omni-automation.com/omnifocus/OF-API.html#UnderlineStyle) *read-only*

No underline.

### `var Single` → [`UnderlineStyle`](https://omni-automation.com/omnifocus/OF-API.html#UnderlineStyle) *read-only*

A single line.

### `var Thick` → [`UnderlineStyle`](https://omni-automation.com/omnifocus/OF-API.html#UnderlineStyle) *read-only*

A single thick line.

### `var all` → `Array` of [`UnderlineStyle`](https://omni-automation.com/omnifocus/OF-API.html#UnderlineStyle) *read-only*

# `Version`

## Constructors

### `new Version(versionString: String)` → [`Version`](https://omni-automation.com/omnifocus/OF-API.html#Version)

Parses a string representation of a `Version` and returns an instance, or throws an error if the string isn’t a valid version.

## Instance Functions

### `function equals(version: [Version](https://omni-automation.com/omnifocus/OF-API.html#Version))` → `Boolean`

Returns true if the receiving `Version` is equal to the argument `Version`.

### `function atLeast(version: [Version](https://omni-automation.com/omnifocus/OF-API.html#Version))` → `Boolean`

Returns true if the receiving `Version` is at the same as or newer than the argument `Version`.

### `function isAfter(version: [Version](https://omni-automation.com/omnifocus/OF-API.html#Version))` → `Boolean`

Returns true if the receiving `Version` is strictly after the argument `Version`.

### `function isBefore(version: [Version](https://omni-automation.com/omnifocus/OF-API.html#Version))` → `Boolean`

Returns true if the receiving `Version` is strictly before the argument `Version`.

## Instance Properties

### `var versionString` → `String` *read-only*

Returns as an opaque string representation of the `Version`, suitable for display or logging. This should never be used in comparisons of any sort.

# `Window`

## Instance Functions

### `function close()`

# `DocumentWindow` : [`Window`](https://omni-automation.com/omnifocus/OF-API.html#Window)

## Instance Functions

### `function selectObjects(objects: Array of [DatabaseObject](https://omni-automation.com/omnifocus/OF-API.html#DatabaseObject))`

Clears the current selection and then selects the given objects, if present in the current perspective of this window. On iOS, if `objects` contains more than one object, this will put the outline view into edit mode to accomodate multiple selection.

### `function forecastDayForDate(date: Date)` → [`ForecastDay`](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay)

Returns a `ForecastDay` object that encompasses `date`. This will throw an error if Forecast is not the current perspective in this window.

### `function selectForecastDays(days: Array of [ForecastDay](https://omni-automation.com/omnifocus/OF-API.html#ForecastDay))`

Selects the days in the Forecast picker represented by `days`. On iOS, only the first day is selected, and the rest are ignored. This will throw an error if Forecast is not the current perspective in this window.

## Instance Properties

### `var content` → [`ContentTree`](https://omni-automation.com/omnifocus/OF-API.html#ContentTree) or `null` *read-only*

The tree of nodes representing the content area of the window.

### `var focus` → [`SectionArray`](https://omni-automation.com/omnifocus/OF-API.html#SectionArray) or `null`

The Folders and Projects that the window is focusing on, limiting the sidebar to show only these items. Focus is only supported on macOS- getting or setting this on iOS results in an error.

### `var inspectorVisible` → `Boolean`

Whether the inspector is currently visible in the window. On iOS, showing this pane may implicitly hide other panes and may be only transiently visible, depending on the available space.

### `var isTab` → `Boolean` *read-only*

Whether or not this window is a tab. This is not available on iOS.

### `var perspective` → [`Perspective.BuiltIn`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.BuiltIn) or [`Perspective.Custom`](https://omni-automation.com/omnifocus/OF-API.html#Perspective.Custom) or `null`

### `var selection` → [`Selection`](https://omni-automation.com/omnifocus/OF-API.html#Selection) *read-only*

The current selection in the window.

### `var sidebar` → [`SidebarTree`](https://omni-automation.com/omnifocus/OF-API.html#SidebarTree) or `null` *read-only*

The tree of nodes representing the sidebar of the window.

### `var sidebarVisible` → `Boolean`

Whether the sidebar is currently visible in the window. On iOS, showing this pane may implicitly hide other panes and may be only transiently visible, depending on the available space.

### `var tabGroupWindows` → `Array` of [`DocumentWindow`](https://omni-automation.com/omnifocus/OF-API.html#DocumentWindow) *read-only*

The array of sibling `Window` objects that are in tabs alongside this `Window`. If `isTab` is false, then this will return an array that solely contains this `Window`. This is not available on iOS.

# `XML`

# `XML.Document`

## Class Functions

### `function fromData(data: [Data](https://omni-automation.com/omnifocus/OF-API.html#Data), whitespaceBehavior: [XML.WhitespaceBehavior](https://omni-automation.com/omnifocus/OF-API.html#XML.WhitespaceBehavior) or null)` → [`XML.Document`](https://omni-automation.com/omnifocus/OF-API.html#XML.Document)

Parse the given data as an XML document.

## Constructors

### `new XML.Document(rootElement: String or [XML.Element](https://omni-automation.com/omnifocus/OF-API.html#XML.Element), configuration: [XML.Document.Configuration](https://omni-automation.com/omnifocus/OF-API.html#XML.Document.Configuration) or null)` → [`XML.Document`](https://omni-automation.com/omnifocus/OF-API.html#XML.Document)

Returns a new `XML.Document` with the given root element and configuration.

## Instance Functions

### `function xmlData()` → [`Data`](https://omni-automation.com/omnifocus/OF-API.html#Data)

Encodes the document as XML.

### `function addElement(name: String, function: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function)(‍‍) or null)`

Appends a new element with the given name. If a function is passed, it is pushed it on the current element stack, the supplied function is called, and then the element is popped off the stack.

### `function appendString(string: String)`

Appends the given string as a child of `topElement`.

### `function setAttribute(attribute: String, value: String or null)`

Sets the specified attribute on `topElement`.

## Instance Properties

### `var dtdPublicID` → `String` or `null` *read-only*

### `var dtdSystemID` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null` *read-only*

### `var rootElement` → [`XML.Element`](https://omni-automation.com/omnifocus/OF-API.html#XML.Element) *read-only*

### `var schemaID` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null` *read-only*

### `var schemaNamespace` → `String` or `null` *read-only*

### `var stringEncoding` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding) *read-only*

### `var topElement` → [`XML.Element`](https://omni-automation.com/omnifocus/OF-API.html#XML.Element) *read-only*

Returns the element at the top of the current element stack. Intially this is the root element, but when `addElement()` is called, it is temporarily updated to the new element (possibly recursively).

### `var whitespaceBehavior` → [`XML.WhitespaceBehavior`](https://omni-automation.com/omnifocus/OF-API.html#XML.WhitespaceBehavior) *read-only*

# `XML.Document.Configuration`

## Constructors

### `new XML.Document.Configuration()` → [`XML.Document.Configuration`](https://omni-automation.com/omnifocus/OF-API.html#XML.Document.Configuration)

Returns a new `XML.Document.Configuration` with default settings.

## Instance Properties

### `var dtdPublicID` → `String` or `null`

### `var dtdSystemID` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null`

### `var schemaID` → [`URL`](https://omni-automation.com/omnifocus/OF-API.html#URL) or `null`

### `var schemaNamespace` → `String` or `null`

### `var stringEncoding` → [`StringEncoding`](https://omni-automation.com/omnifocus/OF-API.html#StringEncoding)

### `var whitespaceBehavior` → [`XML.WhitespaceBehavior`](https://omni-automation.com/omnifocus/OF-API.html#XML.WhitespaceBehavior) or `null`

# `XML.Element`

## Constructors

### `new XML.Element(name: String)` → [`XML.Element`](https://omni-automation.com/omnifocus/OF-API.html#XML.Element)

Returns a new `XML.Element` with the given name.

## Instance Functions

### `function childAtIndex(childIndex: Number)` → `String` or [`XML.Element`](https://omni-automation.com/omnifocus/OF-API.html#XML.Element) or `null`

Returns the child at the given index, or nil if the index is past the last child.

### `function insertChild(child: String or [XML.Element](https://omni-automation.com/omnifocus/OF-API.html#XML.Element), childIndex: Number)`

Inserts the new child at the specified index. If the index is past the end of the current children, it is appended instead.

### `function appendChild(child: String or [XML.Element](https://omni-automation.com/omnifocus/OF-API.html#XML.Element))`

Adds the new item to the end of the children.

### `function removeChildAtIndex(childIndex: Number)`

Removes the child at the given index. If the index is past the end of the current children, no removal occurs.

### `function removeAllChildren()`

Removes any existing children.

### `function firstChildNamed(name: String)` → [`XML.Element`](https://omni-automation.com/omnifocus/OF-API.html#XML.Element) or `null`

Returns the first child element with the given name, or `null` if there is no such child.

### `function firstChildAtPath(path: String)` → [`XML.Element`](https://omni-automation.com/omnifocus/OF-API.html#XML.Element) or `null`

Given a `path` which is a string separated by `"/"`, returns the first element at that path.

### `function firstChildWithAttribute(attribute: String, value: String)` → [`XML.Element`](https://omni-automation.com/omnifocus/OF-API.html#XML.Element) or `null`

Returns the first child with an attribute set to the given value.

### `function attributeNamed(name: String)` → `String` or `null`

Returns the value of the the given attribute or `null` if no value has been assigned.

### `function setAttribute(name: String, value: String or null)`

Sets the value for the specified attribute. If the element already had a value for this attribute, it is replaced in place. If there previously was no value for this attribute, the attribute is appended to `attributeNames`. If the new value is `null`, the attribute is removed.

### `function apply(function: [Function](https://omni-automation.com/omnifocus/OF-API.html#Function)(‍node: String or [XML.Element](https://omni-automation.com/omnifocus/OF-API.html#XML.Element)‍) → [_omnijs_AnonymousProxy](https://omni-automation.com/omnifocus/OF-API.html#_omnijs_AnonymousProxy) or null)` → [`_omnijs_AnonymousProxy`](https://omni-automation.com/omnifocus/OF-API.html#_omnijs_AnonymousProxy) or `null`

Calls the supplied function for each child element or string in the receiver (including the receiver), passing that child as the single argument. The supplied function can optionally return a `ApplyResult` to skip enumeration of some elements.

## Instance Properties

### `var attributeCount` → `Number` *read-only*

Returns the number of attributes assigned to this element.

### `var attributeNames` → `Array` of `String` *read-only*

Returns the names of the attributes in the order they were added to the element.

### `var children` → `Array` of `String` or [`XML.Element`](https://omni-automation.com/omnifocus/OF-API.html#XML.Element)

The current child strings and elements.

### `var childrenCount` → `Number` *read-only*

Returns the current count of child strings and elements.

### `var lastChild` → `String` or [`XML.Element`](https://omni-automation.com/omnifocus/OF-API.html#XML.Element) or `null` *read-only*

Returns the last child of the element, or `null` if there are no children.

### `var name` → `String` *read-only*

Returns the name of the element.

### `var stringContents` → `String` *read-only*

Gathers all the immediate and descendent string children and returns them concatenated them as single string.

# `XML.WhitespaceBehavior`

## Constructors

### `new XML.WhitespaceBehavior(defaultBehavior: [XML.WhitespaceBehavior.Type](https://omni-automation.com/omnifocus/OF-API.html#XML.WhitespaceBehavior.Type))` → [`XML.WhitespaceBehavior`](https://omni-automation.com/omnifocus/OF-API.html#XML.WhitespaceBehavior)

Returns a new `XML.WhitespaceBehavior` with the given default type.

## Instance Functions

### `function setBehaviorForElementName(behavior: [XML.WhitespaceBehavior.Type](https://omni-automation.com/omnifocus/OF-API.html#XML.WhitespaceBehavior.Type), elementName: String)`

### `function behaviorForElementName(elementName: String)` → [`XML.WhitespaceBehavior.Type`](https://omni-automation.com/omnifocus/OF-API.html#XML.WhitespaceBehavior.Type)

## Instance Properties

### `var defaultBehavior` → [`XML.WhitespaceBehavior.Type`](https://omni-automation.com/omnifocus/OF-API.html#XML.WhitespaceBehavior.Type) *read-only*

# `XML.WhitespaceBehavior.Type`

## Class Properties

### `var Auto` → [`XML.WhitespaceBehavior.Type`](https://omni-automation.com/omnifocus/OF-API.html#XML.WhitespaceBehavior.Type) *read-only*

Inherit the behavior from the enclosing element

### `var Ignore` → [`XML.WhitespaceBehavior.Type`](https://omni-automation.com/omnifocus/OF-API.html#XML.WhitespaceBehavior.Type) *read-only*

Ignore whitespace

### `var Preserve` → [`XML.WhitespaceBehavior.Type`](https://omni-automation.com/omnifocus/OF-API.html#XML.WhitespaceBehavior.Type) *read-only*

Preserve whitespace

### `var all` → `Array` of [`XML.WhitespaceBehavior.Type`](https://omni-automation.com/omnifocus/OF-API.html#XML.WhitespaceBehavior.Type) *read-only*